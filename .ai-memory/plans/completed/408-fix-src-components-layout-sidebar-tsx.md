# Subtask 408: Fix violations in src/components/layout/Sidebar.tsx

Target File: `src/components/layout/Sidebar.tsx`

## Violations

- **Line 43**: abbreviations - Invalid abbreviation casing
  `{ to: "/api-explorer", label: "API Explorer", icon: Code2 },`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

