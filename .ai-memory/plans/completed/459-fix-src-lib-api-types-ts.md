Status: completed

# Subtask 459: Fix violations in src/lib/api/types.ts

Target File: `src/lib/api/types.ts`

## Violations

- **Line 1**: abbreviations - Invalid abbreviation casing
  `// API types — all interfaces and type aliases for the WP Plugin Publish API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112**: abbreviations - Invalid abbreviation casing
  `/** Common fields observed across API request bodies */`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 202**: abbreviations - Invalid abbreviation casing
  `// Core API types`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 691**: abbreviations - Invalid abbreviation casing
  `/** For incrementals: the parent full snapshot's ID */`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 789**: abbreviations - Invalid abbreviation casing
  `// The API methods use those types via the hook layer.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 790**: abbreviations - Invalid abbreviation casing
  `// These are lightweight types for the raw API response shape.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

