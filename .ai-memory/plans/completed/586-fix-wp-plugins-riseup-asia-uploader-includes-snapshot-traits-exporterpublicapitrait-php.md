# Subtask 586: Fix violations in wp-plugins/riseup-asia-uploader/includes/Snapshot/Traits/ExporterPublicApiTrait.php

Status: completed

Target File: `wp-plugins/riseup-asia-uploader/includes/Snapshot/Traits/ExporterPublicApiTrait.php`

## Violations

- **Line 3**: abbreviations - Invalid abbreviation casing
  `* ExporterPublicApiTrait — public API methods for snapshot ZIP export.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32**: abbreviations - Invalid abbreviation casing
  `* @param int $fullSnapshotId The full snapshot's ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148**: abbreviations - Invalid abbreviation casing
  `* Generate a time-limited download URL for an export.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

