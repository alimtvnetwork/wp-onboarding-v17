Status: completed
# Subtask 646: Fix violations in wp-plugins/riseup-asia-uploader/vendor/phpunit/phpunit/src/Framework/Assert/Functions.php  Target File: `wp-plugins/riseup-asia-uploader/vendor/phpunit/phpunit/src/Framework/Assert/Functions.php`  ## Violations  - **Line 2808**: abbreviations - Invalid abbreviation casing   `* Asserts that a string is a valid JSON string.`   **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).  - **Line 2824**: abbreviations - Invalid abbreviation casing   `* Asserts that two given JSON encoded objects or arrays are equal.`   **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).  - **Line 2840**: abbreviations - Invalid abbreviation casing   `* Asserts that two given JSON encoded objects or arrays are not equal.`   **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).  - **Line 2856**: abbreviations - Invalid abbreviation casing   `* Asserts that the generated JSON encoded object and the content of the given file are equal.`   **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).  - **Line 2872**: abbreviations - Invalid abbreviation casing   `* Asserts that the generated JSON encoded object and the content of the given file are not equal.`   **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).  - **Line 2888**: abbreviations - Invalid abbreviation casing   `* Asserts that two JSON files are equal.`   **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).  - **Line 2904**: abbreviations - Invalid abbreviation casing   `* Asserts that two JSON files are not equal.`   **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL). 
