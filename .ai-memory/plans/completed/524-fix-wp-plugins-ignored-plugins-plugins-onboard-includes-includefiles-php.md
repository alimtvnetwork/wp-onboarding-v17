Status: completed

# Subtask 524: Fix violations in wp-plugins/ignored-plugins/plugins-onboard/includes/IncludeFiles.php

Target File: `wp-plugins/ignored-plugins/plugins-onboard/includes/IncludeFiles.php`

## Violations

- **Line 61**: abbreviations - Invalid abbreviation casing
  `/** API & Admin */`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62**: abbreviations - Invalid abbreviation casing
  `const API      = 'api/Api.php';`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76**: abbreviations - Invalid abbreviation casing
  `// ─── Public API ─────────────────────────────────────────────────`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

