# Go Generic Enforce Audit — 2026-02-14

## Completed Phases

### Phase 1: Concrete typed structs ✅
- `apperror.ErrorContext` → `ErrorDiagnostic` struct with 15 typed fields
- `dbops.ContextFields` → `OperationFields` struct with 20+ fields
- `ws.IncomingMessage.Data` → `json.RawMessage` (parse boundary)
- `models.ErrorHistory.Context` / `ErrorLog.Context` → `json.RawMessage`
- `response_types.go`: `ActionResponse.ID` → `string`; paginated responses typed (`[]*middleware.RequestSession`, `[]models.ErrorHistory`, etc.)
- `DirectoryScanResult`, `BulkBootstrapSiteResult` moved to `response_types.go`
- `adapter_plugin.ScanDirectory` returns `*plugin.ScanResult` instead of `any`

### Phase 2: Generic envelope + broadcast ✅
- `envelope.Success[T]`, `Created[T]`, `List[T]`, `ListUnpaginated[T]` — generic with `[T any]`
- `envelope.Deleted` / `Error` use `[]struct{}{}`
- `ws.Broadcast[T]` and `BroadcastWithSession[T]` package-level generic functions
- `respondSuccess[T]`, `respondCreated[T]`, `respondList[T]`, `respondListUnpaginated[T]` in `response.go`

### Phase 3: WithContext migration ✅
- 39 `WithContext(key, value)` calls migrated to typed setters across 7 files
- Added `WithFile(string)` setter
- Removed deprecated `WithContext(string, any)` compat shim entirely
- Zero `any` parameters remain in the apperror public API

### Phase 4: Typed broadcast events ✅
- Created `ws/event_types.go` with 28 typed event structs across all domains
- Migrated all `map[string]any` broadcast calls to typed structs + `ws.Broadcast[T]` in 8 files:
  - `watcher/service.go` — 4 calls (AutoPublishTriggered/Failed/Complete, FileChangeBatch)
  - `git/service.go` — 9 calls (Pull/Build/Commit/Push started/failed/complete)
  - `sync/service.go` — 1 call (SyncStepProgress)
  - `publish/service.go` — 3 calls (StageProgress, StageStatus, StageComplete)
  - `publish/scheduler.go` — 3 calls (JobStarted/Complete, JobsUpdate)
  - `publish/queue.go` — 1 call (QueueStatus)
  - `version/service.go` — 3 calls (VersionCreated, RollbackStarted/Complete)
  - `e2e/service.go` — 5 calls (RunStarted/Completed, TestStarted/Completed, Abort)
- Removed deprecated `Hub.Broadcast(string, any)` and `Hub.BroadcastWithSession(string, any, string)` methods
- Fixed `version.Rollback` return type from `any` to `*ws.RollbackCompleteData`
- Fixed 3 remaining `WithContext` calls (version + e2e) → typed setters
- Converted `SessionLogger.Log` parameter from `map[string]interface{}` to `map[string]any`

## Remaining Work

### Phase 5: interface{} → any cleanup ✅
- 26 `map[string]interface{}` → `map[string]any` replacements in `publish/service.go`
- Fixed last deprecated `Hub.BroadcastWithSession` call in `broadcastProgressWithSession` → `ws.BroadcastWithSession[T]`
- `Publish()` parameter `opts interface{}` → `opts any`
- `getString`/`getBool` helper params, `sessionLog` details param converted
- Zero `map[string]interface{}` remaining in production code (only `envelope_test.go`)

## Remaining Work

### Phase 6: Test files cleanup (optional per GE-5)
- `envelope_test.go` — 1 instance (`map[string]interface{}` in JSON decode)

### Phase 7: Service return types
- `version.GetVersions` / `GetVersion` return `any` — needs typed DB query returns
