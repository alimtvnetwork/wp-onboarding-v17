# Memory: architecture/backend/go-type-remediation-progress
Updated: 2026-03-10

The Go backend remediation (Feature F) is now COMPLETE across all packages with ZERO remaining exceptions:

## Completed Packages
1. **handlers** (~999 instances) — Typed response structs in `response_types.go`, `any` alias throughout
2. **services** (~942 instances) — All 18 service files migrated to `any`
3. **ws** (~30 instances) — Created 10 typed broadcast data structs (SyncProgressData, ScanProgressData, PublishProgressData, FileChangeData, ErrorData, ConnectionTestProgressData, LogData, OperationLogData, ConnectionConfirmation, IncomingMessage) replacing all `map[string]interface{}` literals
4. **envelope** — `interface{}` → `any` in all builder functions (Success, Created, Deleted, List, Error)
5. **models** — Named type aliases `ErrorLogContext`, `ErrorHistoryContext`, `ErrorHistoryRequestBody` for `map[string]any`
6. **config** — `map[string]interface{}` → `map[string]any` in seedFromConfig
7. **database** — `PluginVersionRow` type alias, `any` in SetSettingIfNotExists and query args
8. **dbops** — `ContextFields` type alias for `map[string]any`, `any` in all Exec/Find/Create signatures
9. **logger** — `interface{}` → `any` in all variadic log methods
10. **apperror** — `ErrorContext` type alias, `any` in WithContext
11. **wordpress/client** — `ProgressDetails` changed from `map[string]any` to `json.RawMessage` type alias
12. **wordpress** — All `map[string]string{}` request body literals replaced with typed structs (`PluginSlugRequest`, `PluginFileRequest`, `PluginFileDeleteRequest`, `PluginFileReplaceRequest`, `SyncRequestBody`)
13. **cmd/server/main** — `any` in E2E adapter signatures, broadcast callback, progress callback

## ProgressDetails Migration (2026-02-25)
The final `map[string]any` exception (`ProgressDetails //nolint:ge2`) has been eliminated:
- `ProgressDetails` is now a type alias for `json.RawMessage`
- 18 typed progress detail structs created in `progress_details.go`: `URLProgress`, `URLErrorProgress`, `URLStatusProgress`, `SiteNameProgress`, `AuthInitProgress`, `AuthHintProgress`, `AuthBodyProgress`, `UserAuthProgress`, `UserRolesProgress`, `WriteTestProgress`, `UploadInitProgress`, `UploadBodyProgress`, `ResponseProgress`, `TokenProgress`, `ZipUploadProgress`, `SyncInitProgress`
- 5 typed request body structs created in `request_types.go`: `PluginSlugRequest`, `PluginFileRequest`, `PluginFileDeleteRequest`, `PluginFileReplaceRequest`, `SyncRequestBody`
- All ~30 call sites in `client.go`, `uploader.go`, `uploader_files.go`, `remote_files.go` updated to use `toProgress()` with typed structs
- All `map[string]string{}` literals across `uploader_lifecycle.go`, `uploader_export.go`, `uploader_files.go`, `remote_files.go` replaced with typed request structs
- Callback consumers in `site/service.go` and `site/service_bootstrap.go` simplified (no redundant `json.Marshal`)

## CleanupRetentionDetail Fix (2026-03-10)
- `CleanupRetentionResult.Details` changed from `[]any` to `[]CleanupRetentionDetail`
- `CleanupRetentionDetail` struct created with typed fields: `SnapshotId int64`, `Filename string`, `Size int64`, `Reason string`
- This was the last `[]any` / `map[string]any` violation in backend production code

## Final Audit (2026-03-10)
- **0** `interface{}` instances in production code
- **0** `map[string]any` in production code (49 matches are comments only)
- **0** `[]any` in production code
- **1** acceptable `map[string]any` in `tools/consistency-checker` (schema-less config params, per GE-5)
- **Full GE-1 compliance confirmed**

## Remaining (test files only — acceptable per GE-5)
- `envelope_test.go` — 3 instances (type assertions in test assertions)
- `remote_files_test.go` — 1 instance (JSON decode in test)
