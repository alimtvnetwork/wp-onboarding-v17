# Compact Report (Markdown) — DEFAULT Copy Format

> **Parent:** [Copy Formats Index](./01-index.md)
> **Version:** 3.2.0
> **Updated:** 2026-03-31
> **Purpose:** The default copy format — instant, no API call. Built entirely from CapturedError fields already in memory.

---

> See also: [03-error-modal-reference.md § 11 – Error Report Generation](../04-error-modal-reference.md#11-error-report-generation) for the architectural overview, stripping rules, and `errorLogAdapter.ts` bridging pattern.

Generated by `src/components/errors/errorReportGenerator.ts` → `generateCompactReport()`.

Both `GlobalErrorModal` and `ErrorDetailModal` use this format as their primary copy action. The `ErrorDetailModal` converts its `ErrorLog` objects via `errorLogToCapturedError()` (from `src/components/errors/errorLogAdapter.ts`) before calling `generateCompactReport()`.

---

## Design Principles

| Principle | Detail |
|-----------|--------|
| **No API calls** | Backend section is built from `envelopeErrors`, `envelopeMethodsStack`, `backendStackTrace` already on the `CapturedError` object |
| **Stripped execution chain** | Removes timestamps and base URLs → `GET /sites/2/mappings` instead of `[12:58:22 AM] ⬡ GET http://localhost:8080/api/v1/sites/2/mappings` |
| **Deduplicated chain** | Consecutive identical requests are collapsed to a single entry |
| **Essential sections only** | Omits: ID, Git Commit, Build Time, Timestamp, Invocation Chain, User Interaction Path (numbered), Target Site, Session Info, Details, Request Body, Backend Execution Logs, Parsed Stack Frames, Location |
| **Delegated Server Info included** | When `envelopeErrors.DelegatedRequestServer` exists, it is rendered in the Backend error.log.txt section — critical for proxy-chain debugging |

---

## Sections Included (in order)

| Section | Source | Always Present |
|---------|--------|---------------|
| App metadata (name + version only) | `useVersionInfo()` | ✅ |
| Code, Level | `CapturedError` identity fields | ✅ |
| Page | `error.route` + `error.routeComponent` | If available |
| User Interaction (arrow) | `error.uiClickPathArrow` | If click tracking active |
| Trigger Context | `triggerComponent`, `triggerAction`, `context.source` | If available |
| Message | `error.message` | ✅ |
| Request | `error.method`, `error.endpoint`, `error.responseStatus` | If API error |
| Frontend Execution Chain | `error.executionLogsFormatted` (stripped) | If debug mode enabled |
| Context | `error.context` | If available |
| Frontend Stack Trace | `error.stackTrace` | If JS error |
| Backend error.log.txt | Built from `CapturedError` fields (see below) | If backend data available |

---

## Backend error.log.txt Section (Built from CapturedError)

This section is **not fetched from the API**. It is constructed from:

| CapturedError Field | Maps To |
|---------------------|---------|
| `error.backendStackTrace` | Go Backend Stack |
| `error.envelopeMethodsStack.Backend[]` | Go Methods Stack (numbered) |
| `error.envelopeErrors.DelegatedRequestServer` | Delegated Server Info block |
| `error.envelopeErrors.DelegatedRequestServer.Response` | Response Body |
| `error.envelopeErrors.BackendMessage` | Backend Error line |
| `error.responseStatus` | HTTP status in header |
| `error.method`, `error.endpoint` | Request method/URL in header |
| `error.message` | Error Message line |
| `error.createdAt` | Timestamp in header |

### ⚠️ CRITICAL: Delegated Server Info Must Be Included

When `error.envelopeErrors.DelegatedRequestServer` exists, the compact report **MUST** render the full Delegated Server Info block including:

1. **Endpoint** — the downstream URL that was proxied to
2. **Method** — HTTP method used for the delegated request
3. **Status** — HTTP status code from the delegated server
4. **Stacktrace** — PHP/downstream stack trace (if returned)
5. **RequestBody** — body sent to delegated server (if POST/PUT/PATCH)
6. **Additional Message** — human-readable error context from downstream

Without this section, the recipient cannot diagnose root-cause errors on the delegated WordPress/PHP server.

---

## Complete Sample

````markdown

## Compact Error Report

**App:** WP Publish v1.19.4

**Code:** E9003

**Level:** error

### Page

`/sites` `<Sites>`

### User Interaction

```
Link "Plugins" → Link "Sites" → Button "Manage database snapshots"
```

### Trigger Context

**Component:** QueryClient

**Action:** async_operation

**Source:** App.showGlobalError

### Message

[E3001] failed to fetch snapshot settings: get snapshot settings (GET localhost:8080/riseup-asia-uploader/v1/snapshots/settings): status 403

### Request

**GET** http://localhost:8080/api/v1/sites/1/snapshots/settings
**Status:** 500

### Frontend Execution Chain

```
GET /sites/2/mappings
GET /sites/1/mappings
GET /error-history?limit=100
GET /plugins
GET /sites
GET /sites/1/snapshots
GET /sites/1/snapshots/settings
GET /sites/1/snapshots/providers
POST /error-history
GET /error-history?limit=100
```

### Context

```json
{
  "source": "App.showGlobalError",
  "TriggerComponent": "QueryClient",
  "TriggerAction": "async_operation"
}
```

### Frontend Stack Trace

```
Error: [E3001] failed to fetch snapshot settings: get snapshot settings (GET http://localhost:8080/riseup-asia-uploader/v1/snapshots/settings): status 403
    at queryFn (http://localhost:8080/assets/index-Bk9djDqy.js:1052:5917)
```

---

*Generated by WP Plugin Publish Error Reporter*

### Backend error.log.txt

```
[2026-02-12 00:53:34] HTTP 500 GET FAILED
  Requested To: GET http://localhost:8080/api/v1/sites/1/snapshots/settings
  Error Message: [E3001] failed to fetch snapshot settings: get snapshot settings (GET https://demoat.attoproperty.com.au/riseup-asia-uploader/v1/snapshots/settings): status 403
  Backend Error: [E3025] [E3001] failed to fetch snapshot settings...
  Go Backend Stack:
    handlers.init.handleSiteActionById.func63 at handler_factory.go:107
    api.NewServer.SessionLogging.func3.1 at session_logging.go:107
    api.NewServer.Recovery.func2.1 at middleware.go:245
    api.NewServer.Logging.func1.1 at middleware.go:66
    middleware.CORS.func1 at middleware.go:45
  Go Methods Stack:
    #0 handlers.init.handleSiteActionById.func63 at handler_factory.go:107
    #1 api.NewServer.SessionLogging.func3.1 at session_logging.go:107
    #2 api.NewServer.Recovery.func2.1 at middleware.go:245
    #3 api.NewServer.Logging.func1.1 at middleware.go:66
    #4 middleware.CORS.func1 at middleware.go:45
  Delegated Server Info:
    Endpoint: "https://demoat.attoproperty.com.au/wp-json/riseup-asia-uploader/v1/snapshots/settings"
    Method: "GET"
    Status: 403
    Stacktrace:
        #0 riseup-asia-uploader.php(1098): FileLogger->error()
        #1 class-wp-hook.php(341): Plugin->enrichErrorResponse()
        #2 plugin.php(205): WP_Hook->apply_filters()
        #3 class-wp-rest-server.php(462): apply_filters()
        #4 rest-api.php(467): WP_REST_Server->serve_request()
    Additional Message:
        Endpoint 'snapshots' is not enabled in plugin settings.
  Response Body:
    {
      "Status": { "IsSuccess": false, "IsFailed": true, "Code": 500, ... },
      "Errors": {
        "BackendMessage": "[E3025] [E3001] failed to fetch snapshot settings...",
        "DelegatedRequestServer": {
          "DelegatedEndpoint": "https://demoat.attoproperty.com.au/wp-json/riseup-asia-uploader/v1/snapshots/settings",
          "Method": "GET",
          "StatusCode": 403,
          "AdditionalMessages": "Endpoint 'snapshots' is not enabled..."
        }
      }
    }
```
````

---

## Execution Chain Stripping Rules

```
Input:  "[12:58:22 AM] ⬡ GET http://localhost:8080/api/v1/sites/2/mappings"
Output: "GET /sites/2/mappings"

Rules:
1. Remove timestamp prefix: /^\[.*?\]\s*⬡?\s*/ → ""
2. Remove base URL up to /api/v1: /https?:\/\/[^/]+\/api\/v1/ → ""
3. Remove any remaining base URL: /https?:\/\/[^/]+/ → ""
4. Deduplicate consecutive identical lines
```

---

## Generator Function Signature

```typescript
function generateCompactReport(
  error: CapturedError,
  app?: { appName: string; appVersion: string; gitCommit?: string; buildTime?: string },
): string
```

---

*Compact report format — updated: 2026-03-31*
