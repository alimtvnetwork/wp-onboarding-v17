# Subtask 431: Fix violations in src/components/sites/EditSiteDialog.tsx

Target File: `src/components/sites/EditSiteDialog.tsx`

## Violations

- **Line 255**: abbreviations - Invalid abbreviation casing
  `<Label htmlFor="edit-url">Site URL</Label>`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

