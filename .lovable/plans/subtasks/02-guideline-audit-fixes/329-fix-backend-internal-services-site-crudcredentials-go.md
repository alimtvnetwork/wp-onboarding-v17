# Subtask 329: Fix violations in backend/internal/services/site/CrudCredentials.go

Target File: `backend/internal/services/site/CrudCredentials.go`

## Violations

- **Line 56**: abbreviations - Invalid abbreviation casing
  `// getCredentialById fetches a single credential by ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

