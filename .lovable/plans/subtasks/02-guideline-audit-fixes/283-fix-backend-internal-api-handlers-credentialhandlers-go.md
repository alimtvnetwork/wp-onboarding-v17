# Subtask 283: Fix violations in backend/internal/api/handlers/CredentialHandlers.go

Target File: `backend/internal/api/handlers/CredentialHandlers.go`

## Violations

- **Line 25**: abbreviations - Invalid abbreviation casing
  `// CredentialResponse is the safe JSON response for a credential (no password).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

