# Subtask 325: Fix violations in backend/internal/services/publish_history/Queries.go

Target File: `backend/internal/services/publish_history/Queries.go`

## Violations

- **Line 1**: abbreviations - Invalid abbreviation casing
  `// Package publishhistory — SQL query constants.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

