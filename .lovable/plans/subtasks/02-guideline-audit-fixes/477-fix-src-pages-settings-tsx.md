# Subtask 477: Fix violations in src/pages/Settings.tsx

Target File: `src/pages/Settings.tsx`

## Violations

- **Line 648**: abbreviations - Invalid abbreviation casing
  `Failed API calls retry with exponential backoff`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 987**: abbreviations - Invalid abbreviation casing
  `sessionStorage.setItem("remoteLogs:demoStatus", JSON.stringify(createDemoLogsStatus()));`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 988**: abbreviations - Invalid abbreviation casing
  `sessionStorage.setItem("remoteLogs:demoRetrieve", JSON.stringify(createDemoRetrieveResult()));`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

