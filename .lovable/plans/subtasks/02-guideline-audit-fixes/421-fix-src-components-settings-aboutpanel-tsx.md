# Subtask 421: Fix violations in src/components/settings/AboutPanel.tsx

Target File: `src/components/settings/AboutPanel.tsx`

## Violations

- **Line 118**: abbreviations - Invalid abbreviation casing
  `Copy API URLs and version info for support`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

