# Subtask 424: Fix violations in src/components/shared/CopyDiagnosticsButton.tsx

Target File: `src/components/shared/CopyDiagnosticsButton.tsx`

## Violations

- **Line 29**: abbreviations - Invalid abbreviation casing
  `* A small button that copies API base, WS URL, and app version`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

