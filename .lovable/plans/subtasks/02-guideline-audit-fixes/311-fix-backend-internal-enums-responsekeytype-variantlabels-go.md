# Subtask 311: Fix violations in backend/internal/enums/responsekeytype/VariantLabels.go

Target File: `backend/internal/enums/responsekeytype/VariantLabels.go`

## Violations

- **Line 161**: abbreviations - Invalid abbreviation casing
  `ResolvedUrl:           "Resolved URL",`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 191**: abbreviations - Invalid abbreviation casing
  `SiteUrl:               "Site URL",`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 196**: abbreviations - Invalid abbreviation casing
  `DownloadUrl:           "Download URL",`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 282**: abbreviations - Invalid abbreviation casing
  `RemoteUrl:             "Remote URL",`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 291**: abbreviations - Invalid abbreviation casing
  `OAuthUrl:              "OAuth URL",`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 340**: abbreviations - Invalid abbreviation casing
  `Ip:                    "IP",`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 353**: abbreviations - Invalid abbreviation casing
  `Api:                   "API",`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

