# Subtask 643: Fix violations in wp-plugins/riseup-asia-uploader/vendor/php-stubs/wordpress-stubs/wordpress-stubs.php

Target File: `wp-plugins/riseup-asia-uploader/vendor/php-stubs/wordpress-stubs/wordpress-stubs.php`

## Violations

- **Line 1314**: abbreviations - Invalid abbreviation casing
  `*              for retrieving the header image URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 1367**: abbreviations - Invalid abbreviation casing
  `*                      (the attachment ID of the image). Or an array of arguments: attachment_id,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 1411**: abbreviations - Invalid abbreviation casing
  `* @param string $cropped              Cropped image URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 1412**: abbreviations - Invalid abbreviation casing
  `* @param int    $parent_attachment_id Attachment ID of parent image.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 1425**: abbreviations - Invalid abbreviation casing
  `* @return int Attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 1432**: abbreviations - Invalid abbreviation casing
  `* new object. Returns JSON-encoded object details.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 1441**: abbreviations - Invalid abbreviation casing
  `* Given an attachment ID for a header image, updates its "last used"`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 1454**: abbreviations - Invalid abbreviation casing
  `* Given an attachment ID for a header image, unsets it as a user-uploaded`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 1498**: abbreviations - Invalid abbreviation casing
  `* Gets the ID of a previous crop from the same base image.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 1503**: abbreviations - Invalid abbreviation casing
  `* @return int|false An attachment ID if one exists. False if none.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 1536**: abbreviations - Invalid abbreviation casing
  `* The ID of the attachment post for this file.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 2319**: abbreviations - Invalid abbreviation casing
  `* It is designed to upgrade/install plugins from a local zip, remote zip URL,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 2621**: abbreviations - Invalid abbreviation casing
  `* It is designed to upgrade/install themes from a local zip, remote zip URL,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 2935**: abbreviations - Invalid abbreviation casing
  `* @param int    $current_object_id Optional. ID of the current item. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 3121**: abbreviations - Invalid abbreviation casing
  `* @param int     $current_object_id Optional. ID of the current term. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 3240**: abbreviations - Invalid abbreviation casing
  `* @param int      $current_object_id Optional. ID of the current menu item. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 3332**: abbreviations - Invalid abbreviation casing
  `* @param int      $current_object_id Optional. ID of the current menu item. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 3389**: abbreviations - Invalid abbreviation casing
  `* @param int      $current_object_id Optional. ID of the current menu item. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 3722**: abbreviations - Invalid abbreviation casing
  `* @param string $input_id ID attribute value for the search input field.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 3736**: abbreviations - Invalid abbreviation casing
  `*     @type string $url     The link URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 3866**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id          The post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 3999**: abbreviations - Invalid abbreviation casing
  `* @param bool $with_id Whether to set the ID attribute or not`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 4160**: abbreviations - Invalid abbreviation casing
  `* @global int $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 4481**: abbreviations - Invalid abbreviation casing
  `* @param int    $comment_id Comment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 4675**: abbreviations - Invalid abbreviation casing
  `* ID for a WordPress user account.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 4695**: abbreviations - Invalid abbreviation casing
  `* @param int        $user_id       WP user ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 4722**: abbreviations - Invalid abbreviation casing
  `* Otherwise, this method sends a request to the w.org Events API with location`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 4723**: abbreviations - Invalid abbreviation casing
  `* data. The API will send back a recognized location based on the data, along`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 4734**: abbreviations - Invalid abbreviation casing
  `* the opportunity to anonymize the IP before sending it to w.org, which`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 4752**: abbreviations - Invalid abbreviation casing
  `* Builds an array of args to use in an HTTP request to the w.org Events API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 4764**: abbreviations - Invalid abbreviation casing
  `* Determines the user's actual IP address and attempts to partially`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 4765**: abbreviations - Invalid abbreviation casing
  `* anonymize an IP address by converting it to a network ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 4767**: abbreviations - Invalid abbreviation casing
  `* Geolocating the network ID usually returns a similar location as the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 4768**: abbreviations - Invalid abbreviation casing
  `* actual IP, but provides some privacy for the user.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 4779**: abbreviations - Invalid abbreviation casing
  `* circumstances where the authenticity of the IP address matters. This does`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 4820**: abbreviations - Invalid abbreviation casing
  `* Caches an array of events data from the Events API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 4824**: abbreviations - Invalid abbreviation casing
  `* @param array     $events     Response body from the API request.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 4845**: abbreviations - Invalid abbreviation casing
  `* Adds formatted date and time items for each event in an API response.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 4866**: abbreviations - Invalid abbreviation casing
  `* appear in the list sooner. If a WordCamp is coming up, the API will "stick"`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 4883**: abbreviations - Invalid abbreviation casing
  `* Logs responses to Events API requests.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 5548**: abbreviations - Invalid abbreviation casing
  `*         @type string|false     $owner       Owner name or ID, or false if not available.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 5943**: abbreviations - Invalid abbreviation casing
  `*         @type string|false     $owner       Owner name or ID, or false if not available.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 6317**: abbreviations - Invalid abbreviation casing
  `*     @type string|false $owner       Owner name or ID, or false if not available.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 6366**: abbreviations - Invalid abbreviation casing
  `*         @type string|false     $owner       Owner name or ID, or false if not available.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 6757**: abbreviations - Invalid abbreviation casing
  `*         @type string|false     $owner       Owner name or ID, or false if not available.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 7238**: abbreviations - Invalid abbreviation casing
  `*         @type string|false     $owner       Owner name or ID, or false if not available.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 7344**: abbreviations - Invalid abbreviation casing
  `* Gets URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 7400**: abbreviations - Invalid abbreviation casing
  `* Core class used to implement an internal admin pointers API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 7469**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 7574**: abbreviations - Invalid abbreviation casing
  `* Handles the link URL column output.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 8038**: abbreviations - Invalid abbreviation casing
  `* Handles the ID column output.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 8441**: abbreviations - Invalid abbreviation casing
  `* Handles the ID column output.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 8581**: abbreviations - Invalid abbreviation casing
  `* Uses the transient data from the updates API to determine the known`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 8594**: abbreviations - Invalid abbreviation casing
  `* Uses the transient data from the updates API to determine the slugs of`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 8796**: abbreviations - Invalid abbreviation casing
  `* @global string $s URL encoded search term.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 8826**: abbreviations - Invalid abbreviation casing
  `* @param string $input_id ID attribute value for the search input field.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 9093**: abbreviations - Invalid abbreviation casing
  `* @param string[] $args      Associative array of URL parameters for the link.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 9311**: abbreviations - Invalid abbreviation casing
  `* List Table API: WP_Privacy_Requests_Table class`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 9349**: abbreviations - Invalid abbreviation casing
  `* Normalizes the admin URL to the current page (by request_type).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 9353**: abbreviations - Invalid abbreviation casing
  `* @return string URL to the current admin page.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 9667**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id The ID of the updated post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 9730**: abbreviations - Invalid abbreviation casing
  `* Core class used to implement an admin screen API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 9757**: abbreviations - Invalid abbreviation casing
  `* The unique ID of the screen.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 9910**: abbreviations - Invalid abbreviation casing
  `* @param string $option Option ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 9921**: abbreviations - Invalid abbreviation casing
  `* @param string $option Option ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 9973**: abbreviations - Invalid abbreviation casing
  `* @param string $id Help Tab ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 9996**: abbreviations - Invalid abbreviation casing
  `*     @type string   $id       Tab ID. Must be HTML-safe and should be unique for this menu.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 10019**: abbreviations - Invalid abbreviation casing
  `* @param string $id The help tab ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 10522**: abbreviations - Invalid abbreviation casing
  `* Tests if the SQL server is up to date.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 10572**: abbreviations - Invalid abbreviation casing
  `* Checks if the HTTP API can handle SSL/TLS requests.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 10658**: abbreviations - Invalid abbreviation casing
  `* hosts) via the HTTP API. This may create problems for users as many features are running as`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 10669**: abbreviations - Invalid abbreviation casing
  `* Tests if the REST API is accessible.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 10671**: abbreviations - Invalid abbreviation casing
  `* Various security measures may block the REST API from working, or it may have been disabled in general.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 10953**: abbreviations - Invalid abbreviation casing
  `* @param string $cropped              Cropped image URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 10954**: abbreviations - Invalid abbreviation casing
  `* @param int    $parent_attachment_id Attachment ID of parent image.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 10967**: abbreviations - Invalid abbreviation casing
  `* @return int               Attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 10999**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 11011**: abbreviations - Invalid abbreviation casing
  `* @param int               $post_id  Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 11344**: abbreviations - Invalid abbreviation casing
  `* Prints a theme from the WordPress.org API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 11351**: abbreviations - Invalid abbreviation casing
  `*     An object that contains theme data returned by the WordPress.org API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 11357**: abbreviations - Invalid abbreviation casing
  `*     @type string $preview_url    Preview URL, e.g. 'https://2021.wordpress.net/'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 11358**: abbreviations - Invalid abbreviation casing
  `*     @type string $screenshot_url Screenshot URL, e.g. 'https://wordpress.org/themes/twentytwentyone/'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 11363**: abbreviations - Invalid abbreviation casing
  `*     @type string $download_link  Theme ZIP download URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 11393**: abbreviations - Invalid abbreviation casing
  `* @param stdClass $theme A WordPress.org Theme API object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 11403**: abbreviations - Invalid abbreviation casing
  `* @param stdClass $theme A WordPress.org Theme API object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 11433**: abbreviations - Invalid abbreviation casing
  `* Site ID to generate the Users list table for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 11746**: abbreviations - Invalid abbreviation casing
  `* @param int $page Optional. Page ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 11759**: abbreviations - Invalid abbreviation casing
  `* @param int $page Optional. Page ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 15329**: abbreviations - Invalid abbreviation casing
  `* Parse a URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 15332**: abbreviations - Invalid abbreviation casing
  `* @param string $url URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 15400**: abbreviations - Invalid abbreviation casing
  `* The user's email address, usually used as the login ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 15413**: abbreviations - Invalid abbreviation casing
  `* The client ID, generated in the app definition of the service you're connecting to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 15638**: abbreviations - Invalid abbreviation casing
  `* The hostname to use in the Message-ID header and as default HELO string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 15649**: abbreviations - Invalid abbreviation casing
  `* An ID to be used in the Message-ID header.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 16042**: abbreviations - Invalid abbreviation casing
  `* The most recent Message-ID (including angular brackets).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 16103**: abbreviations - Invalid abbreviation casing
  `* Unique ID used for message ID and boundaries.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 16373**: abbreviations - Invalid abbreviation casing
  `* Return the Message-ID header of the last email.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 16375**: abbreviations - Invalid abbreviation casing
  `* but it's also the message ID of the last sent message except in`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 16724**: abbreviations - Invalid abbreviation casing
  `* Create a unique ID to use for boundaries.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 16981**: abbreviations - Invalid abbreviation casing
  `* @param string $cid         Content ID of the attachment; Use this to reference`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 17002**: abbreviations - Invalid abbreviation casing
  `* @param string $cid         Content ID of the attachment; Use this to reference`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 17165**: abbreviations - Invalid abbreviation casing
  `* Validate whether a string contains a valid value to use as a hostname or IP address.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 17168**: abbreviations - Invalid abbreviation casing
  `* @param string $host The host name or IP address to check`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 17970**: abbreviations - Invalid abbreviation casing
  `* The first capture group in each regex will be used as the ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 17971**: abbreviations - Invalid abbreviation casing
  `* MS ESMTP returns the message ID, which may not be correct for internal tracking.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 17986**: abbreviations - Invalid abbreviation casing
  `* The last transaction ID issued in response to a DATA command,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 18044**: abbreviations - Invalid abbreviation casing
  `* @param string $host    SMTP server IP or host name`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 18057**: abbreviations - Invalid abbreviation casing
  `* @param string $host    SMTP server IP or host name`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 18144**: abbreviations - Invalid abbreviation casing
  `* @param string $host The host name or IP to connect to`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 18472**: abbreviations - Invalid abbreviation casing
  `* Extract and return the ID of the last SMTP transaction based on`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 18474**: abbreviations - Invalid abbreviation casing
  `* Relies on the host providing the ID in response to a DATA command.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 18484**: abbreviations - Invalid abbreviation casing
  `* Get the queue/transaction ID of the last SMTP transaction`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 18574**: abbreviations - Invalid abbreviation casing
  `* Set cURL parameters before the data is sent`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 18576**: abbreviations - Invalid abbreviation casing
  `* @param resource|\CurlHandle $handle cURL handle`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 19518**: abbreviations - Invalid abbreviation casing
  `* IDNA URL encoder`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 19537**: abbreviations - Invalid abbreviation casing
  `* Maximum length of a IDNA URL in ASCII.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 19714**: abbreviations - Invalid abbreviation casing
  `* Checks if the given IP is a valid IPv6 address`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 20225**: abbreviations - Invalid abbreviation casing
  `* Set cURL parameters before the data is sent`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 20228**: abbreviations - Invalid abbreviation casing
  `* @param resource|\CurlHandle $handle cURL handle`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 20247**: abbreviations - Invalid abbreviation casing
  `* @param string $url Full URL we're requesting`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 20510**: abbreviations - Invalid abbreviation casing
  `*    Note: for cURL, a minimum of 1 second applies, as DNS resolution`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 20551**: abbreviations - Invalid abbreviation casing
  `* @param string|Stringable $url URL to request`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 20576**: abbreviations - Invalid abbreviation casing
  `* - `url`: Request URL Same as the `$url` parameter to`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 20599**: abbreviations - Invalid abbreviation casing
  `*    ID from the request array (Note: this can also be overridden on a`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 20646**: abbreviations - Invalid abbreviation casing
  `* @param string $url URL to request`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 20653**: abbreviations - Invalid abbreviation casing
  `* @throws \WpOrg\Requests\Exception When the $url is not an http(s) URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 20662**: abbreviations - Invalid abbreviation casing
  `* @param string $url Original request URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 20803**: abbreviations - Invalid abbreviation casing
  `* URL requested`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 20846**: abbreviations - Invalid abbreviation casing
  `* JSON decode the response body.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 20852**: abbreviations - Invalid abbreviation casing
  `* @param bool|null $associative Optional. When `true`, JSON objects will be returned as associative arrays;`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 20853**: abbreviations - Invalid abbreviation casing
  `*                               When `false`, JSON objects will be returned as objects.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 20854**: abbreviations - Invalid abbreviation casing
  `*                               When `null`, JSON objects will be returned as associative arrays`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21031**: abbreviations - Invalid abbreviation casing
  `* options and URL properties together. A base URL can be set for all requests,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21040**: abbreviations - Invalid abbreviation casing
  `* Base URL for requests`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21077**: abbreviations - Invalid abbreviation casing
  `* @param string|Stringable|null $url Base URL for requests`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21190**: abbreviations - Invalid abbreviation casing
  `* @param string $url URL to request`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21299**: abbreviations - Invalid abbreviation casing
  `* @param string $url URL to request`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21301**: abbreviations - Invalid abbreviation casing
  `* @param string|array $data Data to send either as the POST body, or as parameters in the URL for a GET/HEAD`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21327**: abbreviations - Invalid abbreviation casing
  `* cURL HTTP transport`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21350**: abbreviations - Invalid abbreviation casing
  `* @var array cURL information array, see {@link https://www.php.net/curl_getinfo}`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21354**: abbreviations - Invalid abbreviation casing
  `* cURL version number`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21374**: abbreviations - Invalid abbreviation casing
  `* @param string|Stringable $url URL to request`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21376**: abbreviations - Invalid abbreviation casing
  `* @param string|array $data Data to send either as the POST body, or as parameters in the URL for a GET/HEAD`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21384**: abbreviations - Invalid abbreviation casing
  `* @throws \WpOrg\Requests\Exception       On a cURL error (`curlerror`)`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21403**: abbreviations - Invalid abbreviation casing
  `* Get the cURL handle for use in a multi-request`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21405**: abbreviations - Invalid abbreviation casing
  `* @param string $url URL to request`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21407**: abbreviations - Invalid abbreviation casing
  `* @param string|array $data Data to send either as the POST body, or as parameters in the URL for a GET/HEAD`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21409**: abbreviations - Invalid abbreviation casing
  `* @return resource|\CurlHandle Subrequest's cURL handle`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21420**: abbreviations - Invalid abbreviation casing
  `* @throws \WpOrg\Requests\Exception If the request resulted in a cURL error.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21428**: abbreviations - Invalid abbreviation casing
  `* @param resource|\CurlHandle $handle cURL handle`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21440**: abbreviations - Invalid abbreviation casing
  `* @param resource|\CurlHandle $handle cURL handle`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21488**: abbreviations - Invalid abbreviation casing
  `* @param string|Stringable $url URL to request`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21490**: abbreviations - Invalid abbreviation casing
  `* @param string|array $data Data to send either as the POST body, or as parameters in the URL for a GET/HEAD`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21737**: abbreviations - Invalid abbreviation casing
  `* SimplePie Website URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21739**: abbreviations - Invalid abbreviation casing
  `public const URL = 'http://simplepie.org';`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21743**: abbreviations - Invalid abbreviation casing
  `public const LINKBACK = '<a href="' . self::URL . '" title="' . self::NAME . ' ' . self::VERSION . '">' . self::NAME . '</a>';`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 21969**: abbreviations - Invalid abbreviation casing
  `* Wrong Media RSS Namespace #5. A possible typo of the RSS Advisory Board URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22001**: abbreviations - Invalid abbreviation casing
  `* cURL file source`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22041**: abbreviations - Invalid abbreviation casing
  `* @var string Feed URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22047**: abbreviations - Invalid abbreviation casing
  `* @var ?string Original feed URL, or new feed URL iff HTTP 301 Moved Permanently`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22072**: abbreviations - Invalid abbreviation casing
  `* of cURL, even if a new enough version is installed`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22078**: abbreviations - Invalid abbreviation casing
  `* @var bool Force the given data/URL to be treated as a feed no matter what`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22223**: abbreviations - Invalid abbreviation casing
  `* Previously, it was possible to pass in the feed URL along with cache`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22247**: abbreviations - Invalid abbreviation casing
  `* Force the given data/URL to be treated as a feed`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22253**: abbreviations - Invalid abbreviation casing
  `* @param bool $enable Force the given data/URL to be treated as a feed`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22260**: abbreviations - Invalid abbreviation casing
  `* Set the URL of the feed you want to parse`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22262**: abbreviations - Invalid abbreviation casing
  `* This allows you to enter the URL of the feed you want to parse, or the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22272**: abbreviations - Invalid abbreviation casing
  `* @param string|string[] $url This is the URL (or (deprecated) array of URLs) that you want to parse.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22295**: abbreviations - Invalid abbreviation casing
  `* to parse that data string instead of a remote feed. Any set feed URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22341**: abbreviations - Invalid abbreviation casing
  `* Force SimplePie to use fsockopen() instead of cURL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22400**: abbreviations - Invalid abbreviation casing
  `* Set the length of time (in seconds) that the autodiscovered feed URL will`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22403**: abbreviations - Invalid abbreviation casing
  `* @param int $seconds The autodiscovered feed URL cache duration.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22421**: abbreviations - Invalid abbreviation casing
  `* Return the filename (i.e. hash, without path and without extension) of the file to cache a given URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22423**: abbreviations - Invalid abbreviation casing
  `* @param string $url The URL of the feed to be cached.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22955**: abbreviations - Invalid abbreviation casing
  `* Get the URL for the feed`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22957**: abbreviations - Invalid abbreviation casing
  `* When the 'permanent' mode is enabled, returns the original feed URL,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22962**: abbreviations - Invalid abbreviation casing
  `* may or may not be different from the URL passed to {@see set_feed_url()},`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 22969**: abbreviations - Invalid abbreviation casing
  `* @param bool $permanent Permanent mode to return only the original URL or the first redirection`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 23003**: abbreviations - Invalid abbreviation casing
  `* @param string $namespace The URL of the XML namespace of the elements you're trying to access`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 23020**: abbreviations - Invalid abbreviation casing
  `* @param string $namespace The URL of the XML namespace of the elements you're trying to access`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 23037**: abbreviations - Invalid abbreviation casing
  `* @param string $namespace The URL of the XML namespace of the elements you're trying to access`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 23045**: abbreviations - Invalid abbreviation casing
  `* Get the base URL value from the feed`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 23049**: abbreviations - Invalid abbreviation casing
  `* or failing that, the URL of the feed itself.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 23067**: abbreviations - Invalid abbreviation casing
  `* @param string $base Base URL to resolve URLs against`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 23153**: abbreviations - Invalid abbreviation casing
  `* @return string|null Link URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 23167**: abbreviations - Invalid abbreviation casing
  `* @return string|null Link URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 23267**: abbreviations - Invalid abbreviation casing
  `* Get the feed logo's URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 23270**: abbreviations - Invalid abbreviation casing
  `* have a "feed logo" URL. This points directly to the image itself.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 23527**: abbreviations - Invalid abbreviation casing
  `* @param string $location URL location (scheme is used to determine handler)`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 23558**: abbreviations - Invalid abbreviation casing
  `* Parse a URL into an array`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 23601**: abbreviations - Invalid abbreviation casing
  `* @param string $name Unique ID for the cache`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 23658**: abbreviations - Invalid abbreviation casing
  `* @return array{string, array<string, Item>} First item is the serialized data for storage, second item is the unique ID for this item`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 23708**: abbreviations - Invalid abbreviation casing
  `* @param string $name Unique ID for the cache`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 23801**: abbreviations - Invalid abbreviation casing
  `* @param string $name Unique ID for the cache`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 23889**: abbreviations - Invalid abbreviation casing
  `* @param string $name Unique ID for the cache`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 23966**: abbreviations - Invalid abbreviation casing
  `* Cache ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 23975**: abbreviations - Invalid abbreviation casing
  `* @param string $name Unique ID for the cache`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 24068**: abbreviations - Invalid abbreviation casing
  `* @param string $name Unique ID for the cache`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 24432**: abbreviations - Invalid abbreviation casing
  `* Copyright URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 24463**: abbreviations - Invalid abbreviation casing
  `* Get the copyright URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 24465**: abbreviations - Invalid abbreviation casing
  `* @return string|null URL to copyright information`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 25021**: abbreviations - Invalid abbreviation casing
  `* Get the URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 25038**: abbreviations - Invalid abbreviation casing
  `* Get the player URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 25041**: abbreviations - Invalid abbreviation casing
  `* @return string|null Player URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 25100**: abbreviations - Invalid abbreviation casing
  `* @return string|null Thumbnail URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 25163**: abbreviations - Invalid abbreviation casing
  `*    Can be any relative or absolute URL. Defaults to blank.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 25177**: abbreviations - Invalid abbreviation casing
  `*    Can be any relative or absolute URL. Defaults to blank.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 25269**: abbreviations - Invalid abbreviation casing
  `* Return the string representation of the final requested URL after following all redirects.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 25392**: abbreviations - Invalid abbreviation casing
  `* Supports HTTP 1.0 via cURL or fsockopen, with spotty HTTP 1.1 support`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 25401**: abbreviations - Invalid abbreviation casing
  `* @var string The final URL after following all redirects`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 25437**: abbreviations - Invalid abbreviation casing
  `* @var string The permanent URL or the resource (first URL after the prefix of (only) permanent redirects)`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 26153**: abbreviations - Invalid abbreviation casing
  `* @param string $namespace The URL of the XML namespace of the elements you're trying to access`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 26161**: abbreviations - Invalid abbreviation casing
  `* Get the base URL value.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 26162**: abbreviations - Invalid abbreviation casing
  `* Uses `<xml:base>`, or item link, or enclosure link, or feed base URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 26177**: abbreviations - Invalid abbreviation casing
  `* @param string $base Base URL to resolve URLs against`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 26205**: abbreviations - Invalid abbreviation casing
  `* @param bool $hash Should we force using a hash instead of the supplied ID?`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 26418**: abbreviations - Invalid abbreviation casing
  `* @return string|null Permalink URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 26429**: abbreviations - Invalid abbreviation casing
  `* @return string|null Link URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 26961**: abbreviations - Invalid abbreviation casing
  `* Sanitize a URL by removing HTTP credentials.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 26962**: abbreviations - Invalid abbreviation casing
  `* @param string $url the URL to sanitize.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 26963**: abbreviations - Invalid abbreviation casing
  `* @return string the same URL without HTTP credentials.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 27031**: abbreviations - Invalid abbreviation casing
  `* Checks if the given IP is a valid IPv6 address`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 27040**: abbreviations - Invalid abbreviation casing
  `* Checks if the given IP is a valid IPv6 address`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 28710**: abbreviations - Invalid abbreviation casing
  `* Subfield ID 1`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 28719**: abbreviations - Invalid abbreviation casing
  `* Subfield ID 2`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 29230**: abbreviations - Invalid abbreviation casing
  `* General API for generating and formatting diffs - the differences between`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 29978**: abbreviations - Invalid abbreviation casing
  `*     @type array<string, mixed> $input_schema          Optional. JSON Schema definition for the ability's input.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 29979**: abbreviations - Invalid abbreviation casing
  `*     @type array<string, mixed> $output_schema         Optional. JSON Schema definition for the ability's output.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 29993**: abbreviations - Invalid abbreviation casing
  `*         @type bool                     $show_in_rest Optional. Whether to expose this ability in the REST API. Default false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 30433**: abbreviations - Invalid abbreviation casing
  `*     @type array<string, mixed> $input_schema          Optional. JSON Schema definition for the ability's input.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 30434**: abbreviations - Invalid abbreviation casing
  `*     @type array<string, mixed> $output_schema         Optional. JSON Schema definition for the ability's output.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 30448**: abbreviations - Invalid abbreviation casing
  `*         @type bool                     $show_in_rest Optional. Whether to expose this ability in the REST API. Default false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 30475**: abbreviations - Invalid abbreviation casing
  `*     @type array<string, mixed> $input_schema          Optional. JSON Schema definition for the ability's input. Required if ability accepts an input.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 30476**: abbreviations - Invalid abbreviation casing
  `*     @type array<string, mixed> $output_schema         Optional. JSON Schema definition for the ability's output.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 30490**: abbreviations - Invalid abbreviation casing
  `*         @type bool                     $show_in_rest Optional. Whether to expose this ability in the REST API. Default false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 30503**: abbreviations - Invalid abbreviation casing
  `*     @type array<string, mixed> $input_schema          Optional. JSON Schema definition for the ability's input.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 30504**: abbreviations - Invalid abbreviation casing
  `*     @type array<string, mixed> $output_schema         Optional. JSON Schema definition for the ability's output.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 30518**: abbreviations - Invalid abbreviation casing
  `*         @type bool                     $show_in_rest Whether to expose this ability in the REST API. Default false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 30766**: abbreviations - Invalid abbreviation casing
  `* AtomLib Atom Parser API`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 31203**: abbreviations - Invalid abbreviation casing
  `* @param int     $current_object_id Optional. ID of the current category. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 31345**: abbreviations - Invalid abbreviation casing
  `* @param int     $current_object_id Optional. ID of the current category. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 31520**: abbreviations - Invalid abbreviation casing
  `* @param int        $current_object_id Optional. ID of the current comment. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 31629**: abbreviations - Invalid abbreviation casing
  `public $db_fields = array('parent' => 'post_parent', 'id' => 'ID');`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 31647**: abbreviations - Invalid abbreviation casing
  `* @param int     $current_object_id Optional. ID of the current page. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 31693**: abbreviations - Invalid abbreviation casing
  `public $db_fields = array('parent' => 'post_parent', 'id' => 'ID');`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 31736**: abbreviations - Invalid abbreviation casing
  `* @param int     $current_object_id Optional. ID of the current page. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 31759**: abbreviations - Invalid abbreviation casing
  `* Core class used to implement the Toolbar API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 31814**: abbreviations - Invalid abbreviation casing
  `*     @type string $id     ID of the item.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 31816**: abbreviations - Invalid abbreviation casing
  `*     @type string $parent Optional. ID of the parent node.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 31889**: abbreviations - Invalid abbreviation casing
  `*     @type string $id     ID of the item.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 31890**: abbreviations - Invalid abbreviation casing
  `*     @type string $parent Optional. ID of the parent node. Default 'root'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 31908**: abbreviations - Invalid abbreviation casing
  `* @param string $id The ID of the item.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32044**: abbreviations - Invalid abbreviation casing
  `*     @type int|WP_Error    $id           The response ID, used as the response type `id` attribute. Also`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32045**: abbreviations - Invalid abbreviation casing
  `*                                         accepts a `WP_Error` object if the ID does not exist. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32046**: abbreviations - Invalid abbreviation casing
  `*     @type int|false       $old_id       The previous response ID. Used as the value for the response type`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32049**: abbreviations - Invalid abbreviation casing
  `*                                         -1 (top), HTML ID (after), or -HTML ID (before). Default 1 (bottom).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32051**: abbreviations - Invalid abbreviation casing
  `*                                         ID does not exist. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32131**: abbreviations - Invalid abbreviation casing
  `* @param int   $user_id  User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32179**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32190**: abbreviations - Invalid abbreviation casing
  `*         @type string|null $last_ip   The IP address the application password was last used by.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32211**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32222**: abbreviations - Invalid abbreviation casing
  `*     @type string|null $last_ip   The IP address the application password was last used by.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32242**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32255**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32266**: abbreviations - Invalid abbreviation casing
  `*     @type string|null $last_ip   The IP address the application password was last used by.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32287**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32299**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32311**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32322**: abbreviations - Invalid abbreviation casing
  `* @param int   $user_id   User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32333**: abbreviations - Invalid abbreviation casing
  `*         @type string|null $last_ip   The IP address the application password was last used by.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 32336**: abbreviations - Invalid abbreviation casing
  `* @return int|bool User meta ID if the key didn't exist (ie. this is the first time that an application password`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 33395**: abbreviations - Invalid abbreviation casing
  `* without parsing the entire block tree and JSON attributes into memory.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 33413**: abbreviations - Invalid abbreviation casing
  `* >         all the parsed JSON attributes, nested blocks, and HTML, consider`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 33441**: abbreviations - Invalid abbreviation casing
  `* optionally, block attributes encoded as a JSON object payload.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 33482**: abbreviations - Invalid abbreviation casing
  `*    JSON parser class.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 33483**: abbreviations - Invalid abbreviation casing
  `*  - {@see self::allocate_and_return_parsed_attributes()} extracts the JSON attributes`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 33643**: abbreviations - Invalid abbreviation casing
  `*    to reading and modifying a block’s JSON attributes. It should be fast and memory efficient.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 33765**: abbreviations - Invalid abbreviation casing
  `* Delimiters may include invalid JSON. This parser does not attempt to parse the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 33766**: abbreviations - Invalid abbreviation casing
  `* JSON attributes until requested; when invalid, the attributes will be null. This`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 33969**: abbreviations - Invalid abbreviation casing
  `* Indicates if the last attempt to parse a block’s JSON attributes failed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 33975**: abbreviations - Invalid abbreviation casing
  `* @return int JSON_ERROR_ code from last attempt to parse block JSON attributes.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 34247**: abbreviations - Invalid abbreviation casing
  `* for efficiently interacting with the JSON attributes.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 34267**: abbreviations - Invalid abbreviation casing
  `* Attempts to parse and return the entire JSON attributes from the delimiter,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 34268**: abbreviations - Invalid abbreviation casing
  `* allocating memory and processing the JSON span in the process.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 34271**: abbreviations - Invalid abbreviation casing
  `* even if there is a span of JSON content; this JSON is a parsing error.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 34277**: abbreviations - Invalid abbreviation casing
  `* Since the JSON span inside the comment delimiter may not be valid JSON,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 34281**: abbreviations - Invalid abbreviation casing
  `* If the delimiter contains no JSON span, it will also return `null`,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 34311**: abbreviations - Invalid abbreviation casing
  `* @return array|null Parsed JSON attributes, if present and valid, otherwise `null`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 34614**: abbreviations - Invalid abbreviation casing
  `* ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 34658**: abbreviations - Invalid abbreviation casing
  `* Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 34958**: abbreviations - Invalid abbreviation casing
  `* Block API version.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 35189**: abbreviations - Invalid abbreviation casing
  `*     @type string        $api_version              Block API version.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 35620**: abbreviations - Invalid abbreviation casing
  `* SQL for database query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 35641**: abbreviations - Invalid abbreviation casing
  `* SQL query clauses.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 35648**: abbreviations - Invalid abbreviation casing
  `* SQL WHERE clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 35729**: abbreviations - Invalid abbreviation casing
  `*     @type string          $author_url                Comment author URL. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 35793**: abbreviations - Invalid abbreviation casing
  `*     @type int             $parent                    Parent ID of comment to retrieve children of.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 35803**: abbreviations - Invalid abbreviation casing
  `*     @type int             $post_id                   Limit results to those affiliated with a given post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 35809**: abbreviations - Invalid abbreviation casing
  `*     @type int             $post_author               Post author ID to limit results by. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 35817**: abbreviations - Invalid abbreviation casing
  `*     @type int             $post_parent               Post parent ID to retrieve affiliated comments for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 35832**: abbreviations - Invalid abbreviation casing
  `*     @type int             $user_id                   Include comments for a specific user ID. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 35971**: abbreviations - Invalid abbreviation casing
  `* @param string|array $query Array or URL query string of parameters.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 36017**: abbreviations - Invalid abbreviation casing
  `* Used internally to generate an SQL string for searching across multiple columns.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 36025**: abbreviations - Invalid abbreviation casing
  `* @return string Search SQL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 36064**: abbreviations - Invalid abbreviation casing
  `* Comment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 36073**: abbreviations - Invalid abbreviation casing
  `* ID of the post the comment is associated with.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 36096**: abbreviations - Invalid abbreviation casing
  `* Comment author URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 36103**: abbreviations - Invalid abbreviation casing
  `* Comment author IP address (IPv4 format).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 36162**: abbreviations - Invalid abbreviation casing
  `* Parent comment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 36171**: abbreviations - Invalid abbreviation casing
  `* Comment author ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 36186**: abbreviations - Invalid abbreviation casing
  `* @param int $id Comment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 36272**: abbreviations - Invalid abbreviation casing
  `* Gets a child comment by ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 36276**: abbreviations - Invalid abbreviation casing
  `* @param int $child_id ID of the child.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 36354**: abbreviations - Invalid abbreviation casing
  `* Control ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 36457**: abbreviations - Invalid abbreviation casing
  `*               the control is active (such as it relates to the URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 36466**: abbreviations - Invalid abbreviation casing
  `* If `$args['settings']` is not defined, use the `$id` as the setting ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 36471**: abbreviations - Invalid abbreviation casing
  `* @param string               $id      Control ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 36478**: abbreviations - Invalid abbreviation casing
  `*     @type string               $id              Control ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 36571**: abbreviations - Invalid abbreviation casing
  `* Refreshes the parameters passed to the JavaScript via JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 36579**: abbreviations - Invalid abbreviation casing
  `* Gets the data to export to the client via JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37001**: abbreviations - Invalid abbreviation casing
  `* Finds the changeset post ID for a given changeset UUID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37006**: abbreviations - Invalid abbreviation casing
  `* @return int|null Returns post ID on success and null on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37012**: abbreviations - Invalid abbreviation casing
  `* Gets the changeset post ID for the loaded changeset.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37016**: abbreviations - Invalid abbreviation casing
  `* @return int|null Post ID on success or null if there is no post yet saved.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37060**: abbreviations - Invalid abbreviation casing
  `*    `$_POST['customized']` JSON data.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37117**: abbreviations - Invalid abbreviation casing
  `* @param string $setting_id ID for the WP_Customize_Setting instance.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37144**: abbreviations - Invalid abbreviation casing
  `* Adds customize state query params to a given URL if preview is allowed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37151**: abbreviations - Invalid abbreviation casing
  `* @param string $url URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37152**: abbreviations - Invalid abbreviation casing
  `* @return string URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37197**: abbreviations - Invalid abbreviation casing
  `* work as expected since the parent frame is not being sent the URL to navigate to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37362**: abbreviations - Invalid abbreviation casing
  `*     @type int    $user_id         ID for user who is saving the changeset. Optional, defaults to the current user ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37382**: abbreviations - Invalid abbreviation casing
  `* Preserves the initial JSON post_content passed to save into the post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37385**: abbreviations - Invalid abbreviation casing
  `* from corrupting JSON data.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37388**: abbreviations - Invalid abbreviation casing
  `* run on the setting values being serialized as JSON into the post content`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37459**: abbreviations - Invalid abbreviation casing
  `* @param int      $user_id The user ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37460**: abbreviations - Invalid abbreviation casing
  `* @param array    $args    Adds the context to the cap. Typically the object ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37471**: abbreviations - Invalid abbreviation casing
  `* @param int  $changeset_post_id Changeset post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37482**: abbreviations - Invalid abbreviation casing
  `* @param int $changeset_post_id Changeset post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37502**: abbreviations - Invalid abbreviation casing
  `* Checks locked changeset with heartbeat API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37556**: abbreviations - Invalid abbreviation casing
  `* @param int $changeset_post_id ID for customize_changeset post. Defaults to the changeset for the current manager instance.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37589**: abbreviations - Invalid abbreviation casing
  `* @param WP_Customize_Setting|string $id   Customize Setting object, or ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37631**: abbreviations - Invalid abbreviation casing
  `* @param string $id Customize Setting ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37644**: abbreviations - Invalid abbreviation casing
  `* @param string $id Customize Setting ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37657**: abbreviations - Invalid abbreviation casing
  `* @param WP_Customize_Panel|string $id   Customize Panel object, or ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37680**: abbreviations - Invalid abbreviation casing
  `* @param string $id Panel ID to get.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37693**: abbreviations - Invalid abbreviation casing
  `* @param string $id Panel ID to remove.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37728**: abbreviations - Invalid abbreviation casing
  `* @param WP_Customize_Section|string $id   Customize Section object, or ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37753**: abbreviations - Invalid abbreviation casing
  `* @param string $id Section ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37766**: abbreviations - Invalid abbreviation casing
  `* @param string $id Section ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37801**: abbreviations - Invalid abbreviation casing
  `* @param WP_Customize_Control|string $id   Customize Control object, or ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37833**: abbreviations - Invalid abbreviation casing
  `* @param string $id ID of the control.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37846**: abbreviations - Invalid abbreviation casing
  `* @param string $id ID of the control.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37913**: abbreviations - Invalid abbreviation casing
  `* Sets the initial URL to be previewed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37915**: abbreviations - Invalid abbreviation casing
  `* URL is validated.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37919**: abbreviations - Invalid abbreviation casing
  `* @param string $preview_url URL to be previewed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37925**: abbreviations - Invalid abbreviation casing
  `* Gets the initial URL to be previewed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37929**: abbreviations - Invalid abbreviation casing
  `* @return string URL being previewed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37972**: abbreviations - Invalid abbreviation casing
  `* Sets URL to link the user to when closing the Customizer.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37974**: abbreviations - Invalid abbreviation casing
  `* URL is validated.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37978**: abbreviations - Invalid abbreviation casing
  `* @param string $return_url URL for return link.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37984**: abbreviations - Invalid abbreviation casing
  `* Gets URL to link the user to when closing the Customizer.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 37990**: abbreviations - Invalid abbreviation casing
  `* @return string URL for link to close Customizer.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38001**: abbreviations - Invalid abbreviation casing
  `*     Mapping of 'panel', 'section', 'control' to the ID which should be autofocused.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38003**: abbreviations - Invalid abbreviation casing
  `*     @type string $control ID for control to be autofocused.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38004**: abbreviations - Invalid abbreviation casing
  `*     @type string $section ID for section to be autofocused.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38005**: abbreviations - Invalid abbreviation casing
  `*     @type string $panel   ID for panel to be autofocused.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38022**: abbreviations - Invalid abbreviation casing
  `*     Mapping of 'panel', 'section', 'control' to the ID which should be autofocused.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38024**: abbreviations - Invalid abbreviation casing
  `*     @type string $control ID for control to be autofocused.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38025**: abbreviations - Invalid abbreviation casing
  `*     @type string $section ID for section to be autofocused.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38026**: abbreviations - Invalid abbreviation casing
  `*     @type string $panel   ID for panel to be autofocused.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38160**: abbreviations - Invalid abbreviation casing
  `* Ensures that the provided URL is supported.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38176**: abbreviations - Invalid abbreviation casing
  `* @param string $value URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38177**: abbreviations - Invalid abbreviation casing
  `* @return string Sanitized URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38187**: abbreviations - Invalid abbreviation casing
  `* the render_callback directly since it expects a blog ID as the first`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38303**: abbreviations - Invalid abbreviation casing
  `* @param string      $setting_id   ID for dynamic setting, usually coming from `$_POST['customized']`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38315**: abbreviations - Invalid abbreviation casing
  `* @param string $setting_id    ID for dynamic setting, usually coming from `$_POST['customized']`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38418**: abbreviations - Invalid abbreviation casing
  `* @param string      $partial_id   Partial ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38661**: abbreviations - Invalid abbreviation casing
  `*               the section is active (such as it relates to the URL currently`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38673**: abbreviations - Invalid abbreviation casing
  `* @param string               $id      A specific ID for the panel.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38724**: abbreviations - Invalid abbreviation casing
  `* Gather the parameters passed to client JavaScript via JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38728**: abbreviations - Invalid abbreviation casing
  `* @return array The array to be exported to the client as JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38931**: abbreviations - Invalid abbreviation casing
  `*               the section is active (such as it relates to the URL currently`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 38953**: abbreviations - Invalid abbreviation casing
  `* @param string               $id      A specific ID of the section.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39011**: abbreviations - Invalid abbreviation casing
  `* Gather the parameters passed to client JavaScript via JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39015**: abbreviations - Invalid abbreviation casing
  `* @return array The array to be exported to the client as JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39167**: abbreviations - Invalid abbreviation casing
  `* Callback to convert a Customize PHP setting value to a value that is JSON serializable.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39186**: abbreviations - Invalid abbreviation casing
  `* ID Data.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39221**: abbreviations - Invalid abbreviation casing
  `* @param string               $id      A specific ID of the setting.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39237**: abbreviations - Invalid abbreviation casing
  `*                                                 JSON serializable.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39256**: abbreviations - Invalid abbreviation casing
  `* Get parsed ID data for multidimensional setting.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39261**: abbreviations - Invalid abbreviation casing
  `*     ID data for multidimensional setting.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39263**: abbreviations - Invalid abbreviation casing
  `*     @type string $base ID base`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39297**: abbreviations - Invalid abbreviation casing
  `* The ID for the current site when the preview() method was called.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39453**: abbreviations - Invalid abbreviation casing
  `* Save the value of the setting, using the related API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39502**: abbreviations - Invalid abbreviation casing
  `* Retrieves the data to export to the client via JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39624**: abbreviations - Invalid abbreviation casing
  `* @param string $id_base Widget ID Base.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39645**: abbreviations - Invalid abbreviation casing
  `* @param string      $setting_id ID for dynamic setting, usually coming from `$_POST['customized']`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39753**: abbreviations - Invalid abbreviation casing
  `* Converts a widget_id into its corresponding Customizer setting ID (option name).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39757**: abbreviations - Invalid abbreviation casing
  `* @param string $widget_id Widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39758**: abbreviations - Invalid abbreviation casing
  `* @return string Maybe-parsed widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39777**: abbreviations - Invalid abbreviation casing
  `* @param string $widget_id Widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39784**: abbreviations - Invalid abbreviation casing
  `* Converts a widget ID into its id_base and number components.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39788**: abbreviations - Invalid abbreviation casing
  `* @param string $widget_id Widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39795**: abbreviations - Invalid abbreviation casing
  `* Converts a widget setting ID (option path) to its id_base and number components.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39799**: abbreviations - Invalid abbreviation casing
  `* @param string $setting_id Widget setting ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 39858**: abbreviations - Invalid abbreviation casing
  `* @param string $id        Widget setting ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40013**: abbreviations - Invalid abbreviation casing
  `* @param string $widget_id Widget ID to check.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40024**: abbreviations - Invalid abbreviation casing
  `* @param string $sidebar_id Sidebar ID to check.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40040**: abbreviations - Invalid abbreviation casing
  `* @param string $sidebar_id Sidebar ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40056**: abbreviations - Invalid abbreviation casing
  `* @param string $sidebar_id  Sidebar ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40074**: abbreviations - Invalid abbreviation casing
  `* @param string $id_base Optional. Base of the ID of the widget being sanitized. Default null.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40081**: abbreviations - Invalid abbreviation casing
  `* Converts a widget instance into JSON-representable format.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40088**: abbreviations - Invalid abbreviation casing
  `* @param array  $value   Widget instance to convert to JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40089**: abbreviations - Invalid abbreviation casing
  `* @param string $id_base Optional. Base of the ID of the widget being sanitized. Default null.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40090**: abbreviations - Invalid abbreviation casing
  `* @return array JSON-converted widget instance.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40121**: abbreviations - Invalid abbreviation casing
  `* @param string $widget_id Widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40150**: abbreviations - Invalid abbreviation casing
  `* @param string      $partial_id   Partial ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40207**: abbreviations - Invalid abbreviation casing
  `* @param int|string $index Index, name, or ID of the dynamic sidebar.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40219**: abbreviations - Invalid abbreviation casing
  `* @param int|string $index Index, name, or ID of the dynamic sidebar.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40246**: abbreviations - Invalid abbreviation casing
  `*     @type string $sidebar_id              ID for sidebar for widget to render into.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40328**: abbreviations - Invalid abbreviation casing
  `* Class for generating SQL clauses that filter a primary query according to date.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40332**: abbreviations - Invalid abbreviation casing
  `* primary SQL query string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40334**: abbreviations - Invalid abbreviation casing
  `* Attempting to filter by an invalid date value (eg month=13) will generate SQL that will`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40557**: abbreviations - Invalid abbreviation casing
  `* Generates SQL clauses to be appended to a main query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40565**: abbreviations - Invalid abbreviation casing
  `*     Array containing JOIN and WHERE SQL clauses to append to the main query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40567**: abbreviations - Invalid abbreviation casing
  `*     @type string $join  SQL fragment to append to the main JOIN clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40568**: abbreviations - Invalid abbreviation casing
  `*     @type string $where SQL fragment to append to the main WHERE clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40579**: abbreviations - Invalid abbreviation casing
  `* Generates SQL clauses for a single query array.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40582**: abbreviations - Invalid abbreviation casing
  `* produce the properly nested SQL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40590**: abbreviations - Invalid abbreviation casing
  `*     Array containing JOIN and WHERE SQL clauses to append to a single query array.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40592**: abbreviations - Invalid abbreviation casing
  `*     @type string $join  SQL fragment to append to the main JOIN clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40593**: abbreviations - Invalid abbreviation casing
  `*     @type string $where SQL fragment to append to the main WHERE clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40613**: abbreviations - Invalid abbreviation casing
  `*     Array containing JOIN and WHERE SQL clauses to append to the main query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40615**: abbreviations - Invalid abbreviation casing
  `*     @type string[] $join  Array of SQL fragments to append to the main JOIN clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40616**: abbreviations - Invalid abbreviation casing
  `*     @type string[] $where Array of SQL fragments to append to the main WHERE clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40627**: abbreviations - Invalid abbreviation casing
  `* Turns a first-order date query into SQL for a WHERE clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40636**: abbreviations - Invalid abbreviation casing
  `*     Array containing JOIN and WHERE SQL clauses to append to the main query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40638**: abbreviations - Invalid abbreviation casing
  `*     @type string[] $join  Array of SQL fragments to append to the main JOIN clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40639**: abbreviations - Invalid abbreviation casing
  `*     @type string[] $where Array of SQL fragments to append to the main WHERE clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40656**: abbreviations - Invalid abbreviation casing
  `* @return string|false|int The value to be used in SQL or false on error.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40847**: abbreviations - Invalid abbreviation casing
  `* @param string|false     $src    Full URL of the item, or path of the item relative`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 40853**: abbreviations - Invalid abbreviation casing
  `*                                 which is added to the URL as a query string for cache busting purposes.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41282**: abbreviations - Invalid abbreviation casing
  `* @param string $editor_id HTML ID for the textarea and TinyMCE and Quicktags instances.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41297**: abbreviations - Invalid abbreviation casing
  `*     @type string     $tabfocus_elements The previous and next element ID to move the focus to`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41339**: abbreviations - Invalid abbreviation casing
  `* @param string $editor_id HTML ID for the textarea and TinyMCE and Quicktags instances.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41409**: abbreviations - Invalid abbreviation casing
  `* Returns the TinyMCE base URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41419**: abbreviations - Invalid abbreviation casing
  `* Translates the default TinyMCE strings and returns them as JSON encoded object ready to be loaded with tinymce.addI18n(),`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41427**: abbreviations - Invalid abbreviation casing
  `* @return string Translation object, JSON encoded.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41490**: abbreviations - Invalid abbreviation casing
  `*         @type int    $ID        Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41502**: abbreviations - Invalid abbreviation casing
  `*   ID: int,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41522**: abbreviations - Invalid abbreviation casing
  `* API for easily embedding rich media such as videos and images into content.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41538**: abbreviations - Invalid abbreviation casing
  `* When a URL cannot be embedded, return false instead of returning a link`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41539**: abbreviations - Invalid abbreviation casing
  `* or the URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41582**: abbreviations - Invalid abbreviation casing
  `* @param string   $id       An internal ID/name for the handler. Needs to be unique.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41583**: abbreviations - Invalid abbreviation casing
  `* @param string   $regex    The regex that will be used to see if this handler should be used for a URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41597**: abbreviations - Invalid abbreviation casing
  `* @param string $id       The handler ID that should be removed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41604**: abbreviations - Invalid abbreviation casing
  `* Returns embed HTML for a given URL from embed handlers.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41606**: abbreviations - Invalid abbreviation casing
  `* Attempts to convert a URL into embed HTML by checking the URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41617**: abbreviations - Invalid abbreviation casing
  `* @param string $url The URL attempting to be embedded.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41630**: abbreviations - Invalid abbreviation casing
  `* Attempts to convert a URL into embed HTML. Starts by checking the URL against the regex of`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41631**: abbreviations - Invalid abbreviation casing
  `* the registered embed handlers. If none of the regex matches and it's enabled, then the URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41640**: abbreviations - Invalid abbreviation casing
  `* @param string $url The URL attempting to be embedded.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41641**: abbreviations - Invalid abbreviation casing
  `* @return string|false The embed HTML on success, otherwise the original URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41654**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Post ID to delete the caches for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41663**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Post ID to do the caching for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41684**: abbreviations - Invalid abbreviation casing
  `* @return string The embed HTML on success, otherwise the original URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41692**: abbreviations - Invalid abbreviation casing
  `* @param string $url URL to potentially be linked.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41693**: abbreviations - Invalid abbreviation casing
  `* @return string|false Linked URL or the original URL. False if 'return_false_on_fail' is true.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41699**: abbreviations - Invalid abbreviation casing
  `* Finds the oEmbed cache post ID for a given cache key.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 41704**: abbreviations - Invalid abbreviation casing
  `* @return int|null Post ID on success, null on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 42051**: abbreviations - Invalid abbreviation casing
  `* @param string                           $location URL location (scheme is used to determine handler).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 42125**: abbreviations - Invalid abbreviation casing
  `* @param string $location  URL location (scheme is used to determine handler).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 42462**: abbreviations - Invalid abbreviation casing
  `* Cookie URL path.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 42513**: abbreviations - Invalid abbreviation casing
  `* @param string       $requested_url The URL which the cookie was set on, used for default $domain`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 42530**: abbreviations - Invalid abbreviation casing
  `* Confirms that it's OK to send this cookie to the URL checked against.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 42536**: abbreviations - Invalid abbreviation casing
  `* @param string $url URL you intend to send this cookie to`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 42553**: abbreviations - Invalid abbreviation casing
  `* Retrieve cookie header for usage in the rest of the WordPress HTTP API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 42571**: abbreviations - Invalid abbreviation casing
  `*     @type string          $path    Cookie URL path.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 42599**: abbreviations - Invalid abbreviation casing
  `* Send a HTTP request to a URI using cURL extension.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 42603**: abbreviations - Invalid abbreviation casing
  `* @param string       $url  The request URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 42611**: abbreviations - Invalid abbreviation casing
  `* Determines whether this class can be used for retrieving a URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 42772**: abbreviations - Invalid abbreviation casing
  `* Core class used to implement HTTP API proxy support.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 42779**: abbreviations - Invalid abbreviation casing
  `* cURL MAY support more methods (such as NTLM authentication) depending on your environment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 42899**: abbreviations - Invalid abbreviation casing
  `* We want to keep localhost and the site URL from being sent through the proxy, because`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 42905**: abbreviations - Invalid abbreviation casing
  `* @param string $uri URL of the request.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 42923**: abbreviations - Invalid abbreviation casing
  `* Requested URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 42925**: abbreviations - Invalid abbreviation casing
  `* @var string Requested URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 42937**: abbreviations - Invalid abbreviation casing
  `* @param string $url     URL to request.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43069**: abbreviations - Invalid abbreviation casing
  `* Retrieves the response data for JSON serialization.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43072**: abbreviations - Invalid abbreviation casing
  `* however this may be different if you want to do custom JSON data handling.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43076**: abbreviations - Invalid abbreviation casing
  `* @return mixed Any JSON-serializable value.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43239**: abbreviations - Invalid abbreviation casing
  `* @param string       $url  The request URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43254**: abbreviations - Invalid abbreviation casing
  `* IP Address support is included if the request is being made to an IP address.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43266**: abbreviations - Invalid abbreviation casing
  `* Determines whether this class can be used for retrieving a URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43282**: abbreviations - Invalid abbreviation casing
  `* All code should make use of WP_Http directly through its API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43300**: abbreviations - Invalid abbreviation casing
  `* Debugging includes several actions, which pass different variables for debugging the HTTP API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43377**: abbreviations - Invalid abbreviation casing
  `* @param string       $url  The request URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43413**: abbreviations - Invalid abbreviation casing
  `*                                             be set using the basename of the URL. Default false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43489**: abbreviations - Invalid abbreviation casing
  `* @param string                  $location URL to redirect to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43503**: abbreviations - Invalid abbreviation casing
  `* @throws WpOrg\Requests\Exception On unsuccessful URL validation.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43504**: abbreviations - Invalid abbreviation casing
  `* @param string $location URL to redirect to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43517**: abbreviations - Invalid abbreviation casing
  `* @param string $url  URL to request.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43531**: abbreviations - Invalid abbreviation casing
  `* @param string       $url  The request URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43547**: abbreviations - Invalid abbreviation casing
  `* @param string       $url  The request URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43563**: abbreviations - Invalid abbreviation casing
  `* @param string       $url  The request URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43601**: abbreviations - Invalid abbreviation casing
  `* @param string       $url     Optional. The URL that was requested. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43656**: abbreviations - Invalid abbreviation casing
  `* Determines whether an HTTP API request to the given URL should be blocked.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43661**: abbreviations - Invalid abbreviation casing
  `* You block external URL requests by defining `WP_HTTP_BLOCK_EXTERNAL` as true in your `wp-config.php``
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43684**: abbreviations - Invalid abbreviation casing
  `* @param string $url The URL to parse.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43685**: abbreviations - Invalid abbreviation casing
  `* @return bool|array False on failure; Array of URL components on success;`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43692**: abbreviations - Invalid abbreviation casing
  `* Converts a relative URL to an absolute URL relative to a given URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43694**: abbreviations - Invalid abbreviation casing
  `* If an Absolute URL is provided, no processing of that URL is done.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43698**: abbreviations - Invalid abbreviation casing
  `* @param string $maybe_relative_path The URL which might be relative.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43699**: abbreviations - Invalid abbreviation casing
  `* @param string $url                 The URL which $maybe_relative_path is relative to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43700**: abbreviations - Invalid abbreviation casing
  `* @return string An Absolute URL, in a failure condition where the URL cannot be parsed, the relative URL will be returned.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43710**: abbreviations - Invalid abbreviation casing
  `* @param string $url      The URL which was requested.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43713**: abbreviations - Invalid abbreviation casing
  `* @return array|false|WP_Error An HTTP API response array if the redirect is successfully followed,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43720**: abbreviations - Invalid abbreviation casing
  `* Determines if a specified string represents an IP address or not.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43722**: abbreviations - Invalid abbreviation casing
  `* This function also detects the type of the IP address, returning either`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43724**: abbreviations - Invalid abbreviation casing
  `* This does not verify if the IP is a valid IP, only that it appears to be`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43725**: abbreviations - Invalid abbreviation casing
  `* an IP address.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 43731**: abbreviations - Invalid abbreviation casing
  `* @param string $maybe_ip A suspected IP address.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 44900**: abbreviations - Invalid abbreviation casing
  `* @param int|false $user_id Optional. User ID as context. Default false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 44911**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 44958**: abbreviations - Invalid abbreviation casing
  `* Returns the user ID related to the currently switched locale.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 44962**: abbreviations - Invalid abbreviation casing
  `* @return int|false User ID if set and if the locale has been switched, false otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45331**: abbreviations - Invalid abbreviation casing
  `* Core class used to implement meta queries for the Meta API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45333**: abbreviations - Invalid abbreviation casing
  `* Used for generating SQL clauses that filter a primary query according to metadata keys and values.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45338**: abbreviations - Invalid abbreviation casing
  `* to the primary SQL query string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45369**: abbreviations - Invalid abbreviation casing
  `* Column in meta_table that represents the ID of the object the metadata belongs to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45383**: abbreviations - Invalid abbreviation casing
  `* Column in primary_table that represents the ID of the object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45533**: abbreviations - Invalid abbreviation casing
  `* Generates SQL clauses to be appended to a main query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45540**: abbreviations - Invalid abbreviation casing
  `* @param string $primary_id_column ID column for the filtered object in $primary_table.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45545**: abbreviations - Invalid abbreviation casing
  `*     Array containing JOIN and WHERE SQL clauses to append to the main query,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45548**: abbreviations - Invalid abbreviation casing
  `*     @type string $join  SQL fragment to append to the main JOIN clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45549**: abbreviations - Invalid abbreviation casing
  `*     @type string $where SQL fragment to append to the main WHERE clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45560**: abbreviations - Invalid abbreviation casing
  `* Generates SQL clauses to be appended to a main query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45568**: abbreviations - Invalid abbreviation casing
  `*     Array containing JOIN and WHERE SQL clauses to append to the main query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45570**: abbreviations - Invalid abbreviation casing
  `*     @type string $join  SQL fragment to append to the main JOIN clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45571**: abbreviations - Invalid abbreviation casing
  `*     @type string $where SQL fragment to append to the main WHERE clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45582**: abbreviations - Invalid abbreviation casing
  `* Generates SQL clauses for a single query array.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45585**: abbreviations - Invalid abbreviation casing
  `* produce the properly nested SQL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45593**: abbreviations - Invalid abbreviation casing
  `*     Array containing JOIN and WHERE SQL clauses to append to a single query array.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45595**: abbreviations - Invalid abbreviation casing
  `*     @type string $join  SQL fragment to append to the main JOIN clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45596**: abbreviations - Invalid abbreviation casing
  `*     @type string $where SQL fragment to append to the main WHERE clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45607**: abbreviations - Invalid abbreviation casing
  `* Generates SQL JOIN and WHERE clauses for a first-order query clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45621**: abbreviations - Invalid abbreviation casing
  `*     Array containing JOIN and WHERE SQL clauses to append to a first-order query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45623**: abbreviations - Invalid abbreviation casing
  `*     @type string[] $join  Array of SQL fragments to append to the main JOIN clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45624**: abbreviations - Invalid abbreviation casing
  `*     @type string[] $where Array of SQL fragments to append to the main WHERE clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45796**: abbreviations - Invalid abbreviation casing
  `* @param int    $object_id ID of the object metadata is for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45856**: abbreviations - Invalid abbreviation casing
  `* SQL for database query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45863**: abbreviations - Invalid abbreviation casing
  `* SQL query clauses.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 45994**: abbreviations - Invalid abbreviation casing
  `* @param string|array $query Array or URL query string of parameters.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46025**: abbreviations - Invalid abbreviation casing
  `* Used internally to generate an SQL string for searching across multiple columns.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46033**: abbreviations - Invalid abbreviation casing
  `* @return string Search SQL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46111**: abbreviations - Invalid abbreviation casing
  `* Retrieves a network from the database by its ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46117**: abbreviations - Invalid abbreviation casing
  `* @param int $network_id The ID of the network to retrieve.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46522**: abbreviations - Invalid abbreviation casing
  `* Switches the internal blog ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46524**: abbreviations - Invalid abbreviation casing
  `* This changes the blog ID used to create keys in blog specific groups.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46528**: abbreviations - Invalid abbreviation casing
  `* @param int $blog_id Blog ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46557**: abbreviations - Invalid abbreviation casing
  `* oEmbed API endpoint controller.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46559**: abbreviations - Invalid abbreviation casing
  `* Registers the REST API route and delivers the response data.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46560**: abbreviations - Invalid abbreviation casing
  `* The output format (XML or JSON) is handled by the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46568**: abbreviations - Invalid abbreviation casing
  `* Register the oEmbed REST API route.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46576**: abbreviations - Invalid abbreviation casing
  `* Callback for the embed API endpoint.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46578**: abbreviations - Invalid abbreviation casing
  `* Returns the JSON object for the post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46599**: abbreviations - Invalid abbreviation casing
  `* Callback for the proxy API endpoint.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46601**: abbreviations - Invalid abbreviation casing
  `* Returns the JSON object for the proxied item.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46659**: abbreviations - Invalid abbreviation casing
  `* Takes a URL and returns the corresponding oEmbed provider's URL, if there is one.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46665**: abbreviations - Invalid abbreviation casing
  `* @param string       $url  The URL to the content.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46670**: abbreviations - Invalid abbreviation casing
  `*                          at the given URL for an oEmbed provider when the provider URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46673**: abbreviations - Invalid abbreviation casing
  `* @return string|false The oEmbed provider URL on success, false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46693**: abbreviations - Invalid abbreviation casing
  `* @param string $format   Format of URL that this provider can handle. You can use`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46695**: abbreviations - Invalid abbreviation casing
  `* @param string $provider The URL to the oEmbed provider..`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46714**: abbreviations - Invalid abbreviation casing
  `* @param string $format The format of URL that this provider can handle. You can use`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46721**: abbreviations - Invalid abbreviation casing
  `* Takes a URL and attempts to return the oEmbed data.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46727**: abbreviations - Invalid abbreviation casing
  `* @param string       $url  The URL to the content that should be attempted to be embedded.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46741**: abbreviations - Invalid abbreviation casing
  `* The do-it-all function that takes a URL and attempts to return the HTML.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46748**: abbreviations - Invalid abbreviation casing
  `* @param string       $url  The URL to the content that should be attempted to be embedded.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46763**: abbreviations - Invalid abbreviation casing
  `* Attempts to discover link tags at the given URL for an oEmbed provider.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46767**: abbreviations - Invalid abbreviation casing
  `* @param string $url The URL that should be inspected for discovery `<link>` tags.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46768**: abbreviations - Invalid abbreviation casing
  `* @return string|false The oEmbed provider URL on success, false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46778**: abbreviations - Invalid abbreviation casing
  `* @param string       $provider The URL to the oEmbed provider.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46779**: abbreviations - Invalid abbreviation casing
  `* @param string       $url      The URL to the content that is desired to be embedded.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46798**: abbreviations - Invalid abbreviation casing
  `* @param string $url  The URL to the content that is desired to be embedded.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46812**: abbreviations - Invalid abbreviation casing
  `* @param string       $url The original URL passed to oEmbed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46921**: abbreviations - Invalid abbreviation casing
  `* Checks whether the underlying API to store paused extensions is loaded.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 46925**: abbreviations - Invalid abbreviation casing
  `* @return bool True if the API is loaded, false otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47070**: abbreviations - Invalid abbreviation casing
  `* Initializes by fetching plugin header and plugin API data.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47200**: abbreviations - Invalid abbreviation casing
  `* Returns API data for the dependency.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47205**: abbreviations - Invalid abbreviation casing
  `* @return array|false The dependency's API data on success, otherwise false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47275**: abbreviations - Invalid abbreviation casing
  `* Retrieves and stores dependency plugin data from the WordPress.org Plugin API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47281**: abbreviations - Invalid abbreviation casing
  `* @return array|void An array of dependency API data, or void on early exit.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47480**: abbreviations - Invalid abbreviation casing
  `* The URL or reference to the icon to be used for this menu.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47614**: abbreviations - Invalid abbreviation casing
  `* URL segment to use for edit link of this post type.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47646**: abbreviations - Invalid abbreviation casing
  `* Whether this post type should appear in the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47656**: abbreviations - Invalid abbreviation casing
  `* The base path for this post type's REST API endpoints.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47663**: abbreviations - Invalid abbreviation casing
  `* The namespace for this post type's REST API endpoints.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47670**: abbreviations - Invalid abbreviation casing
  `* The controller for this post type's REST API endpoints.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47679**: abbreviations - Invalid abbreviation casing
  `* The controller instance for this post type's REST API endpoints.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47688**: abbreviations - Invalid abbreviation casing
  `* The controller for this post type's revisions REST API endpoints.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47697**: abbreviations - Invalid abbreviation casing
  `* The controller instance for this post type's revisions REST API endpoints.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47706**: abbreviations - Invalid abbreviation casing
  `* The controller for this post type's autosave REST API endpoints.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47715**: abbreviations - Invalid abbreviation casing
  `* The controller instance for this post type's autosave REST API endpoints.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47724**: abbreviations - Invalid abbreviation casing
  `* A flag to register the post type REST API controller after its associated autosave / revisions controllers, instead of before. Registration order affects route matching priority.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47895**: abbreviations - Invalid abbreviation casing
  `* Gets the REST API controller for this post type.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47908**: abbreviations - Invalid abbreviation casing
  `* Gets the REST API revisions controller for this post type.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47921**: abbreviations - Invalid abbreviation casing
  `* Gets the REST API autosave controller for this post type.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47967**: abbreviations - Invalid abbreviation casing
  `* Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47972**: abbreviations - Invalid abbreviation casing
  `public $ID;`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 47974**: abbreviations - Invalid abbreviation casing
  `* ID of post author.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 48088**: abbreviations - Invalid abbreviation casing
  `* ID of a post's parent post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 48095**: abbreviations - Invalid abbreviation casing
  `* The unique identifier for a post, not necessarily a URL, used as the feed GUID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 48147**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 48266**: abbreviations - Invalid abbreviation casing
  `* The ID of the queried object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 48273**: abbreviations - Invalid abbreviation casing
  `* SQL for the database query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 48644**: abbreviations - Invalid abbreviation casing
  `*     @type int             $attachment_id          Attachment post ID. Used for 'attachment' post_type.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 48645**: abbreviations - Invalid abbreviation casing
  `*     @type int|string      $author                 Author ID, or comma-separated list of IDs.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 48650**: abbreviations - Invalid abbreviation casing
  `*     @type int|string      $cat                    Category ID or comma-separated list of IDs (this or any children).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 48670**: abbreviations - Invalid abbreviation casing
  `*                                                     keyed by post ID (`int[]`).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 48712**: abbreviations - Invalid abbreviation casing
  `*                                                   - 'ID'`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 48725**: abbreviations - Invalid abbreviation casing
  `*     @type int             $p                      Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 48729**: abbreviations - Invalid abbreviation casing
  `*     @type int             $page_id                Page ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 48738**: abbreviations - Invalid abbreviation casing
  `*     @type int             $post_parent            Page ID to retrieve child pages for. Use 0 to only retrieve`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 48873**: abbreviations - Invalid abbreviation casing
  `* Generates SQL for the WHERE clause based on passed search terms.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 48911**: abbreviations - Invalid abbreviation casing
  `* Generates SQL for the ORDER BY condition based on passed search terms.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49086**: abbreviations - Invalid abbreviation casing
  `* @param string|array $query URL query string or array of query arguments.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49107**: abbreviations - Invalid abbreviation casing
  `* Retrieves the ID of the currently queried object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49125**: abbreviations - Invalid abbreviation casing
  `* @param string|array $query URL query string or array of vars.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49201**: abbreviations - Invalid abbreviation casing
  `* @param int|string|int[]|string[] $attachment Optional. Attachment ID, title, slug, or array of such`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49216**: abbreviations - Invalid abbreviation casing
  `* @param int|string|int[]|string[] $author Optional. User ID, nickname, nicename, or array of such`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49231**: abbreviations - Invalid abbreviation casing
  `* @param int|string|int[]|string[] $category Optional. Category ID, name, slug, or array of such`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49246**: abbreviations - Invalid abbreviation casing
  `* @param int|string|int[]|string[] $tag Optional. Tag ID, name, slug, or array of such`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49269**: abbreviations - Invalid abbreviation casing
  `* @param int|string|int[]|string[] $term     Optional. Term ID, name, slug, or array of such`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49279**: abbreviations - Invalid abbreviation casing
  `* Determines whether the current URL is within the comments popup window.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49334**: abbreviations - Invalid abbreviation casing
  `* This is for what is displayed at your site's main URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49406**: abbreviations - Invalid abbreviation casing
  `* @param int|string|int[]|string[] $page Optional. Page ID, title, slug, path, or array of such`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49476**: abbreviations - Invalid abbreviation casing
  `* @param int|string|int[]|string[] $post Optional. Post ID, title, slug, path, or array of such`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49569**: abbreviations - Invalid abbreviation casing
  `* @since 4.4.0 Added the ability to pass a post ID to `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49581**: abbreviations - Invalid abbreviation casing
  `* @param WP_Post|object|int $post WP_Post instance or Post ID/object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49592**: abbreviations - Invalid abbreviation casing
  `* @param WP_Post|object|int $post WP_Post instance or Post ID/object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49606**: abbreviations - Invalid abbreviation casing
  `* @param string $sql  SQL statement.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49701**: abbreviations - Invalid abbreviation casing
  `* The cookie should be validated before calling this API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49707**: abbreviations - Invalid abbreviation casing
  `* @return string|WP_Error Session ID on success, or error object on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49843**: abbreviations - Invalid abbreviation casing
  `* Generates a URL to begin recovery mode.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49845**: abbreviations - Invalid abbreviation casing
  `* Only one recovery mode URL can may be valid at the same time.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49849**: abbreviations - Invalid abbreviation casing
  `* @return string Generated URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49907**: abbreviations - Invalid abbreviation casing
  `* Gets the recovery mode session ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49911**: abbreviations - Invalid abbreviation casing
  `* @return string The session ID if recovery mode is active, empty string otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 49931**: abbreviations - Invalid abbreviation casing
  `* The calling API should immediately die() after calling this function.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 50060**: abbreviations - Invalid abbreviation casing
  `* Core class used to implement a rewrite component API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 50284**: abbreviations - Invalid abbreviation casing
  `* Now it tells WP::parse_request() to check if a URL matching the page`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 50343**: abbreviations - Invalid abbreviation casing
  `* Means that permalink links are enabled and index.php is in the URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 50347**: abbreviations - Invalid abbreviation casing
  `* @return bool Whether permalink links are enabled and index.php is in the URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 50355**: abbreviations - Invalid abbreviation casing
  `* Using permalinks and index.php is not in the URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 50359**: abbreviations - Invalid abbreviation casing
  `* @return bool Whether permalink links are enabled and index.php is NOT in the URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 50417**: abbreviations - Invalid abbreviation casing
  `* Prevents post ID and date permalinks from overlapping. In the case of`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 50726**: abbreviations - Invalid abbreviation casing
  `* Retrieves IIS7 URL Rewrite formatted rewrite rules to write to web.config file.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 50735**: abbreviations - Invalid abbreviation casing
  `* @return string IIS7 URL rewrite rule sets.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 50741**: abbreviations - Invalid abbreviation casing
  `* Adds a rewrite rule that transforms a URL structure to a set of query vars.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 50960**: abbreviations - Invalid abbreviation casing
  `* Core class used to extend the user roles API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 51031**: abbreviations - Invalid abbreviation casing
  `* Core class used to implement a user roles API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 51085**: abbreviations - Invalid abbreviation casing
  `* The site ID the roles are initialized for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 51099**: abbreviations - Invalid abbreviation casing
  `* @param int $site_id Site ID to initialize roles for. Default is the current site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 51133**: abbreviations - Invalid abbreviation casing
  `* after switching wpdb to a new site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 51264**: abbreviations - Invalid abbreviation casing
  `* @param int $site_id Site ID to initialize roles for. Default is the current site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 51271**: abbreviations - Invalid abbreviation casing
  `* Gets the ID of the site for which roles are currently initialized.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 51275**: abbreviations - Invalid abbreviation casing
  `* @return int Site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 51309**: abbreviations - Invalid abbreviation casing
  `* @param string            $src      Optional. Full URL of the script module, or path of the script module relative`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 51328**: abbreviations - Invalid abbreviation casing
  `*                                    It is added to the URL as a query string for cache busting purposes. If $version`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 51397**: abbreviations - Invalid abbreviation casing
  `* @param string            $src      Optional. Full URL of the script module, or path of the script module relative`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 51416**: abbreviations - Invalid abbreviation casing
  `*                                    It is added to the URL as a query string for cache busting purposes. If $version`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 51518**: abbreviations - Invalid abbreviation casing
  `* The data for a Script Module will be serialized as JSON in a script tag with an ID of the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 51544**: abbreviations - Invalid abbreviation casing
  `* Base URL for scripts.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 51546**: abbreviations - Invalid abbreviation casing
  `* Full URL with trailing slash.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 51553**: abbreviations - Invalid abbreviation casing
  `* URL of the content directory.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 51931**: abbreviations - Invalid abbreviation casing
  `* User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 51934**: abbreviations - Invalid abbreviation casing
  `* @var int User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52127**: abbreviations - Invalid abbreviation casing
  `* This uses Core's HTTP API to make requests, which gives plugins the ability`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52149**: abbreviations - Invalid abbreviation casing
  `* @param string       $url             Remote file URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52202**: abbreviations - Invalid abbreviation casing
  `* SQL for database query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52209**: abbreviations - Invalid abbreviation casing
  `* SQL query clauses.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52291**: abbreviations - Invalid abbreviation casing
  `*     @type int             $ID                     A site ID to only return that site. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52315**: abbreviations - Invalid abbreviation casing
  `*     @type int             $network_id             Limit results to those affiliated with a given network ID. If 0,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52330**: abbreviations - Invalid abbreviation casing
  `*     @type int             $lang_id                Limit results to a language ID. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52357**: abbreviations - Invalid abbreviation casing
  `*   ID?: int,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52410**: abbreviations - Invalid abbreviation casing
  `*   ID?: int,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52454**: abbreviations - Invalid abbreviation casing
  `* @param string|array $query Array or URL query string of parameters.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52487**: abbreviations - Invalid abbreviation casing
  `* Used internally to generate an SQL string for searching across multiple columns.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52495**: abbreviations - Invalid abbreviation casing
  `* @return string Search SQL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52544**: abbreviations - Invalid abbreviation casing
  `* Site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52569**: abbreviations - Invalid abbreviation casing
  `* The ID of the site's parent network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52652**: abbreviations - Invalid abbreviation casing
  `* Retrieves a site from the database by its ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52658**: abbreviations - Invalid abbreviation casing
  `* @param int $site_id The ID of the site to retrieve.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52752**: abbreviations - Invalid abbreviation casing
  `* Checks whether a speculation rule for the given mode and ID already exists.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52765**: abbreviations - Invalid abbreviation casing
  `* Returns the speculation rules data ready to be JSON-encoded.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52819**: abbreviations - Invalid abbreviation casing
  `* Base URL for styles.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52821**: abbreviations - Invalid abbreviation casing
  `* Full URL with trailing slash.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52828**: abbreviations - Invalid abbreviation casing
  `* URL of the content directory.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52973**: abbreviations - Invalid abbreviation casing
  `* Generates an enqueued style's fully-qualified URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 52980**: abbreviations - Invalid abbreviation casing
  `* @return string Style's fully-qualified URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53032**: abbreviations - Invalid abbreviation casing
  `* Core class used to implement taxonomy queries for the Taxonomy API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53034**: abbreviations - Invalid abbreviation casing
  `* Used for generating SQL clauses that filter a primary query according to object`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53039**: abbreviations - Invalid abbreviation casing
  `* attached to the primary SQL query string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53087**: abbreviations - Invalid abbreviation casing
  `* Column in 'primary_table' that represents the ID of the object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53166**: abbreviations - Invalid abbreviation casing
  `* Generates SQL clauses to be appended to a main query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53171**: abbreviations - Invalid abbreviation casing
  `* @param string $primary_id_column ID column for the filtered object in $primary_table.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53173**: abbreviations - Invalid abbreviation casing
  `*     Array containing JOIN and WHERE SQL clauses to append to the main query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53175**: abbreviations - Invalid abbreviation casing
  `*     @type string $join  SQL fragment to append to the main JOIN clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53176**: abbreviations - Invalid abbreviation casing
  `*     @type string $where SQL fragment to append to the main WHERE clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53187**: abbreviations - Invalid abbreviation casing
  `* Generates SQL clauses to be appended to a main query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53195**: abbreviations - Invalid abbreviation casing
  `*     Array containing JOIN and WHERE SQL clauses to append to the main query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53197**: abbreviations - Invalid abbreviation casing
  `*     @type string $join  SQL fragment to append to the main JOIN clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53198**: abbreviations - Invalid abbreviation casing
  `*     @type string $where SQL fragment to append to the main WHERE clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53209**: abbreviations - Invalid abbreviation casing
  `* Generates SQL clauses for a single query array.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53212**: abbreviations - Invalid abbreviation casing
  `* produce the properly nested SQL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53220**: abbreviations - Invalid abbreviation casing
  `*     Array containing JOIN and WHERE SQL clauses to append to a single query array.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53222**: abbreviations - Invalid abbreviation casing
  `*     @type string $join  SQL fragment to append to the main JOIN clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53223**: abbreviations - Invalid abbreviation casing
  `*     @type string $where SQL fragment to append to the main WHERE clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53234**: abbreviations - Invalid abbreviation casing
  `* Generates SQL JOIN and WHERE clauses for a "first-order" query clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53243**: abbreviations - Invalid abbreviation casing
  `*     Array containing JOIN and WHERE SQL clauses to append to a first-order query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53245**: abbreviations - Invalid abbreviation casing
  `*     @type string[] $join  Array of SQL fragments to append to the main JOIN clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53246**: abbreviations - Invalid abbreviation casing
  `*     @type string[] $where Array of SQL fragments to append to the main WHERE clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53452**: abbreviations - Invalid abbreviation casing
  `* Whether this taxonomy should appear in the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53462**: abbreviations - Invalid abbreviation casing
  `* The base path for this taxonomy's REST API endpoints.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53469**: abbreviations - Invalid abbreviation casing
  `* The namespace for this taxonomy's REST API endpoints.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53476**: abbreviations - Invalid abbreviation casing
  `* The controller for this taxonomy's REST API endpoints.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53485**: abbreviations - Invalid abbreviation casing
  `* The controller instance for this taxonomy's REST API endpoints.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53631**: abbreviations - Invalid abbreviation casing
  `* Gets the REST API controller for this taxonomy.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53673**: abbreviations - Invalid abbreviation casing
  `* SQL string used to perform database query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53694**: abbreviations - Invalid abbreviation casing
  `* SQL query clauses.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53739**: abbreviations - Invalid abbreviation casing
  `*     @type int|int[]       $object_ids             Object ID, or array of object IDs. Results will be`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53783**: abbreviations - Invalid abbreviation casing
  `*                                                      keyed by term ID (`int[]`).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53785**: abbreviations - Invalid abbreviation casing
  `*                                                      keyed by term ID (`string[]`).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53787**: abbreviations - Invalid abbreviation casing
  `*                                                      keyed by term ID (`string[]`).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53793**: abbreviations - Invalid abbreviation casing
  `*     @type int|int[]       $term_taxonomy_id       Term taxonomy ID, or array of term taxonomy IDs,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53797**: abbreviations - Invalid abbreviation casing
  `*     @type string          $search                 Search criteria to match terms. Will be SQL-formatted with`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53809**: abbreviations - Invalid abbreviation casing
  `*     @type int             $child_of               Term ID to retrieve child terms of. If multiple taxonomies`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53811**: abbreviations - Invalid abbreviation casing
  `*     @type int             $parent                 Parent term ID to retrieve direct-child terms of.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 53925**: abbreviations - Invalid abbreviation casing
  `* @param string|array $query Array or URL query string of parameters.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 54019**: abbreviations - Invalid abbreviation casing
  `* Used internally to generate a SQL string related to the 'search' parameter.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 54026**: abbreviations - Invalid abbreviation casing
  `* @return string Search SQL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 54052**: abbreviations - Invalid abbreviation casing
  `* @param string $sql  SQL statement.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 54071**: abbreviations - Invalid abbreviation casing
  `* Term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 54099**: abbreviations - Invalid abbreviation casing
  `* Term Taxonomy ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 54120**: abbreviations - Invalid abbreviation casing
  `* ID of a term's parent term.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 54149**: abbreviations - Invalid abbreviation casing
  `* @param int    $term_id  Term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 54746**: abbreviations - Invalid abbreviation casing
  `* Returns theme JSON object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 54750**: abbreviations - Invalid abbreviation casing
  `* @return WP_Theme_JSON The theme JSON structure stored in this data object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 54758**: abbreviations - Invalid abbreviation casing
  `* for site-level config and offers an API to work with them.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 54761**: abbreviations - Invalid abbreviation casing
  `* This is a low-level API that may need to do breaking changes. Please,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 54805**: abbreviations - Invalid abbreviation casing
  `* Stores the ID of the custom post type`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 54999**: abbreviations - Invalid abbreviation casing
  `* Returns the ID of the custom post type`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 55067**: abbreviations - Invalid abbreviation casing
  `* as the value of `_link` object in REST API responses.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 55080**: abbreviations - Invalid abbreviation casing
  `* and merges them with incoming theme JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 55095**: abbreviations - Invalid abbreviation casing
  `* This is a low-level API that may need to do breaking changes. Please,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 55129**: abbreviations - Invalid abbreviation casing
  `* This is a low-level API that may need to do breaking changes. Please,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 55592**: abbreviations - Invalid abbreviation casing
  `* @since 6.3.0 Refactored and stabilized selectors API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 56036**: abbreviations - Invalid abbreviation casing
  `* @since 6.6.0 Pass current theme JSON settings to wp_get_typography_font_size_value(), and process background properties.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 56042**: abbreviations - Invalid abbreviation casing
  `* @param array   $theme_json Theme JSON array.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 56068**: abbreviations - Invalid abbreviation casing
  `* @param array $theme_json Theme JSON array.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 56758**: abbreviations - Invalid abbreviation casing
  `* Returns the URL to the directory of a theme's "stylesheet" files.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 56760**: abbreviations - Invalid abbreviation casing
  `* In the case of a child theme, this is the URL to the directory of the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 56765**: abbreviations - Invalid abbreviation casing
  `* @return string URL to the stylesheet directory.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 56771**: abbreviations - Invalid abbreviation casing
  `* Returns the URL to the directory of a theme's "template" files.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 56773**: abbreviations - Invalid abbreviation casing
  `* In the case of a child theme, this is the URL to the directory of the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 56778**: abbreviations - Invalid abbreviation casing
  `* @return string URL to the template directory.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 56796**: abbreviations - Invalid abbreviation casing
  `* Returns the URL to the directory of the theme root.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 56798**: abbreviations - Invalid abbreviation casing
  `* This is typically the absolute URL to wp-content/themes. This forms the basis`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 56819**: abbreviations - Invalid abbreviation casing
  `* @param string $uri Type of URL to return, either 'relative' or an absolute URI. Defaults to absolute URI.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 56935**: abbreviations - Invalid abbreviation casing
  `* @param int $blog_id Optional. ID of the site. Defaults to the current site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 56956**: abbreviations - Invalid abbreviation casing
  `* @param int $blog_id Optional. ID of the site. Defaults to the current site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 57379**: abbreviations - Invalid abbreviation casing
  `* Class for prefixing URL patterns.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 57401**: abbreviations - Invalid abbreviation casing
  `* Prefixes the given URL path pattern with the base path for the given context.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 57406**: abbreviations - Invalid abbreviation casing
  `* The given URL path pattern is only prefixed if it does not already include the expected prefix.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 57410**: abbreviations - Invalid abbreviation casing
  `* @param string $path_pattern URL pattern starting with the path segment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 57412**: abbreviations - Invalid abbreviation casing
  `* @return string URL pattern, prefixed as necessary.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 57542**: abbreviations - Invalid abbreviation casing
  `* The SQL query used to fetch matching users.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 57636**: abbreviations - Invalid abbreviation casing
  `*     @type int             $blog_id             The site ID. Default is the current site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 57674**: abbreviations - Invalid abbreviation casing
  `*     @type string[]        $search_columns      Array of column names to be searched. Accepts 'ID', 'user_login',`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 57680**: abbreviations - Invalid abbreviation casing
  `*                                                - 'ID'`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 57713**: abbreviations - Invalid abbreviation casing
  `*                                                - 'ID'`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 57820**: abbreviations - Invalid abbreviation casing
  `* Used internally to generate an SQL string for searching across multiple columns.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 57877**: abbreviations - Invalid abbreviation casing
  `* @param string $sql        SQL statement.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 57978**: abbreviations - Invalid abbreviation casing
  `* Request ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 57983**: abbreviations - Invalid abbreviation casing
  `public $ID = 0;`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 57985**: abbreviations - Invalid abbreviation casing
  `* User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58109**: abbreviations - Invalid abbreviation casing
  `* The user's ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58114**: abbreviations - Invalid abbreviation casing
  `public $ID = 0;`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58161**: abbreviations - Invalid abbreviation casing
  `* @param int|string|stdClass|WP_User $id      User's ID, a WP_User object, or a user object from the DB.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58163**: abbreviations - Invalid abbreviation casing
  `* @param int                         $site_id Optional Site ID, defaults to current site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58175**: abbreviations - Invalid abbreviation casing
  `* @param int    $site_id Optional. The site ID to initialize for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58184**: abbreviations - Invalid abbreviation casing
  `* @since 4.4.0 Added 'ID' as an alias of 'id' for the `$field` parameter.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58188**: abbreviations - Invalid abbreviation casing
  `* @param string     $field The field to query against: Accepts 'id', 'ID', 'slug', 'email' or 'login'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58191**: abbreviations - Invalid abbreviation casing
  `* @phpstan-param 'id'|'ID'|'slug'|'email'|'login' $field`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58213**: abbreviations - Invalid abbreviation casing
  `* @return mixed Value of the given user meta key (if set). If `$key` is 'id', the user ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58444**: abbreviations - Invalid abbreviation casing
  `* This function also accepts an ID of an object to check against if the capability is a meta capability. Meta`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58451**: abbreviations - Invalid abbreviation casing
  `*     $user->has_cap( 'edit_post', $post->ID );`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58452**: abbreviations - Invalid abbreviation casing
  `*     $user->has_cap( 'edit_post_meta', $post->ID, $meta_key );`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58464**: abbreviations - Invalid abbreviation casing
  `* @param mixed  ...$args Optional further parameters, typically starting with an object ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58465**: abbreviations - Invalid abbreviation casing
  `* @return bool Whether the user has the given capability, or, if an object ID is passed, whether the user has`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58490**: abbreviations - Invalid abbreviation casing
  `* @param int $blog_id Optional. Site ID, defaults to current site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58502**: abbreviations - Invalid abbreviation casing
  `* @param int $site_id Site ID to initialize user capabilities for. Default is the current site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58508**: abbreviations - Invalid abbreviation casing
  `* Gets the ID of the site for which the user's capabilities are currently initialized.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58512**: abbreviations - Invalid abbreviation casing
  `* @return int Site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58595**: abbreviations - Invalid abbreviation casing
  `* @param string $id_base Widget type ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58606**: abbreviations - Invalid abbreviation casing
  `* @param string $id_base Widget type ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58628**: abbreviations - Invalid abbreviation casing
  `* Root ID for all widgets of this type.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58670**: abbreviations - Invalid abbreviation casing
  `* Unique ID number of the current instance.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58677**: abbreviations - Invalid abbreviation casing
  `* Unique ID string of the current instance (id_base-number).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58745**: abbreviations - Invalid abbreviation casing
  `* @param string $id_base         Base ID for the widget, lowercase and unique. If left empty,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58774**: abbreviations - Invalid abbreviation casing
  `* @param string $id_base         Base ID for the widget, lowercase and unique. If left empty,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58821**: abbreviations - Invalid abbreviation casing
  `* @return string ID attribute for `$field_name`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58986**: abbreviations - Invalid abbreviation casing
  `* Implements compatibility for Blogger API, MetaWeblog API, MovableType, and`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 58987**: abbreviations - Invalid abbreviation casing
  `* pingback. Additional WordPress API for managing comments, pages, posts,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59057**: abbreviations - Invalid abbreviation casing
  `* Tests XMLRPC API by saying, "Hello!" to client.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59067**: abbreviations - Invalid abbreviation casing
  `* Tests XMLRPC API by adding two numbers for client.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59151**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59162**: abbreviations - Invalid abbreviation casing
  `* @param int   $post_id Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59173**: abbreviations - Invalid abbreviation casing
  `* @param int $term_id Term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59184**: abbreviations - Invalid abbreviation casing
  `* @param int   $term_id Term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59303**: abbreviations - Invalid abbreviation casing
  `* @param string  $thumbnail_size The image size to use for the thumbnail URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59347**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59357**: abbreviations - Invalid abbreviation casing
  `*         @type int    $post_author    Post author ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59367**: abbreviations - Invalid abbreviation casing
  `*         @type int    $post_thumbnail ID of an image to use as the post thumbnail/featured image.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59376**: abbreviations - Invalid abbreviation casing
  `*             @type string $url    URL for the feed enclosure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59382**: abbreviations - Invalid abbreviation casing
  `* @return int|IXR_Error Post ID on success, IXR_Error instance otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59440**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59443**: abbreviations - Invalid abbreviation casing
  `*     @type int    $3 Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59468**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59471**: abbreviations - Invalid abbreviation casing
  `*     @type int    $3 Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59503**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59506**: abbreviations - Invalid abbreviation casing
  `*     @type int    $3 Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59555**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59585**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59592**: abbreviations - Invalid abbreviation casing
  `* @return int|IXR_Error The term ID on success, or an IXR_Error object on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59613**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59616**: abbreviations - Invalid abbreviation casing
  `*     @type int    $3 Term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59643**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59647**: abbreviations - Invalid abbreviation casing
  `*     @type int    $4 Term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59671**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59675**: abbreviations - Invalid abbreviation casing
  `*     @type int    $4 Term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59711**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59740**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59770**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59805**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59808**: abbreviations - Invalid abbreviation casing
  `*     @type int    $3 User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59851**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59877**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59901**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59932**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59933**: abbreviations - Invalid abbreviation casing
  `*     @type int    $1 Page ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59956**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 59982**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60006**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60009**: abbreviations - Invalid abbreviation casing
  `*     @type int    $3 Page ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60030**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60031**: abbreviations - Invalid abbreviation casing
  `*     @type int    $1 Page ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60060**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60082**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60104**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60126**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60131**: abbreviations - Invalid abbreviation casing
  `* @return int|IXR_Error Category ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60150**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60153**: abbreviations - Invalid abbreviation casing
  `*     @type int    $3 Category ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60174**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60200**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60203**: abbreviations - Invalid abbreviation casing
  `*     @type int    $3 Comment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60236**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60265**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60268**: abbreviations - Invalid abbreviation casing
  `*     @type int    $3 Comment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60300**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60303**: abbreviations - Invalid abbreviation casing
  `*     @type int    $3 Comment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60326**: abbreviations - Invalid abbreviation casing
  `*     @type int        $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60329**: abbreviations - Invalid abbreviation casing
  `*     @type string|int $3 Post ID or URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60352**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60374**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60377**: abbreviations - Invalid abbreviation casing
  `*     @type int    $3 Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60398**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60420**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60442**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60464**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60499**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60516**: abbreviations - Invalid abbreviation casing
  `* Retrieves a media item by ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60523**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60526**: abbreviations - Invalid abbreviation casing
  `*     @type int    $3 Attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60567**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60593**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60617**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60654**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60686**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60689**: abbreviations - Invalid abbreviation casing
  `*     @type int    $3 Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60714**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60717**: abbreviations - Invalid abbreviation casing
  `*     @type int    $3 Revision ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60740**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60762**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60786**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60808**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60809**: abbreviations - Invalid abbreviation casing
  `*     @type int    $1 Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60833**: abbreviations - Invalid abbreviation casing
  `*     @type int    $1 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60883**: abbreviations - Invalid abbreviation casing
  `*     @type int    $1 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60910**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60911**: abbreviations - Invalid abbreviation casing
  `*     @type int    $1 Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60938**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60939**: abbreviations - Invalid abbreviation casing
  `*     @type int    $1 Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 60984**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61007**: abbreviations - Invalid abbreviation casing
  `* @param int   $post_id   Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61020**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id      Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61034**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61060**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61082**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61106**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61132**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61156**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61180**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Blog ID (unused).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61202**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61224**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61279**: abbreviations - Invalid abbreviation casing
  `*     @type int    $0 Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61303**: abbreviations - Invalid abbreviation casing
  `*     @type string $0 URL of page linked from.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61304**: abbreviations - Invalid abbreviation casing
  `*     @type string $1 URL of page linked to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61316**: abbreviations - Invalid abbreviation casing
  `* Retrieves an array of URLs that pingbacked the given URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61506**: abbreviations - Invalid abbreviation casing
  `* @global string       $request      The SQL statement for the request.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61533**: abbreviations - Invalid abbreviation casing
  `* Set the Headers for 404, if nothing is found for requested URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61571**: abbreviations - Invalid abbreviation casing
  `* This class is used to interact with a database without needing to use raw SQL statements.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61587**: abbreviations - Invalid abbreviation casing
  `* Whether to show SQL/DB errors.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61637**: abbreviations - Invalid abbreviation casing
  `* The ID generated for an AUTO_INCREMENT column by the last query (usually INSERT).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61721**: abbreviations - Invalid abbreviation casing
  `*         @type string $0 The query's SQL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61773**: abbreviations - Invalid abbreviation casing
  `* Blog ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61781**: abbreviations - Invalid abbreviation casing
  `* Site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 61991**: abbreviations - Invalid abbreviation casing
  `* Keys are column names, values are format types: 'ID' => '%d'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62087**: abbreviations - Invalid abbreviation casing
  `* A list of incompatible SQL modes.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62105**: abbreviations - Invalid abbreviation casing
  `* The last SQL error that was encountered.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62225**: abbreviations - Invalid abbreviation casing
  `* Changes the current SQL mode, and ensures its WordPress compatibility.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62227**: abbreviations - Invalid abbreviation casing
  `* If no modes are passed, it will ensure the current SQL server modes are compatible.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62231**: abbreviations - Invalid abbreviation casing
  `* @param array $modes Optional. A list of SQL modes to set. Default empty array.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62251**: abbreviations - Invalid abbreviation casing
  `* Sets blog ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62256**: abbreviations - Invalid abbreviation casing
  `* @param int $network_id Optional. Network ID. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62257**: abbreviations - Invalid abbreviation casing
  `* @return int Previous blog ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62267**: abbreviations - Invalid abbreviation casing
  `* @param int $blog_id Optional. Blog ID to retrieve the table prefix for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62268**: abbreviations - Invalid abbreviation casing
  `*                     Defaults to the current blog ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62405**: abbreviations - Invalid abbreviation casing
  `* Prepares a SQL query for safe execution.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62473**: abbreviations - Invalid abbreviation casing
  `* First half of escaping for `LIKE` special characters `%` and `_` before preparing for SQL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62492**: abbreviations - Invalid abbreviation casing
  `* @return string Text in the form of a LIKE phrase. The output is not SQL safe.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62499**: abbreviations - Invalid abbreviation casing
  `* Prints SQL/DB error.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62646**: abbreviations - Invalid abbreviation casing
  `* @param string $query           The query's SQL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62719**: abbreviations - Invalid abbreviation casing
  `*                                Both `$data` columns and `$data` values should be "raw" (neither should be SQL escaped).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62743**: abbreviations - Invalid abbreviation casing
  `*             'ID'      => 123,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62751**: abbreviations - Invalid abbreviation casing
  `*             'ID'      => 456,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62770**: abbreviations - Invalid abbreviation casing
  `*                                Both `$data` columns and `$data` values should be "raw" (neither should be SQL escaped).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62797**: abbreviations - Invalid abbreviation casing
  `*                                Both `$data` columns and `$data` values should be "raw" (neither should be SQL escaped).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62825**: abbreviations - Invalid abbreviation casing
  `*             'ID' => 1,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62835**: abbreviations - Invalid abbreviation casing
  `*             'ID' => 1,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62854**: abbreviations - Invalid abbreviation casing
  `*                                      Both $data columns and $data values should be "raw" (neither should be SQL escaped).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62885**: abbreviations - Invalid abbreviation casing
  `*             'ID' => 1,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 62891**: abbreviations - Invalid abbreviation casing
  `*             'ID' => 1,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63065**: abbreviations - Invalid abbreviation casing
  `* Executes a SQL query and returns the value from the SQL result.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63066**: abbreviations - Invalid abbreviation casing
  `* If the SQL result contains more than one column and/or more than one row,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63068**: abbreviations - Invalid abbreviation casing
  `* the value in the specified column and row from the previous SQL result is returned.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63072**: abbreviations - Invalid abbreviation casing
  `* @param string|null $query Optional. SQL query. Defaults to null, use the result from the previous query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63083**: abbreviations - Invalid abbreviation casing
  `* Executes a SQL query and returns the row from the SQL result.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63087**: abbreviations - Invalid abbreviation casing
  `* @param string|null $query  SQL query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63103**: abbreviations - Invalid abbreviation casing
  `* Executes a SQL query and returns the column from the SQL result.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63104**: abbreviations - Invalid abbreviation casing
  `* If the SQL result contains more than one column, the column specified is returned.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63105**: abbreviations - Invalid abbreviation casing
  `* If $query is null, the specified column from the previous SQL result is returned.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63109**: abbreviations - Invalid abbreviation casing
  `* @param string|null $query Optional. SQL query. Defaults to previous query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63111**: abbreviations - Invalid abbreviation casing
  `* @return array Database query result. Array indexed from 0 by SQL result row number.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63117**: abbreviations - Invalid abbreviation casing
  `* Retrieves an entire SQL result set from the database (i.e., many rows).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63119**: abbreviations - Invalid abbreviation casing
  `* Executes a SQL query and returns the entire SQL result.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63123**: abbreviations - Invalid abbreviation casing
  `* @param string $query  SQL query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63126**: abbreviations - Invalid abbreviation casing
  `*                       from 0 by SQL result row number. Each row is an associative array`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63466**: abbreviations - Invalid abbreviation casing
  `* @param string               $id      Control ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63502**: abbreviations - Invalid abbreviation casing
  `* Refresh the parameters passed to the JavaScript via JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63579**: abbreviations - Invalid abbreviation casing
  `* Refresh the parameters passed to the JavaScript via JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63784**: abbreviations - Invalid abbreviation casing
  `* Refresh the parameters passed to the JavaScript via JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63848**: abbreviations - Invalid abbreviation casing
  `* @param string               $id      Control ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63883**: abbreviations - Invalid abbreviation casing
  `* Refresh the parameters passed to the JavaScript via JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 63961**: abbreviations - Invalid abbreviation casing
  `* Refresh the parameters passed to the JavaScript via JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 64013**: abbreviations - Invalid abbreviation casing
  `* @throws Exception If the setting ID does not match the pattern `custom_css[$stylesheet]`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 64016**: abbreviations - Invalid abbreviation casing
  `* @param string               $id      A specific ID of the setting.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 64084**: abbreviations - Invalid abbreviation casing
  `* @return int|false The post ID or false if the value could not be saved.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 64228**: abbreviations - Invalid abbreviation casing
  `* Saves the value of the setting, using the related API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 64443**: abbreviations - Invalid abbreviation casing
  `* @param string               $id      The control ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 64558**: abbreviations - Invalid abbreviation casing
  `* The post ID represented by this setting instance. This is the db_id.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 64560**: abbreviations - Invalid abbreviation casing
  `* A negative value represents a placeholder ID for a new menu not yet saved.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 64574**: abbreviations - Invalid abbreviation casing
  `* Previous (placeholder) post ID used before creating a new menu item.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 64578**: abbreviations - Invalid abbreviation casing
  `* post ID. This value is always negative to indicate it does not refer to`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 64608**: abbreviations - Invalid abbreviation casing
  `* When status is inserted, the placeholder post ID is stored in $previous_post_id.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 64638**: abbreviations - Invalid abbreviation casing
  `* @param string               $id      A specific ID of the setting.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 64650**: abbreviations - Invalid abbreviation casing
  `* @param int $menu_id       The term ID for the menu.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 64651**: abbreviations - Invalid abbreviation casing
  `* @param int $menu_item_id  The post ID for the menu item.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 64837**: abbreviations - Invalid abbreviation casing
  `* Location ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 64844**: abbreviations - Invalid abbreviation casing
  `* Refresh the parameters passed to JavaScript via JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 64998**: abbreviations - Invalid abbreviation casing
  `* The term ID represented by this setting instance.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65000**: abbreviations - Invalid abbreviation casing
  `* A negative value represents a placeholder ID for a new menu not yet saved.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65007**: abbreviations - Invalid abbreviation casing
  `* Previous (placeholder) term ID used before creating a new menu.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65011**: abbreviations - Invalid abbreviation casing
  `* term ID. This value is always negative to indicate it does not refer to`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65033**: abbreviations - Invalid abbreviation casing
  `* When status is inserted, the placeholder term ID is stored in `$previous_term_id`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65063**: abbreviations - Invalid abbreviation casing
  `* @param string               $id      A specific ID of the setting.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65139**: abbreviations - Invalid abbreviation casing
  `* Requesting a nav_menu object by anything but ID is not supported.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65146**: abbreviations - Invalid abbreviation casing
  `* @param string      $menu_id  ID of the nav_menu term. Requests by slug or name will be ignored.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65232**: abbreviations - Invalid abbreviation casing
  `* @param int   $menu_id          The term ID for the given menu.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65330**: abbreviations - Invalid abbreviation casing
  `* @param string               $id      The control ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65392**: abbreviations - Invalid abbreviation casing
  `* @param string               $id      A specific ID of the section.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65431**: abbreviations - Invalid abbreviation casing
  `* be the same as the associated setting's ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65438**: abbreviations - Invalid abbreviation casing
  `* Parsed ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65442**: abbreviations - Invalid abbreviation casing
  `*     @type string $base ID base.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65473**: abbreviations - Invalid abbreviation casing
  `* The ID for the setting that this partial is primarily responsible for rendering.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65475**: abbreviations - Invalid abbreviation casing
  `* If not supplied, it will default to the ID of the first setting.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65523**: abbreviations - Invalid abbreviation casing
  `* If `$args['settings']` is not defined, use the $id as the setting ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65528**: abbreviations - Invalid abbreviation casing
  `* @param string                         $id        Control ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65536**: abbreviations - Invalid abbreviation casing
  `*     @type string   $primary_setting       The ID for the setting that this partial is primarily responsible for`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65537**: abbreviations - Invalid abbreviation casing
  `*                                           rendering. If not supplied, it will default to the ID of the first setting.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65566**: abbreviations - Invalid abbreviation casing
  `* Retrieves parsed ID data for multidimensional setting.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65571**: abbreviations - Invalid abbreviation casing
  `*     ID data for multidimensional partial.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65573**: abbreviations - Invalid abbreviation casing
  `*     @type string $base ID base.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65619**: abbreviations - Invalid abbreviation casing
  `* Retrieves the data to export to the client via JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65691**: abbreviations - Invalid abbreviation casing
  `* @param WP_Customize_Partial|string $id   Customize Partial object, or Partial ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65715**: abbreviations - Invalid abbreviation casing
  `* @param string $id Customize Partial ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65726**: abbreviations - Invalid abbreviation casing
  `* @param string $id Customize Partial ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65831**: abbreviations - Invalid abbreviation casing
  `* Gather the parameters passed to client JavaScript via JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65835**: abbreviations - Invalid abbreviation casing
  `* @return array The array to be exported to the client as JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65877**: abbreviations - Invalid abbreviation casing
  `* @param string               $id      Control ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 65936**: abbreviations - Invalid abbreviation casing
  `* Refresh the parameters passed to the JavaScript via JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 66126**: abbreviations - Invalid abbreviation casing
  `* Sidebar ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 66133**: abbreviations - Invalid abbreviation casing
  `* Refreshes the parameters passed to the JavaScript via JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 66166**: abbreviations - Invalid abbreviation casing
  `* Widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 66173**: abbreviations - Invalid abbreviation casing
  `* Widget ID base.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 66180**: abbreviations - Invalid abbreviation casing
  `* Sidebar ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 66800**: abbreviations - Invalid abbreviation casing
  `* HTML API: WP_HTML_Decoder class`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 66805**: abbreviations - Invalid abbreviation casing
  `* @subpackage HTML-API`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 66960**: abbreviations - Invalid abbreviation casing
  `* Core class used by the HTML API to represent a DOCTYPE declaration.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 67115**: abbreviations - Invalid abbreviation casing
  `*     $doctype = WP_HTML_Doctype_Info::from_doctype_token( '<!doctypeJSON SILLY "nonsense\'>' );`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 68393**: abbreviations - Invalid abbreviation casing
  `* This allows the HTML API to process the content that will be presented in`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 68482**: abbreviations - Invalid abbreviation casing
  `*       - CSS class and ID selectors match byte-for-byte (case-sensitively).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 68486**: abbreviations - Invalid abbreviation casing
  `*       - CSS class and ID selectors match match in an ASCII case-insensitive manner.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 69012**: abbreviations - Invalid abbreviation casing
  `* This matches the DOM API value `nodeName`. Some values`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 69061**: abbreviations - Invalid abbreviation casing
  `* types in the HTML API cannot allow their entire comment text content to`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 69181**: abbreviations - Invalid abbreviation casing
  `* The HTML API will encode the strings appropriately so that the browser will interpret them`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 69205**: abbreviations - Invalid abbreviation casing
  `* The HTML API will encode the strings appropriately so that the browser will interpret them`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 70147**: abbreviations - Invalid abbreviation casing
  `* This matches the DOM API value `nodeName`. Some values`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 70218**: abbreviations - Invalid abbreviation casing
  `* The HTML API will encode the strings appropriately so that the browser will interpret them`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 70628**: abbreviations - Invalid abbreviation casing
  `* doing so with a full understanding that it's intended to be a private API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 70907**: abbreviations - Invalid abbreviation casing
  `* The HTML API aims to operate in compliance with the HTML5`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 70953**: abbreviations - Invalid abbreviation casing
  `* and other syntactic variations that are normally abstracted in the HTML API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 71108**: abbreviations - Invalid abbreviation casing
  `* Class used to process the Interactivity API on the server.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 71115**: abbreviations - Invalid abbreviation casing
  `* Gets and/or sets the initial state of an Interactivity API store for a`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 71137**: abbreviations - Invalid abbreviation casing
  `* Gets and/or sets the configuration of the Interactivity API for a given`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 71157**: abbreviations - Invalid abbreviation casing
  `* Encodes the config and initial state into JSON and prints them inside a`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 71193**: abbreviations - Invalid abbreviation casing
  `* @return array Data for the Interactivity API script module.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 71234**: abbreviations - Invalid abbreviation casing
  `* Adds the necessary hooks for the Interactivity API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 71247**: abbreviations - Invalid abbreviation casing
  `* for the Interactivity API router, indicating which modules can be loaded`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 71262**: abbreviations - Invalid abbreviation casing
  `* navigation in the Interactivity API router. Script modules marked with`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 72997**: abbreviations - Invalid abbreviation casing
  `* Used to determine if the JSON data has been parsed yet.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 72999**: abbreviations - Invalid abbreviation casing
  `* Allows lazy-parsing of JSON data where possible.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73168**: abbreviations - Invalid abbreviation casing
  `* Checks if the request has specified a JSON Content-Type.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73172**: abbreviations - Invalid abbreviation casing
  `* @return bool True if the Content-Type header is JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73254**: abbreviations - Invalid abbreviation casing
  `* These are parsed from the URL using the regex.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73266**: abbreviations - Invalid abbreviation casing
  `* Typically, this is set after parsing the URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73392**: abbreviations - Invalid abbreviation casing
  `* Retrieves the parameters from a JSON-formatted body.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73402**: abbreviations - Invalid abbreviation casing
  `* Parses the JSON parameters.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73404**: abbreviations - Invalid abbreviation casing
  `* Avoids parsing the JSON data until we need to access it.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73408**: abbreviations - Invalid abbreviation casing
  `* @return true|WP_Error True if the JSON data was passed or no JSON data was provided, WP_Error if invalid JSON was passed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73416**: abbreviations - Invalid abbreviation casing
  `* Parses out URL-encoded bodies for request methods that aren't supported`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73550**: abbreviations - Invalid abbreviation casing
  `* Retrieves a WP_REST_Request object from a full URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73554**: abbreviations - Invalid abbreviation casing
  `* @param string $url URL with protocol, domain, path and query args.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73602**: abbreviations - Invalid abbreviation casing
  `*                           or an absolute URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73604**: abbreviations - Invalid abbreviation casing
  `* @param array  $attributes Optional. Link parameters to send along with the URL. Default empty array.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73614**: abbreviations - Invalid abbreviation casing
  `* @param string      $rel  Link relation. Either an IANA registered type, or an absolute URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73627**: abbreviations - Invalid abbreviation casing
  `* (including `href` with the URL for the response), or a list of these`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73657**: abbreviations - Invalid abbreviation casing
  `* @param string $rel   Link relation. Either an IANA registered type, or an absolute URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73737**: abbreviations - Invalid abbreviation casing
  `* Core class used to implement the WordPress REST API server.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73838**: abbreviations - Invalid abbreviation casing
  `* list in JSON rather than an object/map.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73850**: abbreviations - Invalid abbreviation casing
  `* Retrieves an appropriate error representation in JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73862**: abbreviations - Invalid abbreviation casing
  `* @return string JSON representation of the error.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73874**: abbreviations - Invalid abbreviation casing
  `* @return int The JSON encode options.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73880**: abbreviations - Invalid abbreviation casing
  `* Handles serving a REST API request.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73883**: abbreviations - Invalid abbreviation casing
  `* callback then outputs a JSON representation of the returned value.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 73935**: abbreviations - Invalid abbreviation casing
  `* Gets the target hints for a REST API Link.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74073**: abbreviations - Invalid abbreviation casing
  `* This may be a standalone REST API request, or an internal request dispatched from within a regular page load.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74110**: abbreviations - Invalid abbreviation casing
  `* Returns if an error occurred during most recent JSON encode/decode.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74130**: abbreviations - Invalid abbreviation casing
  `* @return WP_REST_Response The API root index data.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74140**: abbreviations - Invalid abbreviation casing
  `* @param WP_REST_Response $response REST API response.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74146**: abbreviations - Invalid abbreviation casing
  `* Exposes the site logo through the WordPress REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74153**: abbreviations - Invalid abbreviation casing
  `* @param WP_REST_Response $response REST API response.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74159**: abbreviations - Invalid abbreviation casing
  `* Exposes the site icon through the WordPress REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74166**: abbreviations - Invalid abbreviation casing
  `* @param WP_REST_Response $response REST API response.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74172**: abbreviations - Invalid abbreviation casing
  `* Exposes an image through the WordPress REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74178**: abbreviations - Invalid abbreviation casing
  `* @param WP_REST_Response $response REST API response.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74179**: abbreviations - Invalid abbreviation casing
  `* @param int              $image_id Image attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74311**: abbreviations - Invalid abbreviation casing
  `* Core base controller for managing and interacting with REST API items.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74506**: abbreviations - Invalid abbreviation casing
  `* Retrieves the item's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74644**: abbreviations - Invalid abbreviation casing
  `* @see https://github.com/WP-API/WP-API/issues/1585`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74656**: abbreviations - Invalid abbreviation casing
  `* Core controller used to access ability categories via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74665**: abbreviations - Invalid abbreviation casing
  `* REST API namespace.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74672**: abbreviations - Invalid abbreviation casing
  `* REST API base route.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74745**: abbreviations - Invalid abbreviation casing
  `* Retrieves the ability category's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74766**: abbreviations - Invalid abbreviation casing
  `* Core controller used to access abilities via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74775**: abbreviations - Invalid abbreviation casing
  `* REST API namespace.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74782**: abbreviations - Invalid abbreviation casing
  `* REST API base route.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74855**: abbreviations - Invalid abbreviation casing
  `* Retrieves the ability's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74876**: abbreviations - Invalid abbreviation casing
  `* Core controller used to execute abilities via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74885**: abbreviations - Invalid abbreviation casing
  `* REST API namespace.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74892**: abbreviations - Invalid abbreviation casing
  `* REST API base route.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74964**: abbreviations - Invalid abbreviation casing
  `* Core class to access a user's application passwords via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 74981**: abbreviations - Invalid abbreviation casing
  `* Registers the REST API routes for the application passwords controller.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 75222**: abbreviations - Invalid abbreviation casing
  `* Retrieves the application password's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 75233**: abbreviations - Invalid abbreviation casing
  `* Core class to access posts via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 75311**: abbreviations - Invalid abbreviation casing
  `* @return bool Result of password check taking into account REST API considerations.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 75328**: abbreviations - Invalid abbreviation casing
  `* Gets the post, if the ID is valid.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 75332**: abbreviations - Invalid abbreviation casing
  `* @param int $id Supplied ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 75333**: abbreviations - Invalid abbreviation casing
  `* @return WP_Post|WP_Error Post object if ID is valid, WP_Error otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 75510**: abbreviations - Invalid abbreviation casing
  `* @param int $featured_media Featured Media ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 75511**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id        Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 75536**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id  Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 75547**: abbreviations - Invalid abbreviation casing
  `* @param int             $post_id The post ID to update the terms form.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 75641**: abbreviations - Invalid abbreviation casing
  `* "Protected: %s" or "Private: %s", as the REST API communicates the status of a post`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 75675**: abbreviations - Invalid abbreviation casing
  `* Retrieves the post's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 75722**: abbreviations - Invalid abbreviation casing
  `* Core controller used to access attachments via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 75799**: abbreviations - Invalid abbreviation casing
  `* @param int $featured_media Featured Media ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 75800**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id        Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 75898**: abbreviations - Invalid abbreviation casing
  `* Retrieves the attachment's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76016**: abbreviations - Invalid abbreviation casing
  `* Core class used to access revisions via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76052**: abbreviations - Invalid abbreviation casing
  `* Get the parent post, if the ID is valid.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76056**: abbreviations - Invalid abbreviation casing
  `* @param int $parent_post_id Supplied ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76057**: abbreviations - Invalid abbreviation casing
  `* @return WP_Post|WP_Error Post object if ID is valid, WP_Error otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76074**: abbreviations - Invalid abbreviation casing
  `* Get the revision, if the ID is valid.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76078**: abbreviations - Invalid abbreviation casing
  `* @param int $id Supplied ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76079**: abbreviations - Invalid abbreviation casing
  `* @return WP_Post|WP_Error Revision post object if ID is valid, WP_Error otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76182**: abbreviations - Invalid abbreviation casing
  `* Retrieves the revision's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76215**: abbreviations - Invalid abbreviation casing
  `* Core class used to access autosaves via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76249**: abbreviations - Invalid abbreviation casing
  `* @param int $parent_id Supplied ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76250**: abbreviations - Invalid abbreviation casing
  `* @return WP_Post|WP_Error Post object if ID is valid, WP_Error otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76292**: abbreviations - Invalid abbreviation casing
  `* Get the autosave, if the ID is valid.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76297**: abbreviations - Invalid abbreviation casing
  `* @return WP_Post|WP_Error Revision post object if ID is valid, WP_Error otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76316**: abbreviations - Invalid abbreviation casing
  `* Retrieves the autosave's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76335**: abbreviations - Invalid abbreviation casing
  `* @return mixed The autosave revision ID or WP_Error.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76380**: abbreviations - Invalid abbreviation casing
  `* Registers the necessary REST API routes.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76408**: abbreviations - Invalid abbreviation casing
  `* Parse block metadata for a block, and prepare it for an API response.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76443**: abbreviations - Invalid abbreviation casing
  `* Retrieves the theme's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76464**: abbreviations - Invalid abbreviation casing
  `* Core class used to access block pattern categories via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76511**: abbreviations - Invalid abbreviation casing
  `* Prepare a raw block pattern category before it gets output in a REST API response.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76523**: abbreviations - Invalid abbreviation casing
  `* Retrieves the block pattern category schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76534**: abbreviations - Invalid abbreviation casing
  `* Core class used to access block patterns via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76603**: abbreviations - Invalid abbreviation casing
  `* Prepare a raw block pattern before it gets output in a REST API response.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76616**: abbreviations - Invalid abbreviation casing
  `* Retrieves the block pattern schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76645**: abbreviations - Invalid abbreviation casing
  `* Registers the necessary REST API routes, one for each dynamic block.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76681**: abbreviations - Invalid abbreviation casing
  `* Retrieves block's output schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76692**: abbreviations - Invalid abbreviation casing
  `* Core class used to access block types via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76825**: abbreviations - Invalid abbreviation casing
  `* Retrieves the block type' schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76883**: abbreviations - Invalid abbreviation casing
  `* Retrieves the pattern's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76894**: abbreviations - Invalid abbreviation casing
  `* Core controller used to access comments via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76950**: abbreviations - Invalid abbreviation casing
  `* Get the comment, if the ID is valid.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76954**: abbreviations - Invalid abbreviation casing
  `* @param int $id Supplied ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 76955**: abbreviations - Invalid abbreviation casing
  `* @return WP_Comment|WP_Error Comment object if ID is valid, WP_Error otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77106**: abbreviations - Invalid abbreviation casing
  `* Retrieves the comment's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77131**: abbreviations - Invalid abbreviation casing
  `* @param int        $comment_id Comment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77310**: abbreviations - Invalid abbreviation casing
  `* Retrieves the font collection's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77352**: abbreviations - Invalid abbreviation casing
  `* Class to access font faces through the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77407**: abbreviations - Invalid abbreviation casing
  `* @param string          $value   Encoded JSON string of font face settings.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77419**: abbreviations - Invalid abbreviation casing
  `* @param string $value Encoded JSON string of font face settings.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77482**: abbreviations - Invalid abbreviation casing
  `* Retrieves the post's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77522**: abbreviations - Invalid abbreviation casing
  `* Get the parent font family, if the ID is valid.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77526**: abbreviations - Invalid abbreviation casing
  `* @param int $font_family_id Supplied ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77527**: abbreviations - Invalid abbreviation casing
  `* @return WP_Post|WP_Error Post object if ID is valid, WP_Error otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77559**: abbreviations - Invalid abbreviation casing
  `* @param string $value Font face src that is a URL or the key for a $_FILES array item.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77662**: abbreviations - Invalid abbreviation casing
  `* @param string          $value   Encoded JSON string of font family settings.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77674**: abbreviations - Invalid abbreviation casing
  `* @param string $value Encoded JSON string of font family settings.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77715**: abbreviations - Invalid abbreviation casing
  `* Retrieves the post's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77759**: abbreviations - Invalid abbreviation casing
  `* @param int $font_family_id Font family post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77779**: abbreviations - Invalid abbreviation casing
  `* @param int $font_family_id Font family post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77809**: abbreviations - Invalid abbreviation casing
  `* Base Global Styles REST API Controller.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77852**: abbreviations - Invalid abbreviation casing
  `* Get the post, if the ID is valid.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77856**: abbreviations - Invalid abbreviation casing
  `* @param int $id Supplied ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77857**: abbreviations - Invalid abbreviation casing
  `* @return WP_Post|WP_Error Post object if ID is valid, WP_Error otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77925**: abbreviations - Invalid abbreviation casing
  `* @since 6.3.0 Adds revisions count and rest URL href to version-history.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77927**: abbreviations - Invalid abbreviation casing
  `* @param integer $id ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 77958**: abbreviations - Invalid abbreviation casing
  `* Retrieves the global styles type' schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78033**: abbreviations - Invalid abbreviation casing
  `* Core class used to access global styles revisions via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78076**: abbreviations - Invalid abbreviation casing
  `* Returns decoded JSON from post content string,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78081**: abbreviations - Invalid abbreviation casing
  `* @param string $raw_json Encoded JSON from global styles custom post content.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78115**: abbreviations - Invalid abbreviation casing
  `* Retrieves the revision's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78138**: abbreviations - Invalid abbreviation casing
  `* Core class to access nav items via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78147**: abbreviations - Invalid abbreviation casing
  `* Gets the nav menu item, if the ID is valid.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78151**: abbreviations - Invalid abbreviation casing
  `* @param int $id Supplied ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78152**: abbreviations - Invalid abbreviation casing
  `* @return object|WP_Error Post object if ID is valid, WP_Error otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78182**: abbreviations - Invalid abbreviation casing
  `* This allows for any user that can `edit_theme_options` or edit any REST API available post type.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78271**: abbreviations - Invalid abbreviation casing
  `* Retrieves the nav menu item's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78316**: abbreviations - Invalid abbreviation casing
  `* Core class used to access menu locations via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78421**: abbreviations - Invalid abbreviation casing
  `* Retrieves the menu location's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78442**: abbreviations - Invalid abbreviation casing
  `* Core class used to managed terms associated with a taxonomy via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78541**: abbreviations - Invalid abbreviation casing
  `* Get the term, if the ID is valid.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78545**: abbreviations - Invalid abbreviation casing
  `* @param int $id Supplied ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78546**: abbreviations - Invalid abbreviation casing
  `* @return WP_Term|WP_Error Term object if ID is valid, WP_Error otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78674**: abbreviations - Invalid abbreviation casing
  `* Retrieves the term's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78706**: abbreviations - Invalid abbreviation casing
  `* Core class used to managed menu terms associated via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78737**: abbreviations - Invalid abbreviation casing
  `* Gets the term, if the ID is valid.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78741**: abbreviations - Invalid abbreviation casing
  `* @param int $id Supplied ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78742**: abbreviations - Invalid abbreviation casing
  `* @return WP_Term|WP_Error Term object if ID is valid, WP_Error otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78750**: abbreviations - Invalid abbreviation casing
  `* This allows for any user that can `edit_theme_options` or edit any REST API available post type.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78874**: abbreviations - Invalid abbreviation casing
  `* Retrieves the term's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78930**: abbreviations - Invalid abbreviation casing
  `* Retrieves the fallbacks' schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78956**: abbreviations - Invalid abbreviation casing
  `* functionality, but is desired for privacy. It prevents api.wordpress.org from knowing the user's IP address.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 78973**: abbreviations - Invalid abbreviation casing
  `* Registers the necessary REST API routes.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 79005**: abbreviations - Invalid abbreviation casing
  `* Prepare a raw block pattern before it gets output in a REST API response.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 79018**: abbreviations - Invalid abbreviation casing
  `* Retrieves the block pattern's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 79058**: abbreviations - Invalid abbreviation casing
  `* Core class to access plugins via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 79339**: abbreviations - Invalid abbreviation casing
  `* Retrieves the plugin's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 79360**: abbreviations - Invalid abbreviation casing
  `* Core class used to access post statuses via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 79455**: abbreviations - Invalid abbreviation casing
  `* Retrieves the post status' schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 79476**: abbreviations - Invalid abbreviation casing
  `* Core class to access post types via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 79560**: abbreviations - Invalid abbreviation casing
  `* Retrieves the post type's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 79584**: abbreviations - Invalid abbreviation casing
  `* Core class to search through all WordPress content via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 79593**: abbreviations - Invalid abbreviation casing
  `* ID property name.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 79601**: abbreviations - Invalid abbreviation casing
  `* URL property name.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 79673**: abbreviations - Invalid abbreviation casing
  `* @param int|string      $item    ID of the item to prepare.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 79681**: abbreviations - Invalid abbreviation casing
  `* Retrieves the item schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 79726**: abbreviations - Invalid abbreviation casing
  `* Core class used to manage a site's settings via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 79798**: abbreviations - Invalid abbreviation casing
  `* Retrieves all of the registered options for the Settings API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 79808**: abbreviations - Invalid abbreviation casing
  `* Retrieves the site setting schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 79839**: abbreviations - Invalid abbreviation casing
  `* registered items, as the REST API will allow additional properties by`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 79976**: abbreviations - Invalid abbreviation casing
  `* @param string|int $id ID of the sidebar.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80020**: abbreviations - Invalid abbreviation casing
  `* Retrieves the block type' schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80050**: abbreviations - Invalid abbreviation casing
  `* Registers API routes.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80082**: abbreviations - Invalid abbreviation casing
  `* Checks that the site can reach the WordPress.org API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80144**: abbreviations - Invalid abbreviation casing
  `* The {@see WP_Site_Health} class is defined in WP-Admin, while the REST API operates in a front-end context.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80164**: abbreviations - Invalid abbreviation casing
  `* Core class used to manage taxonomies via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80259**: abbreviations - Invalid abbreviation casing
  `* Retrieves the taxonomy's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80282**: abbreviations - Invalid abbreviation casing
  `* Core class used to access template autosaves via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80323**: abbreviations - Invalid abbreviation casing
  `* Gets the autosave, if the ID is valid.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80328**: abbreviations - Invalid abbreviation casing
  `* @return WP_Post|WP_Error Autosave post object if ID is valid, WP_Error otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80338**: abbreviations - Invalid abbreviation casing
  `* @param int $parent_id Supplied ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80339**: abbreviations - Invalid abbreviation casing
  `* @return WP_Post|WP_Error Post object if ID is valid, WP_Error otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80356**: abbreviations - Invalid abbreviation casing
  `* Retrieves the autosave's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80367**: abbreviations - Invalid abbreviation casing
  `* Core class used to access template revisions via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80396**: abbreviations - Invalid abbreviation casing
  `* Gets the parent post, if the template ID is valid.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80400**: abbreviations - Invalid abbreviation casing
  `* @param string $parent_template_id Supplied ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80401**: abbreviations - Invalid abbreviation casing
  `* @return WP_Post|WP_Error Post object if ID is valid, WP_Error otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80441**: abbreviations - Invalid abbreviation casing
  `* Retrieves the item's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80452**: abbreviations - Invalid abbreviation casing
  `* Base Templates REST API Controller.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80516**: abbreviations - Invalid abbreviation casing
  `* on the template ID format {theme_name}//{template_slug} and the fact that`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80522**: abbreviations - Invalid abbreviation casing
  `* @param string $id Template ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80523**: abbreviations - Invalid abbreviation casing
  `* @return string Sanitized template ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80670**: abbreviations - Invalid abbreviation casing
  `* @param integer $id ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80698**: abbreviations - Invalid abbreviation casing
  `* Retrieves the block type' schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80710**: abbreviations - Invalid abbreviation casing
  `* Core class used to manage themes via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80844**: abbreviations - Invalid abbreviation casing
  `* Prepares the theme support value for inclusion in the REST API response.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80858**: abbreviations - Invalid abbreviation casing
  `* Retrieves the theme's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80911**: abbreviations - Invalid abbreviation casing
  `* Registers the necessary REST API routes.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80919**: abbreviations - Invalid abbreviation casing
  `* Retrieves the item's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 80951**: abbreviations - Invalid abbreviation casing
  `* Core class used to manage users via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81030**: abbreviations - Invalid abbreviation casing
  `* Get the user, if the ID is valid.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81034**: abbreviations - Invalid abbreviation casing
  `* @param int $id Supplied ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81035**: abbreviations - Invalid abbreviation casing
  `* @return WP_User|WP_Error True if ID is valid, WP_Error otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81225**: abbreviations - Invalid abbreviation casing
  `* @param int   $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81234**: abbreviations - Invalid abbreviation casing
  `* Check a username for the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81249**: abbreviations - Invalid abbreviation casing
  `* Check a user password for the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81269**: abbreviations - Invalid abbreviation casing
  `* Retrieves the user's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81290**: abbreviations - Invalid abbreviation casing
  `* Core class to access widget types via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81419**: abbreviations - Invalid abbreviation casing
  `* Retrieves the widget type's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81434**: abbreviations - Invalid abbreviation casing
  `* - id:        A widget type ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81455**: abbreviations - Invalid abbreviation casing
  `* Renders a single Legacy Widget and wraps it in a JSON-encodable array.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81478**: abbreviations - Invalid abbreviation casing
  `* Core class to access widgets via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81554**: abbreviations - Invalid abbreviation casing
  `* @param string $sidebar_id The sidebar ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81672**: abbreviations - Invalid abbreviation casing
  `* @param string          $sidebar_id ID of the sidebar the widget belongs to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81673**: abbreviations - Invalid abbreviation casing
  `* @return string|WP_Error The saved widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81715**: abbreviations - Invalid abbreviation casing
  `* Retrieves the widget's schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81726**: abbreviations - Invalid abbreviation casing
  `* Core class to manage meta values for an object via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81776**: abbreviations - Invalid abbreviation casing
  `* @param int             $object_id Object ID to fetch meta for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81788**: abbreviations - Invalid abbreviation casing
  `* type before passing back to JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81806**: abbreviations - Invalid abbreviation casing
  `* @param int   $object_id Object ID to fetch meta for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81817**: abbreviations - Invalid abbreviation casing
  `* @param int    $object_id Object ID the field belongs to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81819**: abbreviations - Invalid abbreviation casing
  `* @param string $name      Name for the field that is exposed in the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81833**: abbreviations - Invalid abbreviation casing
  `* @param int    $object_id Object ID to update.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81835**: abbreviations - Invalid abbreviation casing
  `* @param string $name      Name for the field that is exposed in the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81848**: abbreviations - Invalid abbreviation casing
  `* @param int    $object_id Object ID to update.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81850**: abbreviations - Invalid abbreviation casing
  `* @param string $name      Name for the field that is exposed in the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81882**: abbreviations - Invalid abbreviation casing
  `* Retrieves the object's meta schema, conforming to JSON Schema.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81925**: abbreviations - Invalid abbreviation casing
  `* registered items, as the REST API will allow additional properties by`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81950**: abbreviations - Invalid abbreviation casing
  `* Core class to manage comment meta via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 81990**: abbreviations - Invalid abbreviation casing
  `* Core class used to manage meta values for posts via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82049**: abbreviations - Invalid abbreviation casing
  `* Core class used to manage meta values for terms via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82106**: abbreviations - Invalid abbreviation casing
  `* Core class used to manage meta values for users via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82146**: abbreviations - Invalid abbreviation casing
  `* Core base class representing a search handler for an object type in the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82207**: abbreviations - Invalid abbreviation casing
  `* Prepares the search result for a given ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82212**: abbreviations - Invalid abbreviation casing
  `* @param int|string $id     Item ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82218**: abbreviations - Invalid abbreviation casing
  `* Prepares links for the search result of a given ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82223**: abbreviations - Invalid abbreviation casing
  `* @param int|string $id Item ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82229**: abbreviations - Invalid abbreviation casing
  `* Core class representing a search handler for post formats in the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82270**: abbreviations - Invalid abbreviation casing
  `* @param string $id     Item ID, the post format slug.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82277**: abbreviations - Invalid abbreviation casing
  `*     @type string $url   Optional. Post format permalink URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82295**: abbreviations - Invalid abbreviation casing
  `* @param string $id Item ID, the post format slug.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82303**: abbreviations - Invalid abbreviation casing
  `* Core class representing a search handler for posts in the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82340**: abbreviations - Invalid abbreviation casing
  `* Prepares the search result for a given post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82344**: abbreviations - Invalid abbreviation casing
  `* @param int   $id     Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82349**: abbreviations - Invalid abbreviation casing
  `*     @type int    $id    Optional. Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82351**: abbreviations - Invalid abbreviation casing
  `*     @type string $url   Optional. Post permalink URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82365**: abbreviations - Invalid abbreviation casing
  `* Prepares links for the search result of a given ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82369**: abbreviations - Invalid abbreviation casing
  `* @param int $id Item ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82379**: abbreviations - Invalid abbreviation casing
  `* "Protected: %s" or "Private: %s", as the REST API communicates the status of a post`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82404**: abbreviations - Invalid abbreviation casing
  `* Core class representing a search handler for terms in the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82443**: abbreviations - Invalid abbreviation casing
  `* Prepares the search result for a given term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82447**: abbreviations - Invalid abbreviation casing
  `* @param int   $id     Term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82452**: abbreviations - Invalid abbreviation casing
  `*     @type int    $id    Optional. Term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82454**: abbreviations - Invalid abbreviation casing
  `*     @type string $url   Optional. Term permalink URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82468**: abbreviations - Invalid abbreviation casing
  `* Prepares links for the search result of a given ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82472**: abbreviations - Invalid abbreviation casing
  `* @param int $id Item ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82516**: abbreviations - Invalid abbreviation casing
  `* Builds the URL for the sitemap index.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82522**: abbreviations - Invalid abbreviation casing
  `* @return string The sitemap index URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82555**: abbreviations - Invalid abbreviation casing
  `* Gets a URL list for a sitemap.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82561**: abbreviations - Invalid abbreviation casing
  `* @return array[] Array of URL information for a sitemap.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82596**: abbreviations - Invalid abbreviation casing
  `* Gets the URL of a sitemap entry.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82604**: abbreviations - Invalid abbreviation casing
  `* @return string The composed URL for a sitemap entry.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82695**: abbreviations - Invalid abbreviation casing
  `* Gets the URL for the sitemap stylesheet.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82701**: abbreviations - Invalid abbreviation casing
  `* @return string The sitemap stylesheet URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82707**: abbreviations - Invalid abbreviation casing
  `* Gets the URL for the sitemap index stylesheet.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82713**: abbreviations - Invalid abbreviation casing
  `* @return string The sitemap index stylesheet URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82896**: abbreviations - Invalid abbreviation casing
  `* Redirects a URL to the wp-sitemap.xml`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82948**: abbreviations - Invalid abbreviation casing
  `* Gets a URL list for a post type sitemap.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 82957**: abbreviations - Invalid abbreviation casing
  `* @return array[] Array of URL information for a sitemap.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 83014**: abbreviations - Invalid abbreviation casing
  `* Gets a URL list for a taxonomy sitemap.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 83022**: abbreviations - Invalid abbreviation casing
  `* @return array[] Array of URL information for a sitemap.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 83068**: abbreviations - Invalid abbreviation casing
  `* Gets a URL list for a user sitemap.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 83076**: abbreviations - Invalid abbreviation casing
  `* @return array[] Array of URL information for a sitemap.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 83560**: abbreviations - Invalid abbreviation casing
  `* The Style Engine aims to provide a consistent API for rendering styling for blocks`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 83566**: abbreviations - Invalid abbreviation casing
  `* (plugins and/or themes). This is a low-level API that may need to do breaking changes.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 83887**: abbreviations - Invalid abbreviation casing
  `* @param string $widget_id Widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 83968**: abbreviations - Invalid abbreviation casing
  `* @since 4.2.0 Creates a unique HTML ID for the `<select>` element`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 84219**: abbreviations - Invalid abbreviation casing
  `* @param string $id_base         Base ID for the widget, lowercase and unique.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 84270**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $attachment Attachment post ID or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 84283**: abbreviations - Invalid abbreviation casing
  `* @link https://developer.mozilla.org/en-US/docs/Web/API/DOMTokenList`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 84849**: abbreviations - Invalid abbreviation casing
  `* @since 5.4.0 Creates a unique HTML ID for the `<ul>` element`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 85365**: abbreviations - Invalid abbreviation casing
  `* Handles the Heartbeat API in the no-privilege context via AJAX .`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 85990**: abbreviations - Invalid abbreviation casing
  `* Handles the Heartbeat API via AJAX.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86291**: abbreviations - Invalid abbreviation casing
  `* Handles renewing the REST API nonce via AJAX.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86321**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error Value 0 or WP_Error on failure. The link ID on success.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86332**: abbreviations - Invalid abbreviation casing
  `* @param int $link_id Optional. ID of the link to edit. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86333**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error Value 0 or WP_Error on failure. The link ID on success.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86358**: abbreviations - Invalid abbreviation casing
  `* @param int $link_id ID of the link to delete.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86369**: abbreviations - Invalid abbreviation casing
  `* @param int $link_id Link ID to look up.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86377**: abbreviations - Invalid abbreviation casing
  `* Retrieves link data based on its ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86381**: abbreviations - Invalid abbreviation casing
  `* @param int|stdClass $link Link ID or object to retrieve.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86400**: abbreviations - Invalid abbreviation casing
  `*     @type int    $link_id          Optional. The ID of the existing link if updating.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86401**: abbreviations - Invalid abbreviation casing
  `*     @type string $link_url         The URL the link points to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86403**: abbreviations - Invalid abbreviation casing
  `*     @type string $link_image       Optional. A URL of an image.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86407**: abbreviations - Invalid abbreviation casing
  `*     @type int    $link_owner       Optional. A user ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86411**: abbreviations - Invalid abbreviation casing
  `*     @type string $link_rss         Optional. A URL of an associated RSS feed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86412**: abbreviations - Invalid abbreviation casing
  `*     @type int    $link_category    Optional. The term ID of the link category.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86416**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error Value 0 or WP_Error on failure. The link ID on success.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86442**: abbreviations - Invalid abbreviation casing
  `* @param int   $link_id         ID of the link to update.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86454**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error Value 0 or WP_Error on failure. The updated link ID on success.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86530**: abbreviations - Invalid abbreviation casing
  `* @return string|null Comment post ID on success.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86549**: abbreviations - Invalid abbreviation casing
  `* Returns a WP_Comment object based on comment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86553**: abbreviations - Invalid abbreviation casing
  `* @param int $id ID of comment to retrieve.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86567**: abbreviations - Invalid abbreviation casing
  `* @param int|int[] $post_id Either a single Post ID or an array of Post IDs`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86624**: abbreviations - Invalid abbreviation casing
  `* @param string $profiles      URL to the contributor's WordPress.org profile page.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86656**: abbreviations - Invalid abbreviation casing
  `* @param array  $credits The credits groups returned from the API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86685**: abbreviations - Invalid abbreviation casing
  `* @param string   $widget_id        Widget ID  (used in the 'id' attribute for the widget).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86845**: abbreviations - Invalid abbreviation casing
  `* @param string   $widget_id  The widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86861**: abbreviations - Invalid abbreviation casing
  `* @param int|false $widget_control_id Optional. Registered widget ID. Default false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 86918**: abbreviations - Invalid abbreviation casing
  `* @param string $widget_id Widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 87113**: abbreviations - Invalid abbreviation casing
  `* @param int $current_cat     Optional. ID of the current category. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 87114**: abbreviations - Invalid abbreviation casing
  `* @param int $current_parent  Optional. Current parent category ID. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 87115**: abbreviations - Invalid abbreviation casing
  `* @param int $category_parent Optional. Parent ID to retrieve categories for. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 87201**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 87214**: abbreviations - Invalid abbreviation casing
  `* @param int  $user_id       User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 87240**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id User ID to not retrieve posts from.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 87255**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 87267**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 87405**: abbreviations - Invalid abbreviation casing
  `* Handles retrieving the insert-from-URL form for an image.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 87416**: abbreviations - Invalid abbreviation casing
  `* Handles retrieving the insert-from-URL form for an audio file.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 87427**: abbreviations - Invalid abbreviation casing
  `* Handles retrieving the insert-from-URL form for a video file.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 87438**: abbreviations - Invalid abbreviation casing
  `* Handles retrieving the insert-from-URL form for a generic file.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 87552**: abbreviations - Invalid abbreviation casing
  `* @param mixed $file Filename of the original image, Or attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 87723**: abbreviations - Invalid abbreviation casing
  `* @param string $rss  The RSS feed URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 87762**: abbreviations - Invalid abbreviation casing
  `* @param string   $icon_url   Optional. The URL to the icon to be used for this menu.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 87788**: abbreviations - Invalid abbreviation casing
  `* @param string   $icon_url   Optional. The URL to the icon to be used for this menu.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 87869**: abbreviations - Invalid abbreviation casing
  `*                              'attachment'. Accepts false (all) or a specific author ID. Default false (all).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88075**: abbreviations - Invalid abbreviation casing
  `*     @type string $url  URL of the newly-uploaded file.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88178**: abbreviations - Invalid abbreviation casing
  `* Downloads a URL to a local temporary file using the WordPress HTTP API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88186**: abbreviations - Invalid abbreviation casing
  `* @param string $url                    The URL of the file to download.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88425**: abbreviations - Invalid abbreviation casing
  `* @param string        $form_post                    The URL to post the form to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88493**: abbreviations - Invalid abbreviation casing
  `* @param int          $post_id Attachment post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88507**: abbreviations - Invalid abbreviation casing
  `* @param int             $attachment_id The image's attachment post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88523**: abbreviations - Invalid abbreviation casing
  `* @param int             $post_id   Attachment post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88627**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Attachment post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88638**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Attachment post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88650**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Attachment post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88661**: abbreviations - Invalid abbreviation casing
  `* @param string|int   $src      The source file or Attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88683**: abbreviations - Invalid abbreviation casing
  `* @param int $attachment_id The image attachment post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88696**: abbreviations - Invalid abbreviation casing
  `* @param int $attachment_id The image attachment post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88713**: abbreviations - Invalid abbreviation casing
  `* @param int    $attachment_id The attachment post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88728**: abbreviations - Invalid abbreviation casing
  `* @param int    $attachment_id Attachment ID to process.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88746**: abbreviations - Invalid abbreviation casing
  `* @param int    $attachment_id Attachment ID to process.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88758**: abbreviations - Invalid abbreviation casing
  `* @param int    $parent_attachment_id Parent file Attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88772**: abbreviations - Invalid abbreviation casing
  `* @param int    $attachment_id Attachment ID to process.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88847**: abbreviations - Invalid abbreviation casing
  `* @param int          $attachment_id Attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88858**: abbreviations - Invalid abbreviation casing
  `* Retrieves the path or URL of an attachment's attached file.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88861**: abbreviations - Invalid abbreviation casing
  `* then the URL of the file is returned if `allow_url_fopen` is supported.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88866**: abbreviations - Invalid abbreviation casing
  `* @param int          $attachment_id Attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88869**: abbreviations - Invalid abbreviation casing
  `* @return string|false File path or URL on success, false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88880**: abbreviations - Invalid abbreviation casing
  `* @param int $attachment_id Attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88931**: abbreviations - Invalid abbreviation casing
  `* Removes attachment based on ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88935**: abbreviations - Invalid abbreviation casing
  `* @param string $id Importer ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 88999**: abbreviations - Invalid abbreviation casing
  `* @param bool             $with_id Whether to set the ID attribute or not.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89042**: abbreviations - Invalid abbreviation casing
  `* @param int          $id      Image attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89046**: abbreviations - Invalid abbreviation casing
  `* @param string       $url     Optional. Image src URL. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89062**: abbreviations - Invalid abbreviation casing
  `* @param int     $id      Image attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89066**: abbreviations - Invalid abbreviation casing
  `* @param string  $url     Image source URL (not used).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89103**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id   The post ID of a post to attach the media item to. Required, but can`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89107**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error ID of the attachment or a WP_Error object on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89119**: abbreviations - Invalid abbreviation casing
  `* @param int      $post_id    Optional. The post ID the media is associated with.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89122**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error The ID of the attachment or a WP_Error on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89155**: abbreviations - Invalid abbreviation casing
  `* Retrieves the upload iframe source URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89162**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89164**: abbreviations - Invalid abbreviation casing
  `* @return string Upload iframe source URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89174**: abbreviations - Invalid abbreviation casing
  `* @return null|array|void Array of error messages keyed by attachment ID, null or void on success.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89190**: abbreviations - Invalid abbreviation casing
  `* Downloads an image from the specified URL, saves it as an attachment, and optionally attaches it to a post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89196**: abbreviations - Invalid abbreviation casing
  `* @since 5.4.0 The original URL of the attachment is stored in the `_source_url``
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89200**: abbreviations - Invalid abbreviation casing
  `* @param string $file        The URL of the image to download.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89201**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id     Optional. The post ID the media is to be associated with.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89203**: abbreviations - Invalid abbreviation casing
  `* @param string $return_type Optional. Accepts 'html' (image tag html) or 'src' (URL),`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89204**: abbreviations - Invalid abbreviation casing
  `*                            or 'id' (attachment ID). Default 'html'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89205**: abbreviations - Invalid abbreviation casing
  `* @return string|int|WP_Error Populated HTML img tag, attachment ID, or attachment source`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89257**: abbreviations - Invalid abbreviation casing
  `* Retrieves HTML for the Link URL buttons with the default link type as specified.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89353**: abbreviations - Invalid abbreviation casing
  `* @param int   $post_id Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89367**: abbreviations - Invalid abbreviation casing
  `* @param int          $attachment_id Attachment ID for modification.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89590**: abbreviations - Invalid abbreviation casing
  `* @param int    $parent_id Attachment parent ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89971**: abbreviations - Invalid abbreviation casing
  `* Returns whether the server supports URL rewriting.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 89980**: abbreviations - Invalid abbreviation casing
  `* @return bool Whether the server supports URL rewriting.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90191**: abbreviations - Invalid abbreviation casing
  `* @param string $rewrite_rule The XML fragment with URL Rewrite rule.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90215**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90258**: abbreviations - Invalid abbreviation casing
  `* @param string $screen_id The screen ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90271**: abbreviations - Invalid abbreviation casing
  `* @param string $screen_id The screen ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90284**: abbreviations - Invalid abbreviation casing
  `* @param string $screen_id The screen ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90303**: abbreviations - Invalid abbreviation casing
  `* Adds the latest Heartbeat and REST API nonce to the Heartbeat response.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90339**: abbreviations - Invalid abbreviation casing
  `* Removes single-use URL parameters and create canonical link based on new URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90341**: abbreviations - Invalid abbreviation casing
  `* Removes specific query string parameters from a URL, create the canonical link,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90342**: abbreviations - Invalid abbreviation casing
  `* put it in the admin header, and change the current URL to match.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90536**: abbreviations - Invalid abbreviation casing
  `* @param int  $blog_id Site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90556**: abbreviations - Invalid abbreviation casing
  `* @param int $id The user ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90598**: abbreviations - Invalid abbreviation casing
  `* @param int $id The ID of the site to display the setting for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90608**: abbreviations - Invalid abbreviation casing
  `* @param int $id The user ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90609**: abbreviations - Invalid abbreviation casing
  `* @return int|false The ID of the refreshed user or false if the user does not exist.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90706**: abbreviations - Invalid abbreviation casing
  `* @param int $network_id The network ID to check.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90749**: abbreviations - Invalid abbreviation casing
  `*     @type int    $blog_id  The site ID. Default is the current site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90751**: abbreviations - Invalid abbreviation casing
  `*     @type string $selected The ID of the selected link.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90850**: abbreviations - Invalid abbreviation casing
  `* @param int|string $nav_menu_selected_id ID, name, or slug of the currently selected menu.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90928**: abbreviations - Invalid abbreviation casing
  `* @param int     $menu_id   The menu ID for which to save this item. Value of 0 makes a draft, orphaned menu item. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90953**: abbreviations - Invalid abbreviation casing
  `* @param int $menu_id Optional. The ID of the menu to format. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90987**: abbreviations - Invalid abbreviation casing
  `* @param int|string $nav_menu_selected_id    ID, slug, or name of the currently-selected menu.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 90995**: abbreviations - Invalid abbreviation casing
  `* If a JSON blob of navigation menu data is in POST data, expand it and inject`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 91110**: abbreviations - Invalid abbreviation casing
  `* Retrieves plugin installer pages from the WordPress.org Plugins API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 91112**: abbreviations - Invalid abbreviation casing
  `* It is possible for a plugin to override the Plugin API result with three`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 91122**: abbreviations - Invalid abbreviation casing
  `* Plugin Installation API entirely. If `$action` is 'query_plugins' or 'plugin_information',`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 91148**: abbreviations - Invalid abbreviation casing
  `* @param string       $action API action to perform: 'query_plugins', 'plugin_information',`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 91151**: abbreviations - Invalid abbreviation casing
  `*     Optional. Array or object of arguments to serialize for the Plugin Info API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 91304**: abbreviations - Invalid abbreviation casing
  `* @param array|object $api  Data about the plugin retrieved from the API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 91310**: abbreviations - Invalid abbreviation casing
  `*     @type string $url     Plugin installation URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 91342**: abbreviations - Invalid abbreviation casing
  `*     An array or object of plugin data. Can be retrieved from the API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 91419**: abbreviations - Invalid abbreviation casing
  `*     @type string $UpdateURI       ID of the plugin for update purposes, should be a URI.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 91655**: abbreviations - Invalid abbreviation casing
  `* @param string $redirect     Optional. URL to redirect to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 91808**: abbreviations - Invalid abbreviation casing
  `* @param string    $icon_url   Optional. The URL to the icon to be used for this menu.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92160**: abbreviations - Invalid abbreviation casing
  `* Gets the URL to access a particular menu page based on the slug it was registered with.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92162**: abbreviations - Invalid abbreviation casing
  `* If the slug hasn't been registered properly, no URL will be returned.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92169**: abbreviations - Invalid abbreviation casing
  `* @param bool   $display   Optional. Whether or not to display the URL. Default true.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92170**: abbreviations - Invalid abbreviation casing
  `* @return string The menu page URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92413**: abbreviations - Invalid abbreviation casing
  `* @param string $redirect Optional. URL to redirect to. Default empty string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92489**: abbreviations - Invalid abbreviation casing
  `* @return int Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92548**: abbreviations - Invalid abbreviation casing
  `* @return int Post ID if post exists, 0 otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92560**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error Post ID on success, WP_Error on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92570**: abbreviations - Invalid abbreviation casing
  `* @return int|void Post ID on success, void on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92576**: abbreviations - Invalid abbreviation casing
  `* Adds post meta data defined in the `$_POST` superglobal for a post with given ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92587**: abbreviations - Invalid abbreviation casing
  `* Deletes post meta data by meta ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92610**: abbreviations - Invalid abbreviation casing
  `* Returns post meta data by meta ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92621**: abbreviations - Invalid abbreviation casing
  `* Returns meta data for the given post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92627**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id A post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92629**: abbreviations - Invalid abbreviation casing
  `*     Array of meta data arrays for the given post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92636**: abbreviations - Invalid abbreviation casing
  `*         @type string $meta_id    Meta ID as a numeric string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92637**: abbreviations - Invalid abbreviation casing
  `*         @type string $post_id    Post ID as a numeric string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92651**: abbreviations - Invalid abbreviation casing
  `* Updates post meta data by meta ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92655**: abbreviations - Invalid abbreviation casing
  `* @param int    $meta_id    Meta ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92669**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92670**: abbreviations - Invalid abbreviation casing
  `* @return void|int|WP_Error Void if nothing fixed. 0 or WP_Error on update failure. The post ID on update success.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92738**: abbreviations - Invalid abbreviation casing
  `* @param string $box_id    Meta box ID (used in the 'id' attribute for the meta box).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92750**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post  Post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92773**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post      Post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92786**: abbreviations - Invalid abbreviation casing
  `* @param int|null         $thumbnail_id Optional. Thumbnail attachment ID. Default null.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92787**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post|null $post         Optional. The post ID or object associated`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92799**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post ID or object of the post to check for editing.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92800**: abbreviations - Invalid abbreviation casing
  `* @return int|false ID of the user with lock. False if the post does not exist, post is not locked,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92811**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post ID or object of the post being edited.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92813**: abbreviations - Invalid abbreviation casing
  `*     Array of the lock time and user ID. False if the post does not exist, or there`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92817**: abbreviations - Invalid abbreviation casing
  `*     @type int $1 The ID of the current user.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92841**: abbreviations - Invalid abbreviation casing
  `* @param array|int $post_data Associative array containing the post data, or integer post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92842**: abbreviations - Invalid abbreviation casing
  `*                             If a numeric post ID is provided, will use the `$_POST` superglobal.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92843**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error The autosave revision ID. WP_Error or 0 on error.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92866**: abbreviations - Invalid abbreviation casing
  `* @return string URL to redirect to show the preview.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92879**: abbreviations - Invalid abbreviation casing
  `* @return mixed The value 0 or WP_Error on failure. The saved post ID on success.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92880**: abbreviations - Invalid abbreviation casing
  `*               The ID can be the draft post_id or the autosave revision post_id.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92890**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Optional. Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 92974**: abbreviations - Invalid abbreviation casing
  `* because it disables the ability to save navigation blocks via REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93007**: abbreviations - Invalid abbreviation casing
  `* @param int $request_id Request ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93019**: abbreviations - Invalid abbreviation casing
  `* @param int $request_id Request ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93020**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error Request ID on success, or a WP_Error on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93059**: abbreviations - Invalid abbreviation casing
  `*             @type string $name   The user-facing name of an item name-value pair, e.g. 'IP Address'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93085**: abbreviations - Invalid abbreviation casing
  `* @param int $request_id The export request ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93095**: abbreviations - Invalid abbreviation casing
  `* @param int $request_id The request ID for this personal data export.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93112**: abbreviations - Invalid abbreviation casing
  `* @param int    $request_id      The request ID for this personal data export.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93139**: abbreviations - Invalid abbreviation casing
  `* @param int    $request_id    The request ID for this personal data erasure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93150**: abbreviations - Invalid abbreviation casing
  `* @param WP_Post|int $post         The post object or post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93151**: abbreviations - Invalid abbreviation casing
  `* @param int         $compare_from The revision ID to compare from.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93152**: abbreviations - Invalid abbreviation casing
  `* @param int         $compare_to   The revision ID to come to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93164**: abbreviations - Invalid abbreviation casing
  `* @param WP_Post|int $post                 The post object or post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93165**: abbreviations - Invalid abbreviation casing
  `* @param int         $selected_revision_id The selected revision ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93166**: abbreviations - Invalid abbreviation casing
  `* @param int         $from                 Optional. The revision ID to compare from.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93183**: abbreviations - Invalid abbreviation casing
  `* Retrieve the SQL for creating database tables.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93189**: abbreviations - Invalid abbreviation casing
  `* @param string $scope   Optional. The tables for which to retrieve SQL. Can be all, global, ms_global, or blog tables. Defaults to all.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93190**: abbreviations - Invalid abbreviation casing
  `* @param int    $blog_id Optional. The site ID for which to retrieve SQL. Default is the current site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93191**: abbreviations - Invalid abbreviation casing
  `* @return string The SQL needed to create the requested tables.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93300**: abbreviations - Invalid abbreviation casing
  `* @param int    $network_id        ID of network to populate.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93321**: abbreviations - Invalid abbreviation casing
  `* @param int   $network_id Network ID to populate meta for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93334**: abbreviations - Invalid abbreviation casing
  `* @param int   $site_id Site ID to populate meta for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93347**: abbreviations - Invalid abbreviation casing
  `* @return string[] The column header labels keyed by column ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93430**: abbreviations - Invalid abbreviation casing
  `* @param int        $category_parent Optional. ID of parent category.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93431**: abbreviations - Invalid abbreviation casing
  `* @return string|null Returns the category ID as a numeric string if the pairing exists, null if not.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93437**: abbreviations - Invalid abbreviation casing
  `* Gets category object for given ID and 'edit' filter context.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93453**: abbreviations - Invalid abbreviation casing
  `* @param int        $category_parent Optional. ID of parent category.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93465**: abbreviations - Invalid abbreviation casing
  `* @param int      $post_id    Optional. The post ID. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93481**: abbreviations - Invalid abbreviation casing
  `*     @type int        $cat_ID               Category ID. A non-zero value updates an existing category.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93487**: abbreviations - Invalid abbreviation casing
  `*     @type int|string $category_parent      Category parent ID. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93490**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error The ID number of the new or updated Category on success. Zero or a WP_Error on failure,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93514**: abbreviations - Invalid abbreviation casing
  `* @return int|false The ID number of the new or updated Category on success. Zero or FALSE on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93527**: abbreviations - Invalid abbreviation casing
  `*               Returns an array of the term ID and the term taxonomy ID if the pairing exists.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93528**: abbreviations - Invalid abbreviation casing
  `*               Returns 0 if term ID 0 is passed to the function.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93558**: abbreviations - Invalid abbreviation casing
  `* Gets comma-separated list of terms available to edit for the given post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93590**: abbreviations - Invalid abbreviation casing
  `* @param int         $descendants_and_self Optional. ID of the category to output along with its descendants.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93611**: abbreviations - Invalid abbreviation casing
  `* @param int          $post_id Optional. Post ID. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93615**: abbreviations - Invalid abbreviation casing
  `*     @type int    $descendants_and_self ID of the category to output along with its descendants.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93666**: abbreviations - Invalid abbreviation casing
  `* @param int $link_id Optional. The link ID. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93781**: abbreviations - Invalid abbreviation casing
  `* @param int         $default_page Optional. The default page ID to be pre-selected. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93782**: abbreviations - Invalid abbreviation casing
  `* @param int         $parent_page  Optional. The parent page ID. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93784**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post         Post ID or WP_Post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93818**: abbreviations - Invalid abbreviation casing
  `* @param string                 $id            Meta box ID (used in the 'id' attribute for the meta box).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93824**: abbreviations - Invalid abbreviation casing
  `*                                              screen ID, WP_Screen object, or array of screen IDs. Default`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93913**: abbreviations - Invalid abbreviation casing
  `* @param string                 $id      Meta box ID (used in the 'id' attribute for the meta box).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93915**: abbreviations - Invalid abbreviation casing
  `*                                        post type, 'link', or 'comment'). Accepts a single screen ID,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93949**: abbreviations - Invalid abbreviation casing
  `* Part of the Settings API. Use this to define new settings sections for an admin page.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 93988**: abbreviations - Invalid abbreviation casing
  `* Part of the Settings API. Use this to define a settings field that will show`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94030**: abbreviations - Invalid abbreviation casing
  `* Part of the Settings API. Use this in a settings page callback function`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94048**: abbreviations - Invalid abbreviation casing
  `* Part of the Settings API. Use this in a settings page to output`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94066**: abbreviations - Invalid abbreviation casing
  `* Part of the Settings API. Use this to show messages to users about settings validation`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94139**: abbreviations - Invalid abbreviation casing
  `* Part of the Settings API. Outputs a div for each error retrieved by`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94143**: abbreviations - Invalid abbreviation casing
  `* Settings API is submitted. Errors should be added during the validation`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94197**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94604**: abbreviations - Invalid abbreviation casing
  `* @param bool $api Optional. Whether try to fetch tags from the WordPress.org API. Defaults to true.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94611**: abbreviations - Invalid abbreviation casing
  `* Retrieves theme installer pages from the WordPress.org Themes API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94613**: abbreviations - Invalid abbreviation casing
  `* It is possible for a theme to override the Themes API result with three`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94623**: abbreviations - Invalid abbreviation casing
  `* Theme API entirely. If `$action` is 'query_themes', 'theme_information', or 'feature_list',`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94647**: abbreviations - Invalid abbreviation casing
  `* @param string       $action API action to perform: Accepts 'query_themes', 'theme_information',`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94650**: abbreviations - Invalid abbreviation casing
  `*     Optional. Array or object of arguments to serialize for the Themes API. Default empty array.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94678**: abbreviations - Invalid abbreviation casing
  `*         @type bool $screenshot_url     Whether to return the URL of the first screenshot. Default false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94683**: abbreviations - Invalid abbreviation casing
  `*         @type bool $theme_url          Whether to return theme's URL. Default false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94797**: abbreviations - Invalid abbreviation casing
  `* @param string $redirect Optional. URL to redirect to. Default empty string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94817**: abbreviations - Invalid abbreviation casing
  `* Retrieve translations from WordPress Translation API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94822**: abbreviations - Invalid abbreviation casing
  `* @param array|object $args Translation API arguments. Optional.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94835**: abbreviations - Invalid abbreviation casing
  `*             @type string   $package      URL to download the translation package.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94858**: abbreviations - Invalid abbreviation casing
  `* Get available translations from the WordPress.org API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94866**: abbreviations - Invalid abbreviation casing
  `*     If the API response results in an error, an empty array will be returned.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94874**: abbreviations - Invalid abbreviation casing
  `*         @type string   $package      URL to download the translation package.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 94900**: abbreviations - Invalid abbreviation casing
  `* @param array[] $languages Array of available languages (populated via the Translation API).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 95075**: abbreviations - Invalid abbreviation casing
  `* @return object|array|false The response from the API on success, false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 95265**: abbreviations - Invalid abbreviation casing
  `*         @type string id        ID of the notice.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 95368**: abbreviations - Invalid abbreviation casing
  `*     @type string $url              The URL of the site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 95369**: abbreviations - Invalid abbreviation casing
  `*     @type int    $user_id          The ID of the site owner.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 95404**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 95432**: abbreviations - Invalid abbreviation casing
  `* @param string $blog_url   Site URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 95433**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id    Administrator's user ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 95975**: abbreviations - Invalid abbreviation casing
  `* to query all tables first and then run the SQL statement creating the table.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 95982**: abbreviations - Invalid abbreviation casing
  `* @param string $create_ddl SQL statement to create table.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96025**: abbreviations - Invalid abbreviation casing
  `* @param string $create_ddl  SQL statement to add column.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96083**: abbreviations - Invalid abbreviation casing
  `* Modifies the database based on specified SQL statements.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96250**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error WP_Error or User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96262**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id Optional. User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96263**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error User ID of the updated user or WP_Error on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96292**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96305**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96317**: abbreviations - Invalid abbreviation casing
  `* If the `$reassign` parameter is not assigned to a user ID, then all posts will`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96318**: abbreviations - Invalid abbreviation casing
  `* be deleted of that user. The action {@see 'delete_user'} that is passed the user ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96320**: abbreviations - Invalid abbreviation casing
  `* The user meta will also be deleted that are for that user ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96326**: abbreviations - Invalid abbreviation casing
  `* @param int $id       User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96327**: abbreviations - Invalid abbreviation casing
  `* @param int $reassign Optional. Reassign posts and links to new User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96338**: abbreviations - Invalid abbreviation casing
  `* @param int $id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96413**: abbreviations - Invalid abbreviation casing
  `*     @type string $success_url The URL the user will be redirected to after approving the application.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96414**: abbreviations - Invalid abbreviation casing
  `*     @type string $reject_url  The URL the user will be redirected to after rejecting the application.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96429**: abbreviations - Invalid abbreviation casing
  `* Validates the redirect URL protocol scheme. The protocol can be anything except `http` and `javascript`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96433**: abbreviations - Invalid abbreviation casing
  `* @param string $url The redirect URL to be validated.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96434**: abbreviations - Invalid abbreviation casing
  `* @return true|WP_Error True if the redirect URL is valid, a WP_Error object otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96469**: abbreviations - Invalid abbreviation casing
  `* @param string $sidebar      Sidebar ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96654**: abbreviations - Invalid abbreviation casing
  `* @return string|false The new URL to redirect to, or false if no redirection is needed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96780**: abbreviations - Invalid abbreviation casing
  `* Registers a new ability using the Abilities API. It requires three steps:`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96847**: abbreviations - Invalid abbreviation casing
  `* and outputs using JSON Schema syntax. They serve two critical purposes: automatic`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96849**: abbreviations - Invalid abbreviation casing
  `* API contracts for developers.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96851**: abbreviations - Invalid abbreviation casing
  `* WordPress implements a validator based on a subset of the JSON Schema Version 4`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96853**: abbreviations - Invalid abbreviation casing
  `* For details on supported JSON Schema properties and syntax, see the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96854**: abbreviations - Invalid abbreviation casing
  `* related WordPress REST API Schema documentation:`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96894**: abbreviations - Invalid abbreviation casing
  `* validating API keys, or verifying system state:`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96900**: abbreviations - Invalid abbreviation casing
  `* ### REST API Integration`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96902**: abbreviations - Invalid abbreviation casing
  `* Abilities can be exposed through the REST API by setting `show_in_rest``
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96909**: abbreviations - Invalid abbreviation casing
  `* This allows abilities to be invoked via HTTP requests to the WordPress REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96936**: abbreviations - Invalid abbreviation casing
  `*     @type array<string, mixed> $input_schema        Optional. JSON Schema definition for validating the ability's input.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96937**: abbreviations - Invalid abbreviation casing
  `*                                                     Must be a valid JSON Schema object defining the structure and`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96939**: abbreviations - Invalid abbreviation casing
  `*                                                     API documentation.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96940**: abbreviations - Invalid abbreviation casing
  `*     @type array<string, mixed> $output_schema       Optional. JSON Schema definition for the ability's output.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96956**: abbreviations - Invalid abbreviation casing
  `*         @type bool                     $show_in_rest Optional. Whether to expose this ability in the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 96971**: abbreviations - Invalid abbreviation casing
  `* Unregisters an ability from the Abilities API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 97386**: abbreviations - Invalid abbreviation casing
  `* @global int      $user_id      The ID of the user being edited. Not to be confused with the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 97387**: abbreviations - Invalid abbreviation casing
  `*                                global $user_ID, which contains the ID of the current user.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 97388**: abbreviations - Invalid abbreviation casing
  `* @global int      $post_id      The ID of the post when editing comments for a single post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 97531**: abbreviations - Invalid abbreviation casing
  `* @param int    $user    Optional. ID of the user to check, defaults to 0 for current user.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 97580**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post|null $post Optional. Post ID or post object. Default is global `$post` object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 97588**: abbreviations - Invalid abbreviation casing
  `* if the author's ID is available.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 97607**: abbreviations - Invalid abbreviation casing
  `* - ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 97633**: abbreviations - Invalid abbreviation casing
  `* @param int|false $user_id Optional. User ID. Defaults to the current post author.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 97646**: abbreviations - Invalid abbreviation casing
  `* @param int|false $user_id Optional. User ID. Defaults to the current post author.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 97663**: abbreviations - Invalid abbreviation casing
  `* @return string An HTML link if the author's URL exists in user meta,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 97727**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL to the author page for the user with the ID provided.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 97733**: abbreviations - Invalid abbreviation casing
  `* @param int    $author_id       Author ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 97735**: abbreviations - Invalid abbreviation casing
  `* @return string The URL to the author's page.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 97754**: abbreviations - Invalid abbreviation casing
  `*                                       'display_name', 'post_count', 'ID', 'meta_value', 'user_login'. Default 'name'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 97763**: abbreviations - Invalid abbreviation casing
  `*     @type string       $feed_image    If not empty, show a link to the author's feed and use this image URL as`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 97829**: abbreviations - Invalid abbreviation casing
  `* Once a source is registered, any block that supports the Block Bindings API can use a value`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 97843**: abbreviations - Invalid abbreviation casing
  `*       // For example, you can use the `$source_args` to look up a value in a custom table or get it from an external API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 98172**: abbreviations - Invalid abbreviation casing
  `* REST API paths that will be preloaded for a given block editor context.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 98233**: abbreviations - Invalid abbreviation casing
  `* The API's format is snake_case, `register_block_pattern()` expects camelCase.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 98238**: abbreviations - Invalid abbreviation casing
  `* @param array $pattern Pattern as returned from the Pattern Directory API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 98249**: abbreviations - Invalid abbreviation casing
  `* @since 6.2.0 Normalize the pattern from the API (snake_case) to the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 98263**: abbreviations - Invalid abbreviation casing
  `* @since 6.2.0 Normalized the pattern from the API (snake_case) to the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 98276**: abbreviations - Invalid abbreviation casing
  `* @since 6.2.0 Normalized the pattern from the API (snake_case) to the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 98875**: abbreviations - Invalid abbreviation casing
  `* @since 6.3.0 Updated preset styles to use Selectors API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 99076**: abbreviations - Invalid abbreviation casing
  `* @param bool|array $settings Optional Theme JSON settings array that overrides any global theme settings.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 99416**: abbreviations - Invalid abbreviation casing
  `*     @type int      $wp_id     Post ID of customized template.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 99451**: abbreviations - Invalid abbreviation casing
  `* Also checks for templates registered via the Template Registration API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 99754**: abbreviations - Invalid abbreviation casing
  `* Gets the URL to a block asset.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 99759**: abbreviations - Invalid abbreviation casing
  `* @return string|false The URL to the block asset or false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 99765**: abbreviations - Invalid abbreviation casing
  `* Finds a script module ID for the selected block metadata field. It detects`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 99768**: abbreviations - Invalid abbreviation casing
  `* automatically generated module ID. It returns unprocessed script module`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 99769**: abbreviations - Invalid abbreviation casing
  `* ID otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 99775**: abbreviations - Invalid abbreviation casing
  `* @param int    $index      Optional. Index of the script module ID to register when multiple`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 99777**: abbreviations - Invalid abbreviation casing
  `* @return string|false Script module ID or false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 99853**: abbreviations - Invalid abbreviation casing
  `* by avoiding multiple reads from the filesystem and parsing JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 99875**: abbreviations - Invalid abbreviation casing
  `* @param string $file_or_folder Path to the JSON file with metadata definition for`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 99877**: abbreviations - Invalid abbreviation casing
  `*                               If providing the path to a JSON file, the filename must end with `block.json`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 99923**: abbreviations - Invalid abbreviation casing
  `*                                         a path to the JSON file with metadata definition for the block,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 99988**: abbreviations - Invalid abbreviation casing
  `* @param int|string|WP_Post|null $post Optional. Post content, post ID, or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 100008**: abbreviations - Invalid abbreviation casing
  `* @param int|string|WP_Post|null $post       Optional. Post content, post ID, or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 100176**: abbreviations - Invalid abbreviation casing
  `* Hooks into the REST API response for the Posts endpoint and adds the first and last inner blocks.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 100242**: abbreviations - Invalid abbreviation casing
  `* The serialized result is a JSON-encoded string, with unicode escape sequence`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 100449**: abbreviations - Invalid abbreviation casing
  `* @param string[]       $allowed_protocols Optional. Array of allowed URL protocols.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 100478**: abbreviations - Invalid abbreviation casing
  `* @param string[]              $allowed_protocols Optional. Array of allowed URL protocols.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 100496**: abbreviations - Invalid abbreviation casing
  `* @param string[]        $allowed_protocols Optional. Array of allowed URL protocols.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 100807**: abbreviations - Invalid abbreviation casing
  `* Strips all HTML from the content of footnotes, and sanitizes the ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 100814**: abbreviations - Invalid abbreviation casing
  `* @param string $footnotes JSON-encoded string of an array containing the content and ID of each footnote.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 100815**: abbreviations - Invalid abbreviation casing
  `* @return string Filtered content without any HTML on the footnote content and with the sanitized ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 100927**: abbreviations - Invalid abbreviation casing
  `* @param string $dropdown_id ID of the dropdown field.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 101072**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Deleted post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 101112**: abbreviations - Invalid abbreviation casing
  `* @param string $dropdown_id ID of the dropdown field.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 101773**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 102145**: abbreviations - Invalid abbreviation casing
  `* Add Interactivity API directives to the navigation-submenu and page-list`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 102419**: abbreviations - Invalid abbreviation casing
  `* @param array $children The children grouped by parent post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 102813**: abbreviations - Invalid abbreviation casing
  `* Any improvements in the word counting, for example with the HTML API`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 103318**: abbreviations - Invalid abbreviation casing
  `* @param  mixed $value Attachment ID of the custom logo or an empty value.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 103711**: abbreviations - Invalid abbreviation casing
  `* @param int|string $index       Index, name, or ID of the dynamic sidebar.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 103824**: abbreviations - Invalid abbreviation casing
  `*                                          String must contain '%id' and '%class' to inherit the category ID and`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 103887**: abbreviations - Invalid abbreviation casing
  `* @param int    $bookmark The bookmark ID to get field.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 103929**: abbreviations - Invalid abbreviation casing
  `*     @type string   $search         Search terms. Will be SQL-formatted with wildcards before and after`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 103981**: abbreviations - Invalid abbreviation casing
  `* @param int    $bookmark_id Bookmark ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 103997**: abbreviations - Invalid abbreviation casing
  `* @param int $bookmark_id Bookmark ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104304**: abbreviations - Invalid abbreviation casing
  `* Switches the internal blog ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104313**: abbreviations - Invalid abbreviation casing
  `* @param int $blog_id Site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104340**: abbreviations - Invalid abbreviation casing
  `* Redirects incoming links to the proper URL based on the site url.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104352**: abbreviations - Invalid abbreviation casing
  `* Will also attempt to find the correct link when a user enters a URL that does`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104353**: abbreviations - Invalid abbreviation casing
  `* not exist based on exact WordPress query. Will instead try to parse the URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104364**: abbreviations - Invalid abbreviation casing
  `* @param string $requested_url Optional. The URL that was requested, used to`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104366**: abbreviations - Invalid abbreviation casing
  `* @param bool   $do_redirect   Optional. Redirect to the new URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104367**: abbreviations - Invalid abbreviation casing
  `* @return string|void The string of the URL, if redirect needed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104373**: abbreviations - Invalid abbreviation casing
  `* Removes arguments from a query string if they are not present in a URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104388**: abbreviations - Invalid abbreviation casing
  `* Strips the #fragment from a URL, if one is present.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104392**: abbreviations - Invalid abbreviation casing
  `* @param string $url The URL to strip.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104393**: abbreviations - Invalid abbreviation casing
  `* @return string The altered URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104399**: abbreviations - Invalid abbreviation casing
  `* Attempts to guess the correct URL for a 404 request based on query vars.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104405**: abbreviations - Invalid abbreviation casing
  `* @return string|false The correct URL if one is found. False on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104428**: abbreviations - Invalid abbreviation casing
  `* This function also accepts an ID of an object to map against if the capability is a meta capability. Meta`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104434**: abbreviations - Invalid abbreviation casing
  `*     map_meta_cap( 'edit_posts', $user->ID );`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104435**: abbreviations - Invalid abbreviation casing
  `*     map_meta_cap( 'edit_post', $user->ID, $post->ID );`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104436**: abbreviations - Invalid abbreviation casing
  `*     map_meta_cap( 'edit_post_meta', $user->ID, $post->ID, $meta_key );`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104456**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104457**: abbreviations - Invalid abbreviation casing
  `* @param mixed  ...$args Optional further parameters, typically starting with an object ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104466**: abbreviations - Invalid abbreviation casing
  `* This function also accepts an ID of an object to check against if the capability is a meta capability. Meta`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104473**: abbreviations - Invalid abbreviation casing
  `*     current_user_can( 'edit_post', $post->ID );`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104474**: abbreviations - Invalid abbreviation casing
  `*     current_user_can( 'edit_post_meta', $post->ID, $meta_key );`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104490**: abbreviations - Invalid abbreviation casing
  `* @param mixed  ...$args    Optional further parameters, typically starting with an object ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104500**: abbreviations - Invalid abbreviation casing
  `* This function also accepts an ID of an object to check against if the capability is a meta capability. Meta`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104509**: abbreviations - Invalid abbreviation casing
  `*     current_user_can_for_site( $site_id, 'edit_post', $post->ID );`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104510**: abbreviations - Invalid abbreviation casing
  `*     current_user_can_for_site( $site_id, 'edit_post_meta', $post->ID, $meta_key );`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104514**: abbreviations - Invalid abbreviation casing
  `* @param int    $site_id    Site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104516**: abbreviations - Invalid abbreviation casing
  `* @param mixed  ...$args    Optional further parameters, typically starting with an object ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104525**: abbreviations - Invalid abbreviation casing
  `* This function also accepts an ID of an object to check against if the capability is a meta capability. Meta`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104532**: abbreviations - Invalid abbreviation casing
  `*     author_can( $post, 'edit_post', $post->ID );`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104533**: abbreviations - Invalid abbreviation casing
  `*     author_can( $post, 'edit_post_meta', $post->ID, $meta_key );`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104539**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post       Post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104541**: abbreviations - Invalid abbreviation casing
  `* @param mixed       ...$args    Optional further parameters, typically starting with an object ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104550**: abbreviations - Invalid abbreviation casing
  `* This function also accepts an ID of an object to check against if the capability is a meta capability. Meta`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104556**: abbreviations - Invalid abbreviation casing
  `*     user_can( $user->ID, 'edit_posts' );`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104557**: abbreviations - Invalid abbreviation casing
  `*     user_can( $user->ID, 'edit_post', $post->ID );`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104558**: abbreviations - Invalid abbreviation casing
  `*     user_can( $user->ID, 'edit_post_meta', $post->ID, $meta_key );`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104564**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_User $user       User ID or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104566**: abbreviations - Invalid abbreviation casing
  `* @param mixed       ...$args    Optional further parameters, typically starting with an object ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104575**: abbreviations - Invalid abbreviation casing
  `* This function also accepts an ID of an object to check against if the capability is a meta capability. Meta`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104581**: abbreviations - Invalid abbreviation casing
  `*     user_can_for_site( $user->ID, $site_id, 'edit_posts' );`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104582**: abbreviations - Invalid abbreviation casing
  `*     user_can_for_site( $user->ID, $site_id, 'edit_post', $post->ID );`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104583**: abbreviations - Invalid abbreviation casing
  `*     user_can_for_site( $user->ID, $site_id, 'edit_post_meta', $post->ID, $meta_key );`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104587**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_User $user       User ID or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104588**: abbreviations - Invalid abbreviation casing
  `* @param int         $site_id    Site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104590**: abbreviations - Invalid abbreviation casing
  `* @param mixed       ...$args    Optional further parameters, typically starting with an object ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104682**: abbreviations - Invalid abbreviation casing
  `* @param int|false $user_id Optional. The ID of a user. Defaults to false, to check the current user.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104695**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id ID of the user to be granted Super Admin privileges.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104710**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id ID of the user Super Admin privileges to be revoked from.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104753**: abbreviations - Invalid abbreviation casing
  `*     @type int       $1 Concerned user ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104754**: abbreviations - Invalid abbreviation casing
  `*     @type mixed  ...$2 Optional second and further parameters, typically object ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104763**: abbreviations - Invalid abbreviation casing
  `* Retrieves category link URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104769**: abbreviations - Invalid abbreviation casing
  `* @param int|object $category Category ID or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104781**: abbreviations - Invalid abbreviation casing
  `* @param int    $category_id Category ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104794**: abbreviations - Invalid abbreviation casing
  `* This tag may be used outside The Loop by passing a post ID as the parameter.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104801**: abbreviations - Invalid abbreviation casing
  `* @param int|false $post_id Optional. The post ID. Defaults to current post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104808**: abbreviations - Invalid abbreviation casing
  `* Retrieves category name based on category ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104812**: abbreviations - Invalid abbreviation casing
  `* @param int $cat_id Category ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104836**: abbreviations - Invalid abbreviation casing
  `* @param int|false $post_id   Optional. ID of the post to retrieve categories for. Defaults to the current post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104852**: abbreviations - Invalid abbreviation casing
  `* As of 2.7, the function can be used anywhere if it is provided a post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104861**: abbreviations - Invalid abbreviation casing
  `* @param int|string|int[]|string[] $category Category ID, name, slug, or array of such`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104878**: abbreviations - Invalid abbreviation casing
  `* @param int|false $post_id   Optional. ID of the post to retrieve categories for. Defaults to the current post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 104888**: abbreviations - Invalid abbreviation casing
  `* @param int $category Optional. Category ID. Defaults to the current category ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105016**: abbreviations - Invalid abbreviation casing
  `*     @type int|int[]    $current_category      ID of category, or array of IDs of categories, that should get the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105029**: abbreviations - Invalid abbreviation casing
  `*     @type string       $feed_image            URL of an image to use for the feed link. Default empty string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105275**: abbreviations - Invalid abbreviation casing
  `* @param int|object $tag Tag ID or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105286**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Post ID or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105301**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id Optional. Post ID. Defaults to the current post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105325**: abbreviations - Invalid abbreviation casing
  `* @param int $tag Optional. Tag ID. Defaults to the current tag ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105337**: abbreviations - Invalid abbreviation casing
  `* @param int  $term       Optional. Term ID. Defaults to the current term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105349**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post     Post ID or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105364**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id  Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105380**: abbreviations - Invalid abbreviation casing
  `* @param int          $term_id  Term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105407**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id  Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105507**: abbreviations - Invalid abbreviation casing
  `* Retrieves category data given a category ID or category object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105512**: abbreviations - Invalid abbreviation casing
  `* If you pass $category an integer of the category ID, then that category will`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105520**: abbreviations - Invalid abbreviation casing
  `* @param int|object $category Category ID or category row object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105535**: abbreviations - Invalid abbreviation casing
  `* Retrieves a category based on URL containing the category slug.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105548**: abbreviations - Invalid abbreviation casing
  `* @param string $category_path URL containing category slugs.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105572**: abbreviations - Invalid abbreviation casing
  `* Retrieves the ID of a category from its name.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105577**: abbreviations - Invalid abbreviation casing
  `* @return int Category ID on success, 0 if the category doesn't exist.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105583**: abbreviations - Invalid abbreviation casing
  `* Retrieves the name of a category from its ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105587**: abbreviations - Invalid abbreviation casing
  `* @param int $cat_id Category ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105596**: abbreviations - Invalid abbreviation casing
  `* You can use either an ID or the category object for both parameters.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105601**: abbreviations - Invalid abbreviation casing
  `* @param int|object $cat1 ID or object to check if this is the parent category.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105630**: abbreviations - Invalid abbreviation casing
  `* @param int    $cat_id  Category ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105658**: abbreviations - Invalid abbreviation casing
  `* Retrieves a post tag by tag ID or tag object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105663**: abbreviations - Invalid abbreviation casing
  `* If you pass $tag an integer of the tag ID, then that tag will be retrieved`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105671**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Term|object $tag    A tag ID or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105683**: abbreviations - Invalid abbreviation casing
  `* Removes the category cache data based on ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105687**: abbreviations - Invalid abbreviation casing
  `* @param int $id Category ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105814**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional. WP_Comment or the ID of the comment for which to retrieve the author.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105827**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional. WP_Comment or the ID of the comment for which to print the author.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105839**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional. WP_Comment or the ID of the comment for which to get the author's email.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105858**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional. WP_Comment or the ID of the comment for which to print the author's email.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105880**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment   Optional. Comment ID or WP_Comment object. Default is the current comment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105901**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment   Optional. Comment ID or WP_Comment object. Default is the current comment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105909**: abbreviations - Invalid abbreviation casing
  `* Retrieves the HTML link to the URL of the author of the current comment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105917**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional. WP_Comment or the ID of the comment for which to get the author's link.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105919**: abbreviations - Invalid abbreviation casing
  `* @return string The comment author name or HTML link for author's URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105925**: abbreviations - Invalid abbreviation casing
  `* Displays the HTML link to the URL of the author of the current comment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105930**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional. WP_Comment or the ID of the comment for which to print the author's link.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105937**: abbreviations - Invalid abbreviation casing
  `* Retrieves the IP address of the author of the current comment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105942**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional. WP_Comment or the ID of the comment for which to get the author's IP address.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105944**: abbreviations - Invalid abbreviation casing
  `* @return string Comment author's IP address, or an empty string if it's not available.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105950**: abbreviations - Invalid abbreviation casing
  `* Displays the IP address of the author of the current comment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105955**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional. WP_Comment or the ID of the comment for which to print the author's IP address.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105962**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL of the author of the current comment, not linked.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105967**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional. WP_Comment or the ID of the comment for which to get the author's URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105969**: abbreviations - Invalid abbreviation casing
  `* @return string Comment author URL, if provided, an empty string otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105975**: abbreviations - Invalid abbreviation casing
  `* Displays the URL of the author of the current comment, not linked.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105980**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional. WP_Comment or the ID of the comment for which to print the author's URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105987**: abbreviations - Invalid abbreviation casing
  `* Retrieves the HTML link of the URL of the author of the current comment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105989**: abbreviations - Invalid abbreviation casing
  `* $link_text parameter is only used if the URL does not exist for the comment`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 105990**: abbreviations - Invalid abbreviation casing
  `* author. If the URL does exist then the URL will be used and the $link_text`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106005**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment   Optional. Comment ID or WP_Comment object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106013**: abbreviations - Invalid abbreviation casing
  `* Displays the HTML link of the URL of the author of the current comment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106024**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment   Optional. Comment ID or WP_Comment object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106038**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment  $comment   Optional. Comment ID or WP_Comment object. Default current comment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106039**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post     $post      Optional. Post ID or WP_Post object. Default current post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106060**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment  $comment_id Optional. Comment ID or WP_Comment object. Default current comment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106061**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post     $post       Optional. Post ID or WP_Post object. Default current post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106074**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional. WP_Comment or ID of the comment for which to get the date.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106088**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id WP_Comment or ID of the comment for which to print the date.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106102**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional. WP_Comment or ID of the comment for which to get the excerpt.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106115**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional. WP_Comment or ID of the comment for which to print the excerpt.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106122**: abbreviations - Invalid abbreviation casing
  `* Retrieves the comment ID of the current comment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106126**: abbreviations - Invalid abbreviation casing
  `* @return string The comment ID as a numeric string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106132**: abbreviations - Invalid abbreviation casing
  `* Displays the comment ID of the current comment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106179**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106201**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106217**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post  $post Optional. Post ID or WP_Post object. Default is the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106231**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post  $post Optional. Post ID or WP_Post object. Default is the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106246**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional. WP_Comment or ID of the comment for which to get the text.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106262**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional. WP_Comment or ID of the comment for which to print the text.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106279**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional. WP_Comment or ID of the comment for which to get the time.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106293**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional. WP_Comment or ID of the comment for which to print the time.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106305**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional. WP_Comment or ID of the comment for which to get the type.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106325**: abbreviations - Invalid abbreviation casing
  `* Retrieves the current post's trackback URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106328**: abbreviations - Invalid abbreviation casing
  `* retrieve the pretty path. If permalinks weren't enabled, the ID of the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106333**: abbreviations - Invalid abbreviation casing
  `* @return string The trackback URL after being filtered.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106339**: abbreviations - Invalid abbreviation casing
  `* Displays the current post's trackback URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106344**: abbreviations - Invalid abbreviation casing
  `* @return void|string Should only be used to echo the trackback URL, use get_trackback_url()`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106372**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default current post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106387**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default current post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106418**: abbreviations - Invalid abbreviation casing
  `* and the post ID respectively.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106451**: abbreviations - Invalid abbreviation casing
  `* Displays the link to the comments for the current post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106478**: abbreviations - Invalid abbreviation casing
  `*                                      to addComment.moveForm(), and appended to the link URL as a hash value.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106493**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post    $post    Optional. Post ID or WP_Post object the comment is going to be displayed on.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106521**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post    $post    Optional. Post ID or WP_Post object the comment is going to be displayed on.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106539**: abbreviations - Invalid abbreviation casing
  `*                              to addComment.moveForm(), and appended to the link URL as a hash value.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106546**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object the comment is going to be displayed on.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106569**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object the comment is going to be displayed on.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106661**: abbreviations - Invalid abbreviation casing
  `* Gets the comment's reply to ID from the $_GET['replytocom'].`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106669**: abbreviations - Invalid abbreviation casing
  `* @return int Comment's reply to ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106701**: abbreviations - Invalid abbreviation casing
  `*     @type int      $page              Page ID to list comments for. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106765**: abbreviations - Invalid abbreviation casing
  `*         @type string $url     Comment author URL field HTML.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106802**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object to generate the form for. Default current post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106862**: abbreviations - Invalid abbreviation casing
  `* @param string $url          Comment author URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106864**: abbreviations - Invalid abbreviation casing
  `* @param string $user_ip      Comment author IP address.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106879**: abbreviations - Invalid abbreviation casing
  `* @param int   $post_id The ID of the post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106884**: abbreviations - Invalid abbreviation casing
  `*     @type int    $post_id Limit results to those affiliated with a given post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 106947**: abbreviations - Invalid abbreviation casing
  `* Retrieves comment data given a comment ID or comment object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107062**: abbreviations - Invalid abbreviation casing
  `*              with the Object Cache API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107112**: abbreviations - Invalid abbreviation casing
  `* @param int    $comment_id Comment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107123**: abbreviations - Invalid abbreviation casing
  `* @return int|false Meta ID on success, false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107141**: abbreviations - Invalid abbreviation casing
  `* @param int    $comment_id Comment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107158**: abbreviations - Invalid abbreviation casing
  `* @param int    $comment_id Comment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107167**: abbreviations - Invalid abbreviation casing
  `*               An empty array if a valid but non-existing comment ID is passed and `$single` is false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107168**: abbreviations - Invalid abbreviation casing
  `*               An empty string if a valid but non-existing comment ID is passed and `$single` is true.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107190**: abbreviations - Invalid abbreviation casing
  `* Updates comment meta field based on comment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107193**: abbreviations - Invalid abbreviation casing
  `* same key and comment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107203**: abbreviations - Invalid abbreviation casing
  `* @param int    $comment_id Comment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107209**: abbreviations - Invalid abbreviation casing
  `* @return int|bool Meta ID if the key didn't exist, true on successful update,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107286**: abbreviations - Invalid abbreviation casing
  `* @param string $ip        Comment author's IP address.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107333**: abbreviations - Invalid abbreviation casing
  `* @param int   $comment_id Comment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107403**: abbreviations - Invalid abbreviation casing
  `* @param string $user_ip    The comment author's IP address.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107453**: abbreviations - Invalid abbreviation casing
  `* post ID available.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107459**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id   Comment ID or WP_Comment object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107474**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Comment ID or WP_Comment object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107485**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Comment ID or WP_Comment object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107496**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Comment ID or WP_Comment object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107507**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Comment ID or WP_Comment object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107514**: abbreviations - Invalid abbreviation casing
  `* Retrieves the status of a comment by comment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107518**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Comment ID or WP_Comment object`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107562**: abbreviations - Invalid abbreviation casing
  `* Gets current commenter's name, email, and URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107576**: abbreviations - Invalid abbreviation casing
  `*     @type string $comment_author_url   The URL address of the current commenter, or an empty string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107618**: abbreviations - Invalid abbreviation casing
  `*     @type string     $comment_author_IP    The IP address of the `$comment_author`. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107619**: abbreviations - Invalid abbreviation casing
  `*     @type string     $comment_author_url   The URL address of the `$comment_author`. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107627**: abbreviations - Invalid abbreviation casing
  `*     @type int        $comment_parent       ID of this comment's parent, if any. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107628**: abbreviations - Invalid abbreviation casing
  `*     @type int        $comment_post_ID      ID of the post that relates to the comment, if any.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107633**: abbreviations - Invalid abbreviation casing
  `*     @type int        $user_id              ID of the user who submitted the comment. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107635**: abbreviations - Invalid abbreviation casing
  `* @return int|false The new comment's ID on success, false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107689**: abbreviations - Invalid abbreviation casing
  `* inserting comment into database. Calls {@see 'comment_post'} action with comment ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107713**: abbreviations - Invalid abbreviation casing
  `*     @type string $comment_author_url   The comment author URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107719**: abbreviations - Invalid abbreviation casing
  `*     @type int    $comment_parent       The ID of this comment's parent, if any. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107720**: abbreviations - Invalid abbreviation casing
  `*     @type int    $comment_post_ID      The ID of the post that relates to the comment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107721**: abbreviations - Invalid abbreviation casing
  `*     @type int    $user_id              The ID of the user who submitted the comment. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107725**: abbreviations - Invalid abbreviation casing
  `*     @type string $comment_author_IP    Comment author IP address in IPv4 format. Default is the value of`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107730**: abbreviations - Invalid abbreviation casing
  `* @return int|false|WP_Error The ID of the comment on success, false or WP_Error on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107755**: abbreviations - Invalid abbreviation casing
  `* @param int $comment_id ID of the comment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107769**: abbreviations - Invalid abbreviation casing
  `* @param int $comment_id Comment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107776**: abbreviations - Invalid abbreviation casing
  `* Send a notification to the post author when a new note is added via the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107795**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id     Comment ID or WP_Comment object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107813**: abbreviations - Invalid abbreviation casing
  `* @since 5.5.0 The return values for an invalid comment or post ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107848**: abbreviations - Invalid abbreviation casing
  `* later date and only updated once per post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107858**: abbreviations - Invalid abbreviation casing
  `* @param int|null $post_id     Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107861**: abbreviations - Invalid abbreviation casing
  `* @return bool|void True on success, false on failure or if post with ID does`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107874**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Post ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107881**: abbreviations - Invalid abbreviation casing
  `* Finds a pingback server URI based on the given URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107889**: abbreviations - Invalid abbreviation casing
  `* @param string $url        URL to ping.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107937**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Post ID or object to do trackbacks on.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107948**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107949**: abbreviations - Invalid abbreviation casing
  `* @return int Same post ID as provided.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107962**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post    Post ID or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107988**: abbreviations - Invalid abbreviation casing
  `* @param string $trackback_url URL to send trackbacks.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 107991**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id       Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 108042**: abbreviations - Invalid abbreviation casing
  `* @param int|array $ids Comment ID or an array of comment IDs to remove from cache.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 108050**: abbreviations - Invalid abbreviation casing
  `* Will add the comments in $comments to the cache. If comment ID already exists`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 108052**: abbreviations - Invalid abbreviation casing
  `* cache using the comment group with the key using the ID of the comments.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 108099**: abbreviations - Invalid abbreviation casing
  `* @param int  $post_id Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 108116**: abbreviations - Invalid abbreviation casing
  `*     @type string|int $comment_post_ID             The ID of the post that relates to the comment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 108119**: abbreviations - Invalid abbreviation casing
  `*     @type string     $url                         The comment author URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 108121**: abbreviations - Invalid abbreviation casing
  `*     @type string|int $comment_parent              The ID of this comment's parent, if any. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 108824**: abbreviations - Invalid abbreviation casing
  `* @global int    $blog_id    The current site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 108881**: abbreviations - Invalid abbreviation casing
  `* @param int $postid Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 108903**: abbreviations - Invalid abbreviation casing
  `* Returns or prints a category ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 108910**: abbreviations - Invalid abbreviation casing
  `* @return int Category ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 109303**: abbreviations - Invalid abbreviation casing
  `function list_cats($optionall = 1, $all = 'All', $sort_column = 'ID', $sort_order = 'asc', $file = '', $list = \true, $optiondates = 0, $optioncount = 0, $hide_empty = 1, $use_desc_for_title = 1, $children = \false, $child_of = 0, $categories = 0, $recurse = 0, $feed = '', $feed_image = '', $exclude = '', $hierarchical = \false)`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 109338**: abbreviations - Invalid abbreviation casing
  `function dropdown_cats($optionall = 1, $all = 'All', $orderby = 'ID', $order = 'asc', $show_last_update = 0, $show_count = 0, $hide_empty = 1, $optionnone = \false, $selected = 0, $exclude = 0)`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 109374**: abbreviations - Invalid abbreviation casing
  `* Sets the categories that the post ID belongs to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 109490**: abbreviations - Invalid abbreviation casing
  `* Gets the links associated with category by ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 109563**: abbreviations - Invalid abbreviation casing
  `* Gets the name of category by ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 109637**: abbreviations - Invalid abbreviation casing
  `* @return int The new user's ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 109657**: abbreviations - Invalid abbreviation casing
  `* @param int $comment_id The ID of the comment`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 109666**: abbreviations - Invalid abbreviation casing
  `* Retrieve the category name by the category ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 109672**: abbreviations - Invalid abbreviation casing
  `* @param int $cat_id Category ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 109685**: abbreviations - Invalid abbreviation casing
  `* @param int    $id      Category ID to retrieve children.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 109686**: abbreviations - Invalid abbreviation casing
  `* @param string $before  Optional. Prepend before category term ID. Default '/'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 109687**: abbreviations - Invalid abbreviation casing
  `* @param string $after   Optional. Append after category term ID. Default empty string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 109936**: abbreviations - Invalid abbreviation casing
  `* @param int $auth_id The ID of the author.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 109943**: abbreviations - Invalid abbreviation casing
  `* Retrieve the URL to the home page of the author of the current post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 109949**: abbreviations - Invalid abbreviation casing
  `* @return string The URL to the author's page.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 109955**: abbreviations - Invalid abbreviation casing
  `* Display the URL to the home page of the author of the current post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 109965**: abbreviations - Invalid abbreviation casing
  `* Retrieve the ID of the author of the current post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 109971**: abbreviations - Invalid abbreviation casing
  `* @return string|int The author's ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 109977**: abbreviations - Invalid abbreviation casing
  `* Display the ID of the author of the current post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110130**: abbreviations - Invalid abbreviation casing
  `* @param int   $id       Optional. Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110140**: abbreviations - Invalid abbreviation casing
  `* Retrieve icon URL and Path.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110146**: abbreviations - Invalid abbreviation casing
  `* @param int  $id       Optional. Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110148**: abbreviations - Invalid abbreviation casing
  `* @return array Icon URL and full path to file, respectively.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110160**: abbreviations - Invalid abbreviation casing
  `* @param int   $id       Optional. Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110175**: abbreviations - Invalid abbreviation casing
  `* @param int   $id       Optional. Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110184**: abbreviations - Invalid abbreviation casing
  `* Retrieves bookmark data based on ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110190**: abbreviations - Invalid abbreviation casing
  `* @param int    $bookmark_id ID of link`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110202**: abbreviations - Invalid abbreviation casing
  `* Checks and cleans a URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110204**: abbreviations - Invalid abbreviation casing
  `* A number of characters are removed from the URL. If the URL is for displaying`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110206**: abbreviations - Invalid abbreviation casing
  `* is applied to the returned cleaned URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110212**: abbreviations - Invalid abbreviation casing
  `* @param string $url The URL to be cleaned.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110214**: abbreviations - Invalid abbreviation casing
  `* @param string $context Optional. How the URL will be used. Default is 'display'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110277**: abbreviations - Invalid abbreviation casing
  `* @param string|int $name            Widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110292**: abbreviations - Invalid abbreviation casing
  `* @param int|string $id Widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110311**: abbreviations - Invalid abbreviation casing
  `* @param int|string $name             Sidebar ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110327**: abbreviations - Invalid abbreviation casing
  `* @param int|string $id Widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110341**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110363**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id User ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110385**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id User ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110405**: abbreviations - Invalid abbreviation casing
  `* @param int $id Site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110406**: abbreviations - Invalid abbreviation casing
  `* @return array List of users that are part of that site ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110431**: abbreviations - Invalid abbreviation casing
  `* @param false|int $user  Optional. User ID to retrieve the field for. Default false (current user).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110502**: abbreviations - Invalid abbreviation casing
  `* @param int $parent ID of parent term under which to confine the exists search.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110503**: abbreviations - Invalid abbreviation casing
  `* @return mixed Get the term ID or term object, if exists.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110562**: abbreviations - Invalid abbreviation casing
  `* @param string $id            Optional. HTML ID attribute value. Default 'content'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110577**: abbreviations - Invalid abbreviation casing
  `* @param array $ids User ID numbers list.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110693**: abbreviations - Invalid abbreviation casing
  `* @param int $blog_id Site ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110889**: abbreviations - Invalid abbreviation casing
  `* @param int $id Page ID to clean`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110916**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id An optional post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110957**: abbreviations - Invalid abbreviation casing
  `* Some functionality requires API to exist, so some PHP version may lose out`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110978**: abbreviations - Invalid abbreviation casing
  `* Retrieve a single post, based on post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 110987**: abbreviations - Invalid abbreviation casing
  `* @param int $postid Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 111111**: abbreviations - Invalid abbreviation casing
  `* Determines if the URL can be accessed over SSL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 111113**: abbreviations - Invalid abbreviation casing
  `* Determines if the URL can be accessed over SSL by using the WordPress HTTP API to access`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 111114**: abbreviations - Invalid abbreviation casing
  `* the URL using https as the scheme.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 111119**: abbreviations - Invalid abbreviation casing
  `* @param string $url The URL to test.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 111222**: abbreviations - Invalid abbreviation casing
  `* Retrieve permalink from post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 111228**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 111244**: abbreviations - Invalid abbreviation casing
  `* @param string      $url       URL to fetch.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 111278**: abbreviations - Invalid abbreviation casing
  `* Determines whether the current URL is within the comments popup window.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 111362**: abbreviations - Invalid abbreviation casing
  `* Sort categories by ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 111440**: abbreviations - Invalid abbreviation casing
  `* Return the user request object for the specified request ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 111446**: abbreviations - Invalid abbreviation casing
  `* @param int $request_id The ID of the user request.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 111487**: abbreviations - Invalid abbreviation casing
  `* @param string $user_ip The comment author's IP address`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 111544**: abbreviations - Invalid abbreviation casing
  `* This should not be used to escape data going directly into an SQL query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 111611**: abbreviations - Invalid abbreviation casing
  `* the full URL as a referrer to other sites when cross-origin assets are loaded.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 111721**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Optional. Attachment ID. Default is the ID of the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 111805**: abbreviations - Invalid abbreviation casing
  `* Filter the SQL clauses of an attachment query to include filenames.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 111821**: abbreviations - Invalid abbreviation casing
  `* If more than one post uses the same title, the post with the smallest ID will be returned.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 111823**: abbreviations - Invalid abbreviation casing
  `* publication date, not the smallest ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112164**: abbreviations - Invalid abbreviation casing
  `* This is not a public API, but rather an internal handler.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112165**: abbreviations - Invalid abbreviation casing
  `* A future public Webfonts API will replace this stopgap code.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112169**: abbreviations - Invalid abbreviation casing
  `*    b. It does not expose API ins or outs for consumption.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112174**: abbreviations - Invalid abbreviation casing
  `*       the Webfonts API is introduced in Core.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112407**: abbreviations - Invalid abbreviation casing
  `* @param int    $blog_id    Site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112409**: abbreviations - Invalid abbreviation casing
  `* @param mixed  ...$args    Optional further parameters, typically starting with an object ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112457**: abbreviations - Invalid abbreviation casing
  `* @param string   $id       An internal ID/name for the handler. Needs to be unique.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112458**: abbreviations - Invalid abbreviation casing
  `* @param string   $regex    The regex that will be used to see if this handler should be used for a URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112473**: abbreviations - Invalid abbreviation casing
  `* @param string $id       The handler ID that should be removed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112493**: abbreviations - Invalid abbreviation casing
  `* @param string $url Optional. The URL that should be embedded. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112509**: abbreviations - Invalid abbreviation casing
  `* Attempts to fetch the embed HTML for a provided URL using oEmbed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112515**: abbreviations - Invalid abbreviation casing
  `* @param string $url  The URL that should be embedded.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112519**: abbreviations - Invalid abbreviation casing
  `*     @type int|string $width    Optional. The `maxwidth` value passed to the provider URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112520**: abbreviations - Invalid abbreviation casing
  `*     @type int|string $height   Optional. The `maxheight` value passed to the provider URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112522**: abbreviations - Invalid abbreviation casing
  `*                                at the given URL for an oEmbed provider when the provider URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112547**: abbreviations - Invalid abbreviation casing
  `* Adds a URL format and oEmbed provider URL pair.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112553**: abbreviations - Invalid abbreviation casing
  `* @param string $format   The format of URL that this provider can handle. You can use asterisks`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112555**: abbreviations - Invalid abbreviation casing
  `* @param string $provider The URL to the oEmbed provider.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112568**: abbreviations - Invalid abbreviation casing
  `* @param string $format The URL format for the oEmbed provider to remove.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112591**: abbreviations - Invalid abbreviation casing
  `* Catches YouTube iframe embed URLs that are not parsable by oEmbed but can be translated into a URL that is.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112600**: abbreviations - Invalid abbreviation casing
  `* @param string $url     The original URL that was matched by the regex.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112614**: abbreviations - Invalid abbreviation casing
  `* @param string $url The original URL that was matched by the regex.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112628**: abbreviations - Invalid abbreviation casing
  `* @param string $url     The original URL that was matched by the regex.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112636**: abbreviations - Invalid abbreviation casing
  `* Registers the oEmbed REST API route.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112691**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL to embed a specific post in an iframe.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112695**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or object. Defaults to the current post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112696**: abbreviations - Invalid abbreviation casing
  `* @return string|false The post embed URL on success, false if the post doesn't exist.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112702**: abbreviations - Invalid abbreviation casing
  `* Retrieves the oEmbed endpoint URL for a given permalink.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112704**: abbreviations - Invalid abbreviation casing
  `* Pass an empty string as the first argument to get the endpoint base URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112710**: abbreviations - Invalid abbreviation casing
  `* @return string The oEmbed endpoint URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112722**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post   Optional. Post ID or object. Default is global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112734**: abbreviations - Invalid abbreviation casing
  `* @param WP_Post|int $post  Post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112743**: abbreviations - Invalid abbreviation casing
  `* Retrieves the oEmbed response data for a given URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112747**: abbreviations - Invalid abbreviation casing
  `* @param string $url  The URL that should be inspected for discovery `<link>` tags.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112749**: abbreviations - Invalid abbreviation casing
  `* @return object|false oEmbed response data if the URL does belong to the current site. False otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112782**: abbreviations - Invalid abbreviation casing
  `* Hooks into the REST API output to print XML instead of JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112784**: abbreviations - Invalid abbreviation casing
  `* This is only done for the oEmbed API endpoint,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112819**: abbreviations - Invalid abbreviation casing
  `* @param string       $url    The URL of the content to be embedded.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112837**: abbreviations - Invalid abbreviation casing
  `* @param string       $url    The URL of the content to be embedded.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112960**: abbreviations - Invalid abbreviation casing
  `* If the URL belongs to the current site, the result is fetched directly instead of`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112966**: abbreviations - Invalid abbreviation casing
  `* @param string      $url    The URL that should be inspected for discovery `<link>` tags.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 112969**: abbreviations - Invalid abbreviation casing
  `*                     Null if the URL does not belong to the current site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 113110**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 113176**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional comment object or ID. Defaults to global comment object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 113186**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional comment object or ID. Defaults to global comment object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 113198**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment Optional. Comment object or ID. Defaults to global comment object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 113388**: abbreviations - Invalid abbreviation casing
  `* Builds SimplePie object based on RSS or Atom feed from URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 113392**: abbreviations - Invalid abbreviation casing
  `* @param string|string[] $url URL of feed to retrieve. If an array of URLs, the feeds are merged`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 113416**: abbreviations - Invalid abbreviation casing
  `*             @type string|string[] $src                     The URL(s) to each resource containing the font data.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 113481**: abbreviations - Invalid abbreviation casing
  `*                                       or a string containing the path or URL to a JSON file containing the font collection.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 113525**: abbreviations - Invalid abbreviation casing
  `* Returns an array containing the current fonts upload directory's path and URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 113534**: abbreviations - Invalid abbreviation casing
  `*     @type string       $url     Base URL and subdirectory or absolute URL to the fonts upload directory.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 113537**: abbreviations - Invalid abbreviation casing
  `*     @type string       $baseurl URL path without subdir.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 113573**: abbreviations - Invalid abbreviation casing
  `* @param int     $post_id Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 113586**: abbreviations - Invalid abbreviation casing
  `* @param int     $post_id Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 113714**: abbreviations - Invalid abbreviation casing
  `* @internal This function will be removed in 4.5.0 per Shortcode API Roadmap.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 114368**: abbreviations - Invalid abbreviation casing
  `* Used for querying the database for a value from URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 114399**: abbreviations - Invalid abbreviation casing
  `* Ensures a string is a valid SQL 'order by' clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 114634**: abbreviations - Invalid abbreviation casing
  `* Navigates through an array, object, or scalar, and encodes the values to be used in a URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 114648**: abbreviations - Invalid abbreviation casing
  `* Navigates through an array, object, or scalar, and raw-encodes the values to be used in a URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 114662**: abbreviations - Invalid abbreviation casing
  `* Navigates through an array, object, or scalar, and decodes URL-encoded values`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 114703**: abbreviations - Invalid abbreviation casing
  `* Callback to convert URL match to HTML A element.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 114711**: abbreviations - Invalid abbreviation casing
  `* @return string HTML A element with URL address.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 114731**: abbreviations - Invalid abbreviation casing
  `* Helper function used to build the "rel" attribute for a URL when creating an anchor using make_clickable().`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 114735**: abbreviations - Invalid abbreviation casing
  `* @param string $url The URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115046**: abbreviations - Invalid abbreviation casing
  `* @param WP_Post|object|int $post Optional. WP_Post instance or Post ID/object. Default null.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115143**: abbreviations - Invalid abbreviation casing
  `* Checks and cleans a URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115145**: abbreviations - Invalid abbreviation casing
  `* A number of characters are removed from the URL. If the URL is for displaying`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115147**: abbreviations - Invalid abbreviation casing
  `* is applied to the returned cleaned URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115150**: abbreviations - Invalid abbreviation casing
  `* @since 6.9.0 Prepends `https://` to the URL if it does not already contain a scheme`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115153**: abbreviations - Invalid abbreviation casing
  `* @param string   $url       The URL to be cleaned.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115157**: abbreviations - Invalid abbreviation casing
  `* @return string The cleaned URL after the {@see 'clean_url'} filter is applied.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115165**: abbreviations - Invalid abbreviation casing
  `* Sanitizes a URL for database or redirect usage.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115171**: abbreviations - Invalid abbreviation casing
  `* @since 6.9.0 Prepends `https://` to the URL if it does not already contain a scheme`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115176**: abbreviations - Invalid abbreviation casing
  `* @param string   $url       The URL to be cleaned.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115179**: abbreviations - Invalid abbreviation casing
  `* @return string The cleaned URL after sanitize_url() is run.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115185**: abbreviations - Invalid abbreviation casing
  `* Sanitizes a URL for database or redirect usage.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115190**: abbreviations - Invalid abbreviation casing
  `* @since 6.9.0 Prepends `https://` to the URL if it does not already contain a scheme`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115195**: abbreviations - Invalid abbreviation casing
  `* @param string   $url       The URL to be cleaned.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115198**: abbreviations - Invalid abbreviation casing
  `* @return string The cleaned URL after esc_url() is run with the 'db' context.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115288**: abbreviations - Invalid abbreviation casing
  `* Converts full URL paths to absolute paths.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115296**: abbreviations - Invalid abbreviation casing
  `* @param string $link Full URL path.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115379**: abbreviations - Invalid abbreviation casing
  `* @param string[]       $allowed_protocols Array of allowed URL protocols.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115435**: abbreviations - Invalid abbreviation casing
  `* Adds a base URL to relative links in passed content.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115445**: abbreviations - Invalid abbreviation casing
  `* @param string   $base    The base URL to prefix to links.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115453**: abbreviations - Invalid abbreviation casing
  `* Callback to add a base URL to relative links in passed content.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115628**: abbreviations - Invalid abbreviation casing
  `* This should be used when preparing data for core API that expects slashed data.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115629**: abbreviations - Invalid abbreviation casing
  `* This should not be used to escape data going directly into an SQL query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115645**: abbreviations - Invalid abbreviation casing
  `* This should be used to remove slashes from data passed to core API that`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115660**: abbreviations - Invalid abbreviation casing
  `* Extracts and returns the first URL from passed content.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115665**: abbreviations - Invalid abbreviation casing
  `* @return string|false Database-escaped URL via {@see esc_url()} if found, otherwise `false`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115764**: abbreviations - Invalid abbreviation casing
  `* Shortens a URL, to be used as link text.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115769**: abbreviations - Invalid abbreviation casing
  `* @param string $url    URL to shorten.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115770**: abbreviations - Invalid abbreviation casing
  `* @param int    $length Optional. Maximum length of the shortened URL. Default 35 characters.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 115771**: abbreviations - Invalid abbreviation casing
  `* @return string Shortened URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116171**: abbreviations - Invalid abbreviation casing
  `*              updated to accept a post ID or a WP_Post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116178**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post    Post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116185**: abbreviations - Invalid abbreviation casing
  `* Retrieves HTTP Headers from URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116189**: abbreviations - Invalid abbreviation casing
  `* @param string $url        URL to retrieve HTTP headers from.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116216**: abbreviations - Invalid abbreviation casing
  `* Builds a URL query based on an associative or indexed array.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116218**: abbreviations - Invalid abbreviation casing
  `* This is a convenient function for easily building URL queries.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116227**: abbreviations - Invalid abbreviation casing
  `* @param array $data URL-encode key/value pairs.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116228**: abbreviations - Invalid abbreviation casing
  `* @return string URL-encoded string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116254**: abbreviations - Invalid abbreviation casing
  `* Retrieves a modified URL query string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116256**: abbreviations - Invalid abbreviation casing
  `* You can rebuild the URL and append query variables to the URL query by using this function.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116270**: abbreviations - Invalid abbreviation casing
  `* Omitting the URL from either use results in the current URL being used`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116286**: abbreviations - Invalid abbreviation casing
  `* @param string       $value Optional. Either a query variable value, or a URL to act upon.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116287**: abbreviations - Invalid abbreviation casing
  `* @param string       $url   Optional. A URL to act upon.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116288**: abbreviations - Invalid abbreviation casing
  `* @return string New URL query string (unescaped).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116303**: abbreviations - Invalid abbreviation casing
  `* @param false|string    $query Optional. When false uses the current URL. Default false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116304**: abbreviations - Invalid abbreviation casing
  `* @return string New URL query string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116310**: abbreviations - Invalid abbreviation casing
  `* Returns an array of single-use query variable names that can be removed from a URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116314**: abbreviations - Invalid abbreviation casing
  `* @return string[] An array of query variable names to remove from the URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116338**: abbreviations - Invalid abbreviation casing
  `* @param string $uri URI/URL of web page to retrieve.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116560**: abbreviations - Invalid abbreviation casing
  `* Retrieves URL with nonce added to URL query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116564**: abbreviations - Invalid abbreviation casing
  `* @param string     $actionurl URL to add nonce action.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116567**: abbreviations - Invalid abbreviation casing
  `* @return string Escaped URL with nonce action added.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116637**: abbreviations - Invalid abbreviation casing
  `* If it's the same as the current request URL, will return false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116641**: abbreviations - Invalid abbreviation casing
  `* @return string|false Referer URL on success, false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116647**: abbreviations - Invalid abbreviation casing
  `* Retrieves unvalidated referer from the '_wp_http_referer' URL query variable or the HTTP referer.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116649**: abbreviations - Invalid abbreviation casing
  `* If the value of the '_wp_http_referer' URL query variable is not a string then it will be ignored.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116655**: abbreviations - Invalid abbreviation casing
  `* @return string|false Referer URL on success, false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116665**: abbreviations - Invalid abbreviation casing
  `* @return string|false Original referer URL on success, false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116801**: abbreviations - Invalid abbreviation casing
  `* Returns an array containing the current upload directory's path and URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116808**: abbreviations - Invalid abbreviation casing
  `* The upload URL path is set either by the 'upload_url_path' option or by using`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116830**: abbreviations - Invalid abbreviation casing
  `*     @type string       $url     Base URL and subdirectory or absolute URL to upload directory.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116833**: abbreviations - Invalid abbreviation casing
  `*     @type string       $baseurl URL path without subdir.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 116933**: abbreviations - Invalid abbreviation casing
  `*     @type string       $url   URL of the uploaded file.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117139**: abbreviations - Invalid abbreviation casing
  `*     @type string $link_url       A URL to include a link to. Only works in combination with $link_text.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117199**: abbreviations - Invalid abbreviation casing
  `* Kills WordPress execution and displays JSON response with an error message.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117201**: abbreviations - Invalid abbreviation casing
  `* This is the handler for wp_die() when processing JSON requests.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117302**: abbreviations - Invalid abbreviation casing
  `* Encodes a variable into JSON, with some confidence checks.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117309**: abbreviations - Invalid abbreviation casing
  `* @param mixed $value Variable (usually an array or object) to encode as JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117313**: abbreviations - Invalid abbreviation casing
  `* @return string|false The JSON encoded string, or false if it cannot be encoded.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117321**: abbreviations - Invalid abbreviation casing
  `* Performs confidence checks on data that shall be encoded to JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117331**: abbreviations - Invalid abbreviation casing
  `* @param mixed $value Variable (usually an array or object) to encode as JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117333**: abbreviations - Invalid abbreviation casing
  `* @return mixed The sanitized data that shall be encoded to JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117339**: abbreviations - Invalid abbreviation casing
  `* Converts a string to UTF-8, so that it can be safely encoded to JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117354**: abbreviations - Invalid abbreviation casing
  `* Prepares response data to be serialized to JSON.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117371**: abbreviations - Invalid abbreviation casing
  `* Sends a JSON response back to an Ajax request.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117377**: abbreviations - Invalid abbreviation casing
  `* @param mixed $response    Variable (usually an array or object) to encode as JSON,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117386**: abbreviations - Invalid abbreviation casing
  `* Sends a JSON response back to an Ajax request, indicating success.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117392**: abbreviations - Invalid abbreviation casing
  `* @param mixed $value       Optional. Data to encode as JSON, then print and die. Default null.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117401**: abbreviations - Invalid abbreviation casing
  `* Sends a JSON response back to an Ajax request, indicating failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117413**: abbreviations - Invalid abbreviation casing
  `* @param mixed $value       Optional. Data to encode as JSON, then print and die. Default null.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117437**: abbreviations - Invalid abbreviation casing
  `* Reads and decodes a JSON file.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117441**: abbreviations - Invalid abbreviation casing
  `* @param string $filename Path to the JSON file.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117445**: abbreviations - Invalid abbreviation casing
  `*     @type bool $associative Optional. When `true`, JSON objects will be returned as associative arrays.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117446**: abbreviations - Invalid abbreviation casing
  `*                             When `false`, JSON objects will be returned as objects. Default false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117449**: abbreviations - Invalid abbreviation casing
  `* @return mixed Returns the value encoded in JSON in appropriate PHP type.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117459**: abbreviations - Invalid abbreviation casing
  `* Retrieves the WordPress home page URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117470**: abbreviations - Invalid abbreviation casing
  `* @param string $url URL for the home location.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117477**: abbreviations - Invalid abbreviation casing
  `* Retrieves the WordPress site URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117481**: abbreviations - Invalid abbreviation casing
  `* on your localhost while not having to change the database to your URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117488**: abbreviations - Invalid abbreviation casing
  `* @param string $url URL to set the WordPress site location.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117489**: abbreviations - Invalid abbreviation casing
  `* @return string The WordPress site URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117524**: abbreviations - Invalid abbreviation casing
  `* Determines whether WordPress is currently serving a REST API request.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 117535**: abbreviations - Invalid abbreviation casing
  `* @return bool True if it's a WordPress REST API request, false otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118155**: abbreviations - Invalid abbreviation casing
  `* Guesses the URL for the site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118162**: abbreviations - Invalid abbreviation casing
  `* @return string The guessed URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118209**: abbreviations - Invalid abbreviation casing
  `* @param int $site_id    Optional. Site ID to test. Defaults to current site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118210**: abbreviations - Invalid abbreviation casing
  `* @param int $network_id Optional. Network ID of the network to check for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118219**: abbreviations - Invalid abbreviation casing
  `* Gets the main site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118223**: abbreviations - Invalid abbreviation casing
  `* @param int $network_id Optional. The ID of the network for which to get the main site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118225**: abbreviations - Invalid abbreviation casing
  `* @return int The ID of the main site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118235**: abbreviations - Invalid abbreviation casing
  `* @param int $network_id Optional. Network ID to test. Defaults to current network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118242**: abbreviations - Invalid abbreviation casing
  `* Gets the main network ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118246**: abbreviations - Invalid abbreviation casing
  `* @return int The ID of the main network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118454**: abbreviations - Invalid abbreviation casing
  `* @return string SQL clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118465**: abbreviations - Invalid abbreviation casing
  `* @param callable $callback      Function that accepts ( ID, $callback_args ) and outputs parent_ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118466**: abbreviations - Invalid abbreviation casing
  `* @param int      $start         The ID to start the loop check at.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118484**: abbreviations - Invalid abbreviation casing
  `* @param callable $callback      Function that accepts ( ID, callback_arg, ... ) and outputs parent_ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118485**: abbreviations - Invalid abbreviation casing
  `* @param int      $start         The ID to start the loop check at.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118486**: abbreviations - Invalid abbreviation casing
  `* @param array    $override      Optional. An array of ( ID => parent_ID, ... ) to use instead of $callback.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118492**: abbreviations - Invalid abbreviation casing
  `* @return mixed Scalar ID of some arbitrary member of the loop, or array of IDs of all members of loop if`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118574**: abbreviations - Invalid abbreviation casing
  `* Checks whether the given cache ID is either an integer or an integer-like string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118581**: abbreviations - Invalid abbreviation casing
  `* @param mixed $object_id The cache ID to validate.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118582**: abbreviations - Invalid abbreviation casing
  `* @return bool Whether the given $object_id is a valid cache ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118599**: abbreviations - Invalid abbreviation casing
  `* Tests if a given path is a stream URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118603**: abbreviations - Invalid abbreviation casing
  `* @param string $path The resource path or URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118604**: abbreviations - Invalid abbreviation casing
  `* @return bool True if the path is a stream URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118884**: abbreviations - Invalid abbreviation casing
  `* Gets unique ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118893**: abbreviations - Invalid abbreviation casing
  `* @param string $prefix Prefix for the returned ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118894**: abbreviations - Invalid abbreviation casing
  `* @return string Unique ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118902**: abbreviations - Invalid abbreviation casing
  `* Generates an incremental ID that is independent per each different prefix.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118904**: abbreviations - Invalid abbreviation casing
  `* It is similar to `wp_unique_id`, but each prefix has its own internal ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118905**: abbreviations - Invalid abbreviation casing
  `* counter to make each prefix independent from each other. The ID starts at 1`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118912**: abbreviations - Invalid abbreviation casing
  `* @param string $prefix Optional. Prefix for the returned ID. Default empty string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118913**: abbreviations - Invalid abbreviation casing
  `* @return string Incremental ID per prefix.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118921**: abbreviations - Invalid abbreviation casing
  `* Generates a unique ID based on the structure and values of a given array.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118923**: abbreviations - Invalid abbreviation casing
  `* This function serializes the array into a JSON string and generates a hash`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118925**: abbreviations - Invalid abbreviation casing
  `* the generated ID for context or categorization.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118929**: abbreviations - Invalid abbreviation casing
  `* @param array  $data   The input array to generate an ID from.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118930**: abbreviations - Invalid abbreviation casing
  `* @param string $prefix Optional. A prefix to prepend to the generated ID. Default empty string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118931**: abbreviations - Invalid abbreviation casing
  `* @return string The generated unique ID for the array.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 118981**: abbreviations - Invalid abbreviation casing
  `* @return string  The anonymized IP address.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119011**: abbreviations - Invalid abbreviation casing
  `* Returns the URL of the directory used to store personal data export files.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119017**: abbreviations - Invalid abbreviation casing
  `* @return string Exports directory URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119047**: abbreviations - Invalid abbreviation casing
  `* Gets the URL to learn more about updating the PHP version the site is running on.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119049**: abbreviations - Invalid abbreviation casing
  `* This URL can be overridden by specifying an environment variable `WP_UPDATE_PHP_URL` or by using the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119051**: abbreviations - Invalid abbreviation casing
  `* default URL being used. Furthermore the page the URL links to should preferably be localized in the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119056**: abbreviations - Invalid abbreviation casing
  `* @return string URL to learn more about updating PHP.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119062**: abbreviations - Invalid abbreviation casing
  `* Gets the default URL to learn more about updating the PHP version the site is running on.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119064**: abbreviations - Invalid abbreviation casing
  `* Do not use this function to retrieve this URL. Instead, use {@see wp_get_update_php_url()} when relying on the URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119065**: abbreviations - Invalid abbreviation casing
  `* This function does not allow modifying the returned URL, and is only used to compare the actually used URL with the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119071**: abbreviations - Invalid abbreviation casing
  `* @return string Default URL to learn more about updating PHP.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119077**: abbreviations - Invalid abbreviation casing
  `* Prints the default annotation for the web host altering the "Update PHP" page URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119080**: abbreviations - Invalid abbreviation casing
  `* annotation if the web host has altered the default "Update PHP" page URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119096**: abbreviations - Invalid abbreviation casing
  `* Returns the default annotation for the web hosting altering the "Update PHP" page URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119099**: abbreviations - Invalid abbreviation casing
  `* annotation if the web host has altered the default "Update PHP" page URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119109**: abbreviations - Invalid abbreviation casing
  `* Gets the URL for directly updating the PHP version the site is running on.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119111**: abbreviations - Invalid abbreviation casing
  `* A URL will only be returned if the `WP_DIRECT_UPDATE_PHP_URL` environment variable is specified or`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119117**: abbreviations - Invalid abbreviation casing
  `* @return string URL for directly updating PHP or empty string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119127**: abbreviations - Invalid abbreviation casing
  `* The button is only displayed if a URL is returned by `wp_get_direct_php_update_url()`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119136**: abbreviations - Invalid abbreviation casing
  `* Gets the URL to learn more about updating the site to use HTTPS.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119138**: abbreviations - Invalid abbreviation casing
  `* This URL can be overridden by specifying an environment variable `WP_UPDATE_HTTPS_URL` or by using the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119140**: abbreviations - Invalid abbreviation casing
  `* default URL being used. Furthermore the page the URL links to should preferably be localized in the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119145**: abbreviations - Invalid abbreviation casing
  `* @return string URL to learn more about updating to HTTPS.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119151**: abbreviations - Invalid abbreviation casing
  `* Gets the default URL to learn more about updating the site to use HTTPS.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119153**: abbreviations - Invalid abbreviation casing
  `* Do not use this function to retrieve this URL. Instead, use {@see wp_get_update_https_url()} when relying on the URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119154**: abbreviations - Invalid abbreviation casing
  `* This function does not allow modifying the returned URL, and is only used to compare the actually used URL with the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119160**: abbreviations - Invalid abbreviation casing
  `* @return string Default URL to learn more about updating to HTTPS.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119166**: abbreviations - Invalid abbreviation casing
  `* Gets the URL for directly updating the site to use HTTPS.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119168**: abbreviations - Invalid abbreviation casing
  `* A URL will only be returned if the `WP_DIRECT_UPDATE_HTTPS_URL` environment variable is specified or`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119174**: abbreviations - Invalid abbreviation casing
  `* @return string URL for directly updating to HTTPS or empty string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119302**: abbreviations - Invalid abbreviation casing
  `*     @type string   $id                 Optional. The value of the admin notice's ID attribute. Default empty string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119333**: abbreviations - Invalid abbreviation casing
  `*     @type string   $id                 Optional. The value of the admin notice's ID attribute. Default empty string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119494**: abbreviations - Invalid abbreviation casing
  `* @param string|false     $src       Full URL of the script, or path of the script relative to the WordPress root directory.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119497**: abbreviations - Invalid abbreviation casing
  `* @param string|bool|null $ver       Optional. String specifying script version number, if it has one, which is added to the URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119599**: abbreviations - Invalid abbreviation casing
  `* @param string           $src       Full URL of the script, or path of the script relative to the WordPress root directory.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119602**: abbreviations - Invalid abbreviation casing
  `* @param string|bool|null $ver       Optional. String specifying script version number, if it has one, which is added to the URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119733**: abbreviations - Invalid abbreviation casing
  `* @param string|false     $src    Full URL of the stylesheet, or path of the stylesheet relative to the WordPress root directory.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119736**: abbreviations - Invalid abbreviation casing
  `* @param string|bool|null $ver    Optional. String specifying stylesheet version number, if it has one, which is added to the URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119772**: abbreviations - Invalid abbreviation casing
  `* @param string           $src    Full URL of the stylesheet, or path of the stylesheet relative to the WordPress root directory.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119775**: abbreviations - Invalid abbreviation casing
  `* @param string|bool|null $ver    Optional. String specifying stylesheet version number, if it has one, which is added to the URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119989**: abbreviations - Invalid abbreviation casing
  `* Retrieves the logout URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119991**: abbreviations - Invalid abbreviation casing
  `* Returns the URL that allows the user to log out of the site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 119996**: abbreviations - Invalid abbreviation casing
  `* @return string The logout URL. Note: HTML-encoded via esc_html() in wp_nonce_url().`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120002**: abbreviations - Invalid abbreviation casing
  `* Retrieves the login URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120009**: abbreviations - Invalid abbreviation casing
  `* @return string The login URL. Not HTML-encoded.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120015**: abbreviations - Invalid abbreviation casing
  `* Returns the URL that allows the user to register on the site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120019**: abbreviations - Invalid abbreviation casing
  `* @return string User registration URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120037**: abbreviations - Invalid abbreviation casing
  `*     @type string $redirect          URL to redirect to. Must be absolute, as in "https://example.com/mypage/".`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120039**: abbreviations - Invalid abbreviation casing
  `*     @type string $form_id           ID attribute value for the form. Default 'loginform'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120044**: abbreviations - Invalid abbreviation casing
  `*     @type string $id_username       ID attribute value for the username field. Default 'user_login'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120045**: abbreviations - Invalid abbreviation casing
  `*     @type string $id_password       ID attribute value for the password field. Default 'user_pass'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120046**: abbreviations - Invalid abbreviation casing
  `*     @type string $id_remember       ID attribute value for the remember field. Default 'rememberme'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120047**: abbreviations - Invalid abbreviation casing
  `*     @type string $id_submit         ID attribute value for the submit button. Default 'wp-submit'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120082**: abbreviations - Invalid abbreviation casing
  `* Returns the URL that allows the user to reset the lost password.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120087**: abbreviations - Invalid abbreviation casing
  `* @return string Lost password URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120142**: abbreviations - Invalid abbreviation casing
  `* - 'wpurl' - The WordPress address (URL) (set in Settings > General)`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120143**: abbreviations - Invalid abbreviation casing
  `* - 'url' - The Site address (URL) (set in Settings > General)`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120152**: abbreviations - Invalid abbreviation casing
  `* - 'stylesheet_url' - URL to the stylesheet for the active theme. An active child theme`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120156**: abbreviations - Invalid abbreviation casing
  `* - 'template_url' / 'template_directory' - URL of the active theme's directory. An active`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120158**: abbreviations - Invalid abbreviation casing
  `* - 'pingback_url' - The pingback XML-RPC file URL (xmlrpc.php)`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120159**: abbreviations - Invalid abbreviation casing
  `* - 'atom_url' - The Atom feed URL (/feed/atom)`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120160**: abbreviations - Invalid abbreviation casing
  `* - 'rdf_url' - The RDF/RSS 1.0 feed URL (/feed/rdf)`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120161**: abbreviations - Invalid abbreviation casing
  `* - 'rss_url' - The RSS 0.92 feed URL (/feed/rss)`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120162**: abbreviations - Invalid abbreviation casing
  `* - 'rss2_url' - The RSS 2.0 feed URL (/feed)`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120163**: abbreviations - Invalid abbreviation casing
  `* - 'comments_atom_url' - The comments Atom feed URL (/comments/feed)`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120164**: abbreviations - Invalid abbreviation casing
  `* - 'comments_rss2_url' - The comments RSS 2.0 feed URL (/comments/feed)`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120186**: abbreviations - Invalid abbreviation casing
  `* Returns the Site Icon URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120192**: abbreviations - Invalid abbreviation casing
  `* @param int    $blog_id Optional. ID of the blog to get the site icon for. Default current blog.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120193**: abbreviations - Invalid abbreviation casing
  `* @return string Site Icon URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120199**: abbreviations - Invalid abbreviation casing
  `* Displays the Site Icon URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120205**: abbreviations - Invalid abbreviation casing
  `* @param int    $blog_id Optional. ID of the blog to get the site icon for. Default current blog.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120215**: abbreviations - Invalid abbreviation casing
  `* @param int $blog_id Optional. ID of the blog in question. Default current blog.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120226**: abbreviations - Invalid abbreviation casing
  `* @param int $blog_id Optional. ID of the blog in question. Default is the ID of the current blog.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120240**: abbreviations - Invalid abbreviation casing
  `* @param int $blog_id Optional. ID of the blog in question. Default is the ID of the current blog.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120251**: abbreviations - Invalid abbreviation casing
  `* @param int $blog_id Optional. ID of the blog in question. Default is the ID of the current blog.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120505**: abbreviations - Invalid abbreviation casing
  `* @param string $url      URL to archive.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120690**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post   Optional. Post ID or WP_Post object. Default current post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120718**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post   Optional. Post ID or WP_Post object. Default current post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120744**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post   Post ID or post object. Default is global `$post` object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120759**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post      Post ID or post object. Default is global `$post` object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120779**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post   Optional. Post ID or post object. Default is global `$post` object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120799**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post  Optional. Post ID or post object. Default is global `$post` object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120829**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post   Optional. Post ID or WP_Post object. Default current post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120843**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post      Post ID or post object. Default is global `$post` object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 120945**: abbreviations - Invalid abbreviation casing
  `* the full URL as a referrer to other sites when cross-origin assets are loaded.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121066**: abbreviations - Invalid abbreviation casing
  `* @param string $editor_id HTML ID attribute value for the textarea and TinyMCE.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121343**: abbreviations - Invalid abbreviation casing
  `* @param string $url    The URL of the CSS file containing the color scheme.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121376**: abbreviations - Invalid abbreviation casing
  `* Displays the URL of a WordPress admin CSS file.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121400**: abbreviations - Invalid abbreviation casing
  `* stylesheet link to that generated URL is printed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121759**: abbreviations - Invalid abbreviation casing
  `* URL. The URL, and every URL it redirects to, are validated with wp_http_validate_url()`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121768**: abbreviations - Invalid abbreviation casing
  `* @see wp_http_validate_url() For more information about how the URL is validated.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121772**: abbreviations - Invalid abbreviation casing
  `* @param string $url  URL to retrieve.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121805**: abbreviations - Invalid abbreviation casing
  `* URL. The URL, and every URL it redirects to, are validated with wp_http_validate_url()`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121814**: abbreviations - Invalid abbreviation casing
  `* @see wp_http_validate_url() For more information about how the URL is validated.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121818**: abbreviations - Invalid abbreviation casing
  `* @param string $url  URL to retrieve.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121851**: abbreviations - Invalid abbreviation casing
  `* URL. The URL, and every URL it redirects to, are validated with wp_http_validate_url()`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121860**: abbreviations - Invalid abbreviation casing
  `* @see wp_http_validate_url() For more information about how the URL is validated.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121864**: abbreviations - Invalid abbreviation casing
  `* @param string $url  URL to retrieve.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121897**: abbreviations - Invalid abbreviation casing
  `* URL. The URL, and every URL it redirects to, are validated with wp_http_validate_url()`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121906**: abbreviations - Invalid abbreviation casing
  `* @see wp_http_validate_url() For more information about how the URL is validated.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121910**: abbreviations - Invalid abbreviation casing
  `* @param string $url  URL to retrieve.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121942**: abbreviations - Invalid abbreviation casing
  `* There are other API functions available which abstract away the HTTP method:`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121948**: abbreviations - Invalid abbreviation casing
  `* Important: If the URL is user-controlled, use `wp_safe_remote_request()` instead.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121954**: abbreviations - Invalid abbreviation casing
  `* @param string $url  URL to retrieve.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121986**: abbreviations - Invalid abbreviation casing
  `* Important: If the URL is user-controlled, use `wp_safe_remote_get()` instead.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 121993**: abbreviations - Invalid abbreviation casing
  `* @param string $url  URL to retrieve.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122025**: abbreviations - Invalid abbreviation casing
  `* Important: If the URL is user-controlled, use `wp_safe_remote_post()` instead.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122032**: abbreviations - Invalid abbreviation casing
  `* @param string $url  URL to retrieve.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122064**: abbreviations - Invalid abbreviation casing
  `* Important: If the URL is user-controlled, use `wp_safe_remote_head()` instead.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122071**: abbreviations - Invalid abbreviation casing
  `* @param string $url  URL to retrieve.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122209**: abbreviations - Invalid abbreviation casing
  `* @param string $url          Optional. If given, will check if the URL requires SSL and adds`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122222**: abbreviations - Invalid abbreviation casing
  `* @return string URL of the origin. Empty string if no origin.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122242**: abbreviations - Invalid abbreviation casing
  `* @param string|null $origin Origin URL. If not provided, the value of get_http_origin() is used.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122243**: abbreviations - Invalid abbreviation casing
  `* @return string Origin URL if allowed, empty string if not.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122258**: abbreviations - Invalid abbreviation casing
  `* @return string|false Returns the origin URL if headers are sent. Returns false`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122265**: abbreviations - Invalid abbreviation casing
  `* Validates a URL as safe for use in the HTTP API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122272**: abbreviations - Invalid abbreviation casing
  `* - `http:///example.com/caniload.php` - Malformed URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122274**: abbreviations - Invalid abbreviation casing
  `* - `http://example.invalid/caniload.php` - Invalid hostname, as the IP cannot be looked up in DNS.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122278**: abbreviations - Invalid abbreviation casing
  `* - `http://192.168.0.1/caniload.php` - IP address from LAN network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122285**: abbreviations - Invalid abbreviation casing
  `* @param string $url Request URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122286**: abbreviations - Invalid abbreviation casing
  `* @return string|false Returns false if the URL is not safe, or the original URL if it is safe.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122337**: abbreviations - Invalid abbreviation casing
  `* @param string $url       The URL to parse.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122341**: abbreviations - Invalid abbreviation casing
  `* @return mixed False on parse failure; Array of URL components on success;`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122343**: abbreviations - Invalid abbreviation casing
  `*               doesn't exist in the given URL; a string or - in the case of`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122350**: abbreviations - Invalid abbreviation casing
  `* Retrieves a specific component from a parsed URL array.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122359**: abbreviations - Invalid abbreviation casing
  `* @param array|false $url_parts The parsed URL. Can be false if the URL failed to parse.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122363**: abbreviations - Invalid abbreviation casing
  `* @return mixed False on parse failure; Array of URL components on success;`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122365**: abbreviations - Invalid abbreviation casing
  `*               doesn't exist in the given URL; a string or - in the case of`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122390**: abbreviations - Invalid abbreviation casing
  `* This is based on whether both the home and site URL are using HTTPS.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122402**: abbreviations - Invalid abbreviation casing
  `* Checks whether the current site URL is using HTTPS.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122413**: abbreviations - Invalid abbreviation casing
  `* Checks whether the current site's URL where WordPress is stored is using HTTPS.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122415**: abbreviations - Invalid abbreviation casing
  `* This checks the URL where WordPress application files (e.g. wp-blog-header.php or the wp-admin/ folder)`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122475**: abbreviations - Invalid abbreviation casing
  `* If a WordPress site had its URL changed from HTTP to HTTPS, by default this will return `true`, causing WordPress to`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122489**: abbreviations - Invalid abbreviation casing
  `* This function replaces all occurrences of the HTTP version of the site's URL with its HTTPS counterpart, if`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122501**: abbreviations - Invalid abbreviation casing
  `* Update the 'home' and 'siteurl' option to use the HTTPS variant of their URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122514**: abbreviations - Invalid abbreviation casing
  `* Updates the 'https_migration_required' option if needed when the given URL has been updated from HTTP to HTTPS.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122523**: abbreviations - Invalid abbreviation casing
  `* @param mixed $old_url Previous value of the URL option.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122524**: abbreviations - Invalid abbreviation casing
  `* @param mixed $new_url New value of the URL option.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122558**: abbreviations - Invalid abbreviation casing
  `* Gets and/or sets the initial state of an Interactivity API store for a`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122580**: abbreviations - Invalid abbreviation casing
  `* Gets and/or sets the configuration of the Interactivity API for a given`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122602**: abbreviations - Invalid abbreviation casing
  `* by providing a way to pass an array of data, which encodes into a JSON string`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122613**: abbreviations - Invalid abbreviation casing
  `* @return string A complete `data-wp-context` directive with a JSON encoded value representing the context array and`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122620**: abbreviations - Invalid abbreviation casing
  `* Gets the current Interactivity API context for a given namespace.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122668**: abbreviations - Invalid abbreviation casing
  `* @param string[]       $allowed_protocols Optional. Array of allowed URL protocols.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122719**: abbreviations - Invalid abbreviation casing
  `* @param string[]       $allowed_protocols Array of allowed URL protocols.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122745**: abbreviations - Invalid abbreviation casing
  `* @global string[]       $pass_allowed_protocols Array of allowed URL protocols.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122751**: abbreviations - Invalid abbreviation casing
  `* @param string[]       $allowed_protocols Array of allowed URL protocols.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122758**: abbreviations - Invalid abbreviation casing
  `* Returns an array of HTML attribute names whose value contains a URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122761**: abbreviations - Invalid abbreviation casing
  `* a URL according to the HTML specification.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122769**: abbreviations - Invalid abbreviation casing
  `* @return string[] HTML attribute names whose value contains a URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122783**: abbreviations - Invalid abbreviation casing
  `* @global string[]       $pass_allowed_protocols Array of allowed URL protocols.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122812**: abbreviations - Invalid abbreviation casing
  `* @param string[]       $allowed_protocols Array of allowed URL protocols.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122845**: abbreviations - Invalid abbreviation casing
  `* @param string[]       $allowed_protocols Array of allowed URL protocols.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122875**: abbreviations - Invalid abbreviation casing
  `* conform to W3C's HTML specification. It will also remove bad URL protocols`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122882**: abbreviations - Invalid abbreviation casing
  `* @param string[] $allowed_protocols Array of allowed URL protocols.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122937**: abbreviations - Invalid abbreviation casing
  `* Sanitizes a string and removed disallowed URL protocols.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 122947**: abbreviations - Invalid abbreviation casing
  `* @param string[] $allowed_protocols Array of allowed URL protocols.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 123009**: abbreviations - Invalid abbreviation casing
  `* This function searches for URL protocols at the beginning of the string, while`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 123015**: abbreviations - Invalid abbreviation casing
  `* @param string[] $allowed_protocols Array of allowed URL protocols.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 123025**: abbreviations - Invalid abbreviation casing
  `* This function processes URL protocols, checks to see if they're in the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 123034**: abbreviations - Invalid abbreviation casing
  `* @param string[] $allowed_protocols Array of allowed URL protocols.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 123155**: abbreviations - Invalid abbreviation casing
  `* need them in the allowed URL protocols system anyway.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 123348**: abbreviations - Invalid abbreviation casing
  `* @since 6.3.0 Extended support for `filter` to accept a URL and added support for repeat().`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 123381**: abbreviations - Invalid abbreviation casing
  `* Helper function to check if this is a safe PDF URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 123387**: abbreviations - Invalid abbreviation casing
  `* @param string $url The URL to check.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 123388**: abbreviations - Invalid abbreviation casing
  `* @return bool True if the URL is safe, false otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 123424**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_User $user User's ID or a WP_User object. Defaults to current user.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 123979**: abbreviations - Invalid abbreviation casing
  `* @return string|false The translated strings in JSON encoding on success,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 123993**: abbreviations - Invalid abbreviation casing
  `* @return string|false The JSON-encoded translated strings for the given script handle and text domain.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124141**: abbreviations - Invalid abbreviation casing
  `*     @type string   $id                           ID attribute of the select element. Default 'locale'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124210**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124312**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or post object. Default is the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124329**: abbreviations - Invalid abbreviation casing
  `* @param string $url         URL with or without a trailing slash.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124330**: abbreviations - Invalid abbreviation casing
  `* @param string $type_of_url Optional. The type of URL being considered (e.g. single, category, etc)`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124332**: abbreviations - Invalid abbreviation casing
  `* @return string The URL with the trailing slash appended or stripped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124341**: abbreviations - Invalid abbreviation casing
  `* attribute. The id mode uses 'post-' with the post ID for the 'id' attribute.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124356**: abbreviations - Invalid abbreviation casing
  `* @param WP_Post|int|null $post   Optional. Post ID or post object. Defaults to global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124366**: abbreviations - Invalid abbreviation casing
  `* Retrieves the full permalink for the current post or post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124374**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post      Optional. Post ID or post object. Default is the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124376**: abbreviations - Invalid abbreviation casing
  `* @return string|false The permalink URL. False if the post does not exist.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124383**: abbreviations - Invalid abbreviation casing
  `* Retrieves the full permalink for the current post or post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124387**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post      Optional. Post ID or post object. Default is the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124389**: abbreviations - Invalid abbreviation casing
  `* @return string|false The permalink URL. False if the post does not exist.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124403**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post      Optional. Post ID or post object. Default is the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124406**: abbreviations - Invalid abbreviation casing
  `* @return string|false The post permalink URL. False if the post does not exist.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124413**: abbreviations - Invalid abbreviation casing
  `* Retrieves the permalink for the current page or page ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124419**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post      Optional. Post ID or object. Default uses the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124438**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post      Optional. Post ID or object. Default uses the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124456**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post      Optional. Post ID or object. Default uses the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124536**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id Optional. Post ID. Default is the ID of the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124548**: abbreviations - Invalid abbreviation casing
  `* anchor. If no link text is specified, default text is used. If no post ID is`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124554**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id   Optional. Post ID. Default is the ID of the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124569**: abbreviations - Invalid abbreviation casing
  `* @param int    $author_id Author ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124585**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Term|object $cat  The ID or category object whose feed link will be retrieved.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124601**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Term|object $term     The ID or term object whose feed link will be retrieved.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124615**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Term|object $tag  The ID or term object whose feed link will be retrieved.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124628**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Term|object $tag      The ID or term object whose edit link will be retrieved.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124630**: abbreviations - Invalid abbreviation casing
  `* @return string The edit tag link URL for the given tag.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124650**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL for editing a given term.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124655**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Term|object $term        The ID or term object whose edit link will be retrieved.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124661**: abbreviations - Invalid abbreviation casing
  `* @return string|null The edit term link URL for the given term, or null on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124674**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Term|null $term    Optional. Term ID or object. If null, the queried object will be inspected. Default null.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124755**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL used for the post preview.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124761**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post         Optional. Post ID or `WP_Post` object. Defaults to global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124766**: abbreviations - Invalid abbreviation casing
  `* @return string|null URL used for the post preview, or null if the post does not exist.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124781**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post    Optional. Post ID or post object. Default is the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124798**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post      Optional. Post ID or post object. Default is the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124812**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post         Optional. Post ID or post object. Default is the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124815**: abbreviations - Invalid abbreviation casing
  `* @return string|void The delete post link URL for the given post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124826**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Optional. Comment ID or WP_Comment object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124827**: abbreviations - Invalid abbreviation casing
  `* @param string         $context    Optional. Context in which the URL should be used. Either 'display',`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124829**: abbreviations - Invalid abbreviation casing
  `* @return string|void The edit comment link URL for the given comment, or void if the comment id does not exist or`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124853**: abbreviations - Invalid abbreviation casing
  `* @param int|stdClass $link Optional. Bookmark ID. Default is the ID of the current bookmark.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124854**: abbreviations - Invalid abbreviation casing
  `* @return string|void The edit bookmark link URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124867**: abbreviations - Invalid abbreviation casing
  `* @param int    $bookmark Optional. Bookmark ID. Default is the current bookmark.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124878**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id Optional. User ID. Defaults to the current user.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124879**: abbreviations - Invalid abbreviation casing
  `* @return string URL to edit user page or empty string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 124953**: abbreviations - Invalid abbreviation casing
  `* @return string|void The adjacent post relational link URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125054**: abbreviations - Invalid abbreviation casing
  `* @return string The link URL of the previous post in relation to the current post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125089**: abbreviations - Invalid abbreviation casing
  `* @return string The link URL of the next post in relation to the current post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125128**: abbreviations - Invalid abbreviation casing
  `* @return string The link URL of the previous or next post in relation to the current post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125161**: abbreviations - Invalid abbreviation casing
  `* @param bool $escape  Optional. Whether to escape the URL for display, with esc_url().`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125162**: abbreviations - Invalid abbreviation casing
  `*                      If set to false, prepares the URL with sanitize_url(). Default true.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125163**: abbreviations - Invalid abbreviation casing
  `* @return string The link URL for the given page number.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125178**: abbreviations - Invalid abbreviation casing
  `* @return string|void The link URL for next posts page.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125190**: abbreviations - Invalid abbreviation casing
  `* @return string|void The link URL for next posts page if `$display = false`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125526**: abbreviations - Invalid abbreviation casing
  `* @return string The comments page number link URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125705**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL for the current site where the front end is accessible.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125713**: abbreviations - Invalid abbreviation casing
  `* @param string      $path   Optional. Path relative to the home URL. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125714**: abbreviations - Invalid abbreviation casing
  `* @param string|null $scheme Optional. Scheme to give the home URL context. Accepts`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125716**: abbreviations - Invalid abbreviation casing
  `* @return string Home URL link with optional path appended.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125722**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL for a given site where the front end is accessible.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125730**: abbreviations - Invalid abbreviation casing
  `* @param int|null    $blog_id Optional. Site ID. Default null (current site).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125731**: abbreviations - Invalid abbreviation casing
  `* @param string      $path    Optional. Path relative to the home URL. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125732**: abbreviations - Invalid abbreviation casing
  `* @param string|null $scheme  Optional. Scheme to give the home URL context. Accepts`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125734**: abbreviations - Invalid abbreviation casing
  `* @return string Home URL link with optional path appended.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125740**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL for the current site where WordPress application files`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125749**: abbreviations - Invalid abbreviation casing
  `* @param string      $path   Optional. Path relative to the site URL. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125750**: abbreviations - Invalid abbreviation casing
  `* @param string|null $scheme Optional. Scheme to give the site URL context. See set_url_scheme().`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125751**: abbreviations - Invalid abbreviation casing
  `* @return string Site URL link with optional path appended.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125757**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL for a given site where WordPress application files`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125766**: abbreviations - Invalid abbreviation casing
  `* @param int|null    $blog_id Optional. Site ID. Default null (current site).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125767**: abbreviations - Invalid abbreviation casing
  `* @param string      $path    Optional. Path relative to the site URL. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125768**: abbreviations - Invalid abbreviation casing
  `* @param string|null $scheme  Optional. Scheme to give the site URL context. Accepts`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125771**: abbreviations - Invalid abbreviation casing
  `* @return string Site URL link with optional path appended.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125777**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL to the admin area for the current site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125781**: abbreviations - Invalid abbreviation casing
  `* @param string $path   Optional. Path relative to the admin URL. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125784**: abbreviations - Invalid abbreviation casing
  `* @return string Admin URL link with optional path appended.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125790**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL to the admin area for a given site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125794**: abbreviations - Invalid abbreviation casing
  `* @param int|null $blog_id Optional. Site ID. Default null (current site).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125795**: abbreviations - Invalid abbreviation casing
  `* @param string   $path    Optional. Path relative to the admin URL. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125799**: abbreviations - Invalid abbreviation casing
  `* @return string Admin URL link with optional path appended.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125806**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL to the includes directory.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125810**: abbreviations - Invalid abbreviation casing
  `* @param string      $path   Optional. Path relative to the includes URL. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125811**: abbreviations - Invalid abbreviation casing
  `* @param string|null $scheme Optional. Scheme to give the includes URL context. Accepts`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125813**: abbreviations - Invalid abbreviation casing
  `* @return string Includes URL link with optional path appended.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125819**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL to the content directory.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125823**: abbreviations - Invalid abbreviation casing
  `* @param string $path Optional. Path relative to the content URL. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125824**: abbreviations - Invalid abbreviation casing
  `* @return string Content URL link with optional path appended.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125830**: abbreviations - Invalid abbreviation casing
  `* Retrieves a URL within the plugins or mu-plugins directory.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125832**: abbreviations - Invalid abbreviation casing
  `* Defaults to the plugins directory URL if no arguments are supplied.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125836**: abbreviations - Invalid abbreviation casing
  `* @param string $path   Optional. Extra path appended to the end of the URL, including`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125839**: abbreviations - Invalid abbreviation casing
  `*                       The URL will be relative to its directory. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125841**: abbreviations - Invalid abbreviation casing
  `* @return string Plugins URL link with optional paths appended.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125847**: abbreviations - Invalid abbreviation casing
  `* Retrieves the site URL for the current network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125849**: abbreviations - Invalid abbreviation casing
  `* Returns the site URL with the appropriate protocol, 'https' if`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125857**: abbreviations - Invalid abbreviation casing
  `* @param string      $path   Optional. Path relative to the site URL. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125858**: abbreviations - Invalid abbreviation casing
  `* @param string|null $scheme Optional. Scheme to give the site URL context. Accepts`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125860**: abbreviations - Invalid abbreviation casing
  `* @return string Site URL link with optional path appended.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125866**: abbreviations - Invalid abbreviation casing
  `* Retrieves the home URL for the current network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125868**: abbreviations - Invalid abbreviation casing
  `* Returns the home URL with the appropriate protocol, 'https' is_ssl()`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125874**: abbreviations - Invalid abbreviation casing
  `* @param string      $path   Optional. Path relative to the home URL. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125875**: abbreviations - Invalid abbreviation casing
  `* @param string|null $scheme Optional. Scheme to give the home URL context. Accepts`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125877**: abbreviations - Invalid abbreviation casing
  `* @return string Home URL link with optional path appended.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125883**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL to the admin area for the network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125887**: abbreviations - Invalid abbreviation casing
  `* @param string $path   Optional path relative to the admin URL. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125890**: abbreviations - Invalid abbreviation casing
  `* @return string Admin URL link with optional path appended.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125896**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL to the admin area for the current user.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125900**: abbreviations - Invalid abbreviation casing
  `* @param string $path   Optional. Path relative to the admin URL. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125903**: abbreviations - Invalid abbreviation casing
  `* @return string Admin URL link with optional path appended.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125909**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL to the admin area for either the current site or the network depending on context.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125913**: abbreviations - Invalid abbreviation casing
  `* @param string $path   Optional. Path relative to the admin URL. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125916**: abbreviations - Invalid abbreviation casing
  `* @return string Admin URL link with optional path appended.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125922**: abbreviations - Invalid abbreviation casing
  `* Sets the scheme for a URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125927**: abbreviations - Invalid abbreviation casing
  `* @param string      $url    Absolute URL that includes a scheme`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125930**: abbreviations - Invalid abbreviation casing
  `* @return string URL with chosen scheme.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125936**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL to the user's dashboard.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125944**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id Optional. User ID. Defaults to current user.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125949**: abbreviations - Invalid abbreviation casing
  `* @return string Dashboard URL link with optional path appended.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125955**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL to the user's profile editor.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125959**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id Optional. User ID. Defaults to current user.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125962**: abbreviations - Invalid abbreviation casing
  `* @return string Dashboard URL link with optional path appended.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125968**: abbreviations - Invalid abbreviation casing
  `* Returns the canonical URL for a post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125975**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or object. Default is global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 125976**: abbreviations - Invalid abbreviation casing
  `* @return string|false The canonical URL. False if the post does not exist`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126003**: abbreviations - Invalid abbreviation casing
  `* @param int    $id          Optional. A post or site ID. Default is 0, which means the current post or site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126004**: abbreviations - Invalid abbreviation casing
  `* @param string $context     Optional. Whether the ID is a 'site' ID, 'post' ID, or 'media' ID. If 'post',`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126006**: abbreviations - Invalid abbreviation casing
  `*                            to determine the ID and context. Default 'post'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126056**: abbreviations - Invalid abbreviation casing
  `* Retrieves the avatar URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126060**: abbreviations - Invalid abbreviation casing
  `* @param mixed $id_or_email The avatar to retrieve a URL for. Accepts a user ID, Gravatar SHA-256 or MD5 hash,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126066**: abbreviations - Invalid abbreviation casing
  `*     @type string $default        URL for the default image or a default type. Accepts:`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126088**: abbreviations - Invalid abbreviation casing
  `*     @type string $scheme         URL scheme to use. See set_url_scheme() for accepted values.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126093**: abbreviations - Invalid abbreviation casing
  `* @return string|false The URL of the avatar on success, false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126124**: abbreviations - Invalid abbreviation casing
  `* @param mixed $id_or_email The avatar to retrieve. Accepts a user ID, Gravatar SHA-256 or MD5 hash,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126132**: abbreviations - Invalid abbreviation casing
  `*     @type string $default        URL for the default image or a default type. Accepts:`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126154**: abbreviations - Invalid abbreviation casing
  `*     @type string $scheme         URL scheme to use. See set_url_scheme() for accepted values.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126169**: abbreviations - Invalid abbreviation casing
  `*     @type string|false $url          The URL of the avatar that was found, or false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126191**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL of a file in the theme.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126199**: abbreviations - Invalid abbreviation casing
  `* @return string The URL of the file.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126205**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL of a file in the parent theme.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126209**: abbreviations - Invalid abbreviation casing
  `* @param string $file Optional. File to return the URL for in the template directory.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126210**: abbreviations - Invalid abbreviation casing
  `* @return string The URL of the file.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126241**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL to the privacy policy page.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126245**: abbreviations - Invalid abbreviation casing
  `* @return string The URL to the privacy policy page. Empty string if it doesn't exist.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126276**: abbreviations - Invalid abbreviation casing
  `* Returns an array of URL hosts which are considered to be internal hosts.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126281**: abbreviations - Invalid abbreviation casing
  `* This list is used when determining if a specified URL is a link to a page on`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126290**: abbreviations - Invalid abbreviation casing
  `* @return string[] An array of URL hosts.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126297**: abbreviations - Invalid abbreviation casing
  `* Determines whether or not the specified URL is of a host included in the internal hosts list.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126303**: abbreviations - Invalid abbreviation casing
  `* @param string $link The URL to test.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126885**: abbreviations - Invalid abbreviation casing
  `* Retrieves the current site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126891**: abbreviations - Invalid abbreviation casing
  `* @return int Site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126898**: abbreviations - Invalid abbreviation casing
  `* Retrieves the current network ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 126902**: abbreviations - Invalid abbreviation casing
  `* @return int The ID of the current network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127057**: abbreviations - Invalid abbreviation casing
  `* Checks whether current request is a JSON request, or is expecting a JSON response.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127078**: abbreviations - Invalid abbreviation casing
  `* Checks whether a string is a valid JSON Media Type.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127083**: abbreviations - Invalid abbreviation casing
  `* @return bool True if string is a valid JSON Media Type.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127221**: abbreviations - Invalid abbreviation casing
  `* The URL might be the original image, or it might be a resized version. This`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127231**: abbreviations - Invalid abbreviation casing
  `* @param int          $id   Attachment ID for image.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127237**: abbreviations - Invalid abbreviation casing
  `*     @type string $0 Image source URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127332**: abbreviations - Invalid abbreviation casing
  `* parameters are: what WordPress will use for the class, the Attachment ID,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127341**: abbreviations - Invalid abbreviation casing
  `* @param int          $id    Attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127471**: abbreviations - Invalid abbreviation casing
  `* @param int          $post_id Attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127476**: abbreviations - Invalid abbreviation casing
  `*     path and URL if registered size is passed to `$size` parameter. False on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127482**: abbreviations - Invalid abbreviation casing
  `*     @type string $url    URL of image.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127523**: abbreviations - Invalid abbreviation casing
  `* @param int          $attachment_id Image attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127530**: abbreviations - Invalid abbreviation casing
  `*     @type string $0 Image source URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127558**: abbreviations - Invalid abbreviation casing
  `* @param int          $attachment_id Image attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127565**: abbreviations - Invalid abbreviation casing
  `*     @type string       $src           Image attachment URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127597**: abbreviations - Invalid abbreviation casing
  `* Gets the URL of an image attachment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127601**: abbreviations - Invalid abbreviation casing
  `* @param int          $attachment_id Image attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127605**: abbreviations - Invalid abbreviation casing
  `* @return string|false Attachment URL or false if no image is available. If `$size` does not match`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127606**: abbreviations - Invalid abbreviation casing
  `*                      any registered image size, the original image URL will be returned.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127654**: abbreviations - Invalid abbreviation casing
  `* @param int          $attachment_id Image attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127677**: abbreviations - Invalid abbreviation casing
  `* @param int    $attachment_id Optional. The image attachment ID. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127694**: abbreviations - Invalid abbreviation casing
  `* @param int          $attachment_id Image attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127711**: abbreviations - Invalid abbreviation casing
  `* @param string|null  $image_src     Optional. The URL to the image file. Default null.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127714**: abbreviations - Invalid abbreviation casing
  `* @param int          $attachment_id Optional. Image attachment ID. Either `$image_meta` or `$attachment_id``
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127724**: abbreviations - Invalid abbreviation casing
  `* The image meta data is retrieved by attachment post ID. In some cases the post IDs may change.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127733**: abbreviations - Invalid abbreviation casing
  `* @param int    $attachment_id  Optional. The image attachment ID. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127746**: abbreviations - Invalid abbreviation casing
  `* @param int    $attachment_id Optional. The image attachment ID. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127763**: abbreviations - Invalid abbreviation casing
  `* @param int    $attachment_id Image attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127867**: abbreviations - Invalid abbreviation casing
  `* @param int    $attachment_id Image attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127880**: abbreviations - Invalid abbreviation casing
  `* @param int    $attachment_id Image attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127993**: abbreviations - Invalid abbreviation casing
  `*     @type string $id         ID of the image and caption container element, i.e. `<figure>` or `<div>`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127994**: abbreviations - Invalid abbreviation casing
  `*     @type string $caption_id ID of the caption element, i.e. `<figcaption>` or `<p>`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128034**: abbreviations - Invalid abbreviation casing
  `* @since 4.2.0 Passed the shortcode instance ID to `post_gallery` and `post_playlist` filters.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128046**: abbreviations - Invalid abbreviation casing
  `*     @type string       $orderby    The field to use when ordering the images. Default 'menu_order ID'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128047**: abbreviations - Invalid abbreviation casing
  `*                                    Accepts any valid SQL ORDERBY statement.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128048**: abbreviations - Invalid abbreviation casing
  `*     @type int          $id         Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128120**: abbreviations - Invalid abbreviation casing
  `*                                 Otherwise default is 'menu_order ID'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128123**: abbreviations - Invalid abbreviation casing
  `*                                 Default is the current post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128159**: abbreviations - Invalid abbreviation casing
  `* @param string $url The media element URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128199**: abbreviations - Invalid abbreviation casing
  `*     @type string $src      URL to the source of the audio file. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128245**: abbreviations - Invalid abbreviation casing
  `*     @type string $src      URL to the source of the video file. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128365**: abbreviations - Invalid abbreviation casing
  `* @param int|array|object $attachment Attachment ID, data array, or data object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128509**: abbreviations - Invalid abbreviation casing
  `* to be JSON-encoded and fit into an Attachment model.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128513**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $attachment Attachment ID or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128518**: abbreviations - Invalid abbreviation casing
  `*     @type string $author                ID of the attachment author, as a string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128526**: abbreviations - Invalid abbreviation casing
  `*     @type string $editLink              URL to the edit page for the attachment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128531**: abbreviations - Invalid abbreviation casing
  `*     @type string $icon                  Icon URL of the attachment (e.g. /wp-includes/images/media/archive.png).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128532**: abbreviations - Invalid abbreviation casing
  `*     @type int    $id                    ID of the attachment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128533**: abbreviations - Invalid abbreviation casing
  `*     @type string $link                  URL to the attachment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128549**: abbreviations - Invalid abbreviation casing
  `*     @type string $uploadedToLink        URL to the edit page of the parent post of the attachment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128551**: abbreviations - Invalid abbreviation casing
  `*     @type string $url                   Direct URL to the attachment file (from wp-content).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128607**: abbreviations - Invalid abbreviation casing
  `*     @type int|WP_Post $post Post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128623**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128646**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Post ID or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128659**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128673**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128687**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128705**: abbreviations - Invalid abbreviation casing
  `* Tries to convert an attachment URL into a post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128711**: abbreviations - Invalid abbreviation casing
  `* @param string $url The URL to resolve.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128712**: abbreviations - Invalid abbreviation casing
  `* @return int The found post ID, or 0 on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128732**: abbreviations - Invalid abbreviation casing
  `* @param array[] $exporters An array of personal data exporters, keyed by their ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128951**: abbreviations - Invalid abbreviation casing
  `* @param int    $object_id  ID of the object metadata is for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128963**: abbreviations - Invalid abbreviation casing
  `* @return int|false The meta ID on success, false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128970**: abbreviations - Invalid abbreviation casing
  `* ID and metadata key, the metadata will be added.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128980**: abbreviations - Invalid abbreviation casing
  `* @param int    $object_id  ID of the object metadata is for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 128986**: abbreviations - Invalid abbreviation casing
  `* @return int|bool The new meta field ID if a field with the given key didn't exist`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129005**: abbreviations - Invalid abbreviation casing
  `* @param int    $object_id  ID of the object metadata is for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129023**: abbreviations - Invalid abbreviation casing
  `* Retrieves the value of a metadata field for the specified object type and ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129039**: abbreviations - Invalid abbreviation casing
  `* @param int    $object_id ID of the object metadata is for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129048**: abbreviations - Invalid abbreviation casing
  `*               An empty array if a valid but non-existing object ID is passed and `$single` is false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129049**: abbreviations - Invalid abbreviation casing
  `*               An empty string if a valid but non-existing object ID is passed and `$single` is true.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129066**: abbreviations - Invalid abbreviation casing
  `* @param int    $object_id ID of the object metadata is for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129090**: abbreviations - Invalid abbreviation casing
  `* @param int    $object_id ID of the object metadata is for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129101**: abbreviations - Invalid abbreviation casing
  `* Determines if a meta field with the given key exists for the given object ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129107**: abbreviations - Invalid abbreviation casing
  `* @param int    $object_id ID of the object metadata is for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129115**: abbreviations - Invalid abbreviation casing
  `* Retrieves metadata by meta ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129123**: abbreviations - Invalid abbreviation casing
  `* @param int    $meta_id   ID for a specific meta row.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129129**: abbreviations - Invalid abbreviation casing
  `*     @type string $meta_id    Optional. The meta ID when the meta type is any value except 'user'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129130**: abbreviations - Invalid abbreviation casing
  `*     @type string $umeta_id   Optional. The meta ID when the meta type is 'user'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129131**: abbreviations - Invalid abbreviation casing
  `*     @type string $blog_id    Optional. The object ID when the meta type is 'blog'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129132**: abbreviations - Invalid abbreviation casing
  `*     @type string $post_id    Optional. The object ID when the meta type is 'post'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129133**: abbreviations - Invalid abbreviation casing
  `*     @type string $comment_id Optional. The object ID when the meta type is 'comment'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129134**: abbreviations - Invalid abbreviation casing
  `*     @type string $term_id    Optional. The object ID when the meta type is 'term'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129135**: abbreviations - Invalid abbreviation casing
  `*     @type string $user_id    Optional. The object ID when the meta type is 'user'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129153**: abbreviations - Invalid abbreviation casing
  `* Updates metadata by meta ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129161**: abbreviations - Invalid abbreviation casing
  `* @param int          $meta_id    ID for a specific meta row.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129170**: abbreviations - Invalid abbreviation casing
  `* Deletes metadata by meta ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129178**: abbreviations - Invalid abbreviation casing
  `* @param int    $meta_id   ID for a specific meta row.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129210**: abbreviations - Invalid abbreviation casing
  `* Given a meta query, generates SQL clauses to be appended to a main query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129219**: abbreviations - Invalid abbreviation casing
  `* @param string $primary_id_column Primary ID column name.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129222**: abbreviations - Invalid abbreviation casing
  `*     Array containing JOIN and WHERE SQL clauses to append to the main query,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129225**: abbreviations - Invalid abbreviation casing
  `*     @type string $join  SQL fragment to append to the main JOIN clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129226**: abbreviations - Invalid abbreviation casing
  `*     @type string $where SQL fragment to append to the main WHERE clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129320**: abbreviations - Invalid abbreviation casing
  `*                                         should be accessible via the REST API. A custom post type must also declare`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129353**: abbreviations - Invalid abbreviation casing
  `* @param int    $object_id ID of the object metadata is for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129420**: abbreviations - Invalid abbreviation casing
  `* @param int    $object_id   ID of the object the metadata is for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129424**: abbreviations - Invalid abbreviation casing
  `*               and values for an object ID if not. False if a given $meta_key is not registered.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129446**: abbreviations - Invalid abbreviation casing
  `* Returns the object subtype for a given object ID of a specific type.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129452**: abbreviations - Invalid abbreviation casing
  `* @param int    $object_id   ID of the object to retrieve its subtype.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129467**: abbreviations - Invalid abbreviation casing
  `* Gets a full site URL, given a site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129471**: abbreviations - Invalid abbreviation casing
  `* @param int $blog_id Site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129472**: abbreviations - Invalid abbreviation casing
  `* @return string Full site URL if found. Empty string if not.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129478**: abbreviations - Invalid abbreviation casing
  `* Gets a full site URL, given a site name.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129489**: abbreviations - Invalid abbreviation casing
  `* Retrieves a site's ID given its (subdomain or directory) slug.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129495**: abbreviations - Invalid abbreviation casing
  `* @return int|null The site ID, or null if no site is found for the given slug.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129507**: abbreviations - Invalid abbreviation casing
  `* @param int|string|array $fields  Optional. A blog ID, a blog slug, or an array of fields to query against.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129508**: abbreviations - Invalid abbreviation casing
  `*                                  Defaults to the current blog ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129521**: abbreviations - Invalid abbreviation casing
  `* @param int $blog_id Optional. Blog ID. Defaults to current blog.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129527**: abbreviations - Invalid abbreviation casing
  `* Updates the details for a blog and the blogs table for a given blog ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129531**: abbreviations - Invalid abbreviation casing
  `* @param int   $blog_id Blog ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129543**: abbreviations - Invalid abbreviation casing
  `* @param int $site_id Optional. Site ID. Default is the current site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129560**: abbreviations - Invalid abbreviation casing
  `* @param int    $id            A blog ID. Can be null to refer to the current blog.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129561**: abbreviations - Invalid abbreviation casing
  `* @param string $option        Name of option to retrieve. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129569**: abbreviations - Invalid abbreviation casing
  `* Adds a new option for a given blog ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129582**: abbreviations - Invalid abbreviation casing
  `* @param int    $id     A blog ID. Can be null to refer to the current blog.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129583**: abbreviations - Invalid abbreviation casing
  `* @param string $option Name of option to add. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129584**: abbreviations - Invalid abbreviation casing
  `* @param mixed  $value  Option value, can be anything. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129591**: abbreviations - Invalid abbreviation casing
  `* Removes an option by name for a given blog ID. Prevents removal of protected WordPress options.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129595**: abbreviations - Invalid abbreviation casing
  `* @param int    $id     A blog ID. Can be null to refer to the current blog.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129596**: abbreviations - Invalid abbreviation casing
  `* @param string $option Name of option to remove. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129607**: abbreviations - Invalid abbreviation casing
  `* @param int    $id         The blog ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129634**: abbreviations - Invalid abbreviation casing
  `* @param int  $new_blog_id The ID of the blog to switch to. Default: current blog.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129664**: abbreviations - Invalid abbreviation casing
  `* @param int $new_site_id New site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129665**: abbreviations - Invalid abbreviation casing
  `* @param int $old_site_id Old site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129688**: abbreviations - Invalid abbreviation casing
  `* @param int $id Blog ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129699**: abbreviations - Invalid abbreviation casing
  `* @param int    $id       Blog ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129714**: abbreviations - Invalid abbreviation casing
  `* @param int    $blog_id    Blog ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129730**: abbreviations - Invalid abbreviation casing
  `* @param int    $id   Blog ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129773**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Post ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129785**: abbreviations - Invalid abbreviation casing
  `* @param int     $post_id Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129811**: abbreviations - Invalid abbreviation casing
  `* @param int $network_id Optional. The network to get counts for. Default is the current network ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 129952**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130025**: abbreviations - Invalid abbreviation casing
  `* @param string $url Optional. Redirect URL. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130032**: abbreviations - Invalid abbreviation casing
  `* Adds an 'updated=true' argument to a URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130038**: abbreviations - Invalid abbreviation casing
  `* @param string $url Optional. Redirect URL. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130045**: abbreviations - Invalid abbreviation casing
  `* Get a numeric user ID from either an email address or a login.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130047**: abbreviations - Invalid abbreviation casing
  `* A numeric string is considered to be an existing user ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130061**: abbreviations - Invalid abbreviation casing
  `* Get a full site URL, given a domain and a path.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130083**: abbreviations - Invalid abbreviation casing
  `* @return string|int The ID of the newly created blog`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130113**: abbreviations - Invalid abbreviation casing
  `*     @type int|int[] $network_id A network ID or array of network IDs. Set to null to retrieve sites`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130114**: abbreviations - Invalid abbreviation casing
  `*                                 from all networks. Defaults to current network ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130159**: abbreviations - Invalid abbreviation casing
  `* the new blog's ID. It is the first step in creating a new blog.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130168**: abbreviations - Invalid abbreviation casing
  `* @return int|false The ID of the new row`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130220**: abbreviations - Invalid abbreviation casing
  `* @param int    $id         The user ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130237**: abbreviations - Invalid abbreviation casing
  `* @param int    $term_id    An ID for a term on the current blog.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130239**: abbreviations - Invalid abbreviation casing
  `* @return int An ID from the global terms table mapped from $term_id.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130274**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id The unique ID of the user`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130289**: abbreviations - Invalid abbreviation casing
  `* @param int|null $network_id ID of the network. Default is the current network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130303**: abbreviations - Invalid abbreviation casing
  `* @param int $blog_id ID of the blog.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130304**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id ID of the post being looked for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130317**: abbreviations - Invalid abbreviation casing
  `* @param int    $blog_id ID of the blog the user is being added to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130318**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id ID of the user being added.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130339**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id  ID of the user being removed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130340**: abbreviations - Invalid abbreviation casing
  `* @param int $blog_id  Optional. ID of the blog the user is being removed from. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130341**: abbreviations - Invalid abbreviation casing
  `* @param int $reassign Optional. ID of the user to whom to reassign posts. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130352**: abbreviations - Invalid abbreviation casing
  `* @param int $blog_id ID of the source blog.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130353**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id ID of the desired post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130360**: abbreviations - Invalid abbreviation casing
  `* Gets a blog's numeric ID from its URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130373**: abbreviations - Invalid abbreviation casing
  `* @return int 0 if no blog found, otherwise the ID of the matching blog.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130606**: abbreviations - Invalid abbreviation casing
  `* @param int      $id       ID of the user to delete.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130607**: abbreviations - Invalid abbreviation casing
  `* @param int|null $reassign ID of the user to reassign posts and links to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130653**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id    The user ID of the new site's admin.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130659**: abbreviations - Invalid abbreviation casing
  `* @param int    $network_id Optional. Network ID. Only relevant on multi-network installations.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130661**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error Returns WP_Error object on failure, the new site ID on success.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130675**: abbreviations - Invalid abbreviation casing
  `* @param WP_Site|int $blog_id    The new site's object or ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130690**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id The new user's ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130709**: abbreviations - Invalid abbreviation casing
  `* @param int    $network_id Optional. Network ID. Only relevant on multi-network installations.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130711**: abbreviations - Invalid abbreviation casing
  `* @return int|null The site ID if the site name exists, null otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130726**: abbreviations - Invalid abbreviation casing
  `* @param int    $blog_id  Site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130727**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id  User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130752**: abbreviations - Invalid abbreviation casing
  `* @param int $site_id Site ID of the new site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130753**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id User ID of the administrator of the new site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130769**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id  User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130809**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130850**: abbreviations - Invalid abbreviation casing
  `* Logs the user email, IP, and registration date of a new site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130857**: abbreviations - Invalid abbreviation casing
  `* @param WP_Site|int $blog_id The new site's object or ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130858**: abbreviations - Invalid abbreviation casing
  `* @param int|array   $user_id User ID, or array of arguments including 'user_id'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130941**: abbreviations - Invalid abbreviation casing
  `*     @type int    $user_id The ID of the user being added to the current blog.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 130964**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id  User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131044**: abbreviations - Invalid abbreviation casing
  `* Formats a URL to use https.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131050**: abbreviations - Invalid abbreviation casing
  `* @param string $url URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131051**: abbreviations - Invalid abbreviation casing
  `* @return string URL with https as the scheme.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131071**: abbreviations - Invalid abbreviation casing
  `* @param int|null $network_id ID of the network. Default is the current network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131085**: abbreviations - Invalid abbreviation casing
  `* @param int|null $network_id ID of the network. Default is the current network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131100**: abbreviations - Invalid abbreviation casing
  `* @param int|null $network_id ID of the network. Default is the current network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131112**: abbreviations - Invalid abbreviation casing
  `* @param int|null $network_id ID of the network. Default is the current network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131124**: abbreviations - Invalid abbreviation casing
  `* @param int|null $network_id ID of the network. Default is the current network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131189**: abbreviations - Invalid abbreviation casing
  `* @param int|null $network_id ID of the network. Default is the current network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131227**: abbreviations - Invalid abbreviation casing
  `* @param int    $network_id  ID of the network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131324**: abbreviations - Invalid abbreviation casing
  `* If neither a network or site is found, `false` or a URL string will be returned`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131339**: abbreviations - Invalid abbreviation casing
  `*                     Redirect URL if parts exist, but the request as a whole can not be fulfilled.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131404**: abbreviations - Invalid abbreviation casing
  `* @param object|int $network The network's database row or ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131411**: abbreviations - Invalid abbreviation casing
  `* Retrieves network data given a network ID or network object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131446**: abbreviations - Invalid abbreviation casing
  `* @param int|array $ids Network ID or an array of network IDs to remove from cache.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131455**: abbreviations - Invalid abbreviation casing
  `* Will add the networks in $networks to the cache. If network ID already exists`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131457**: abbreviations - Invalid abbreviation casing
  `* cache using the network group with the key using the ID of the networks.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131492**: abbreviations - Invalid abbreviation casing
  `*     @type int    $network_id   The site's network ID. Default is the current network ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131493**: abbreviations - Invalid abbreviation casing
  `*     @type string $registered   When the site was registered, in SQL datetime format. Default is`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131495**: abbreviations - Invalid abbreviation casing
  `*     @type string $last_updated When the site was last updated, in SQL datetime format. Default is`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131502**: abbreviations - Invalid abbreviation casing
  `*     @type int    $lang_id      The site's language ID. Currently unused. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131503**: abbreviations - Invalid abbreviation casing
  `*     @type int    $user_id      User ID for the site administrator. Passed to the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131505**: abbreviations - Invalid abbreviation casing
  `*     @type string $title        Site title. Default is 'Site %d' where %d is the site ID. Passed`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131512**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error The new site's ID on success, or error object on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131541**: abbreviations - Invalid abbreviation casing
  `* @param int   $site_id ID of the site that should be updated.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131543**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error The updated site's ID on success, or error object on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131572**: abbreviations - Invalid abbreviation casing
  `* @param int $site_id ID of the site that should be deleted.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131579**: abbreviations - Invalid abbreviation casing
  `* Retrieves site data given a site ID or site object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131603**: abbreviations - Invalid abbreviation casing
  `* @param array $ids               ID list.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131636**: abbreviations - Invalid abbreviation casing
  `* Performs SQL query to retrieve all metadata for the sites matching `$site_ids` and stores them in the cache.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131665**: abbreviations - Invalid abbreviation casing
  `*   ID?: int,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131811**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Site $site_id Site ID or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131815**: abbreviations - Invalid abbreviation casing
  `*     @type int    $user_id Required. User ID for the site administrator.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131817**: abbreviations - Invalid abbreviation casing
  `*                           site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131843**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Site $site_id Site ID or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131858**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Site $site_id Site ID or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131871**: abbreviations - Invalid abbreviation casing
  `* @param WP_Site|int $blog The site object or ID to be cleared from cache.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131884**: abbreviations - Invalid abbreviation casing
  `* @param int    $site_id    Site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131895**: abbreviations - Invalid abbreviation casing
  `* @return int|false Meta ID on success, false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131911**: abbreviations - Invalid abbreviation casing
  `* @param int    $site_id    Site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131926**: abbreviations - Invalid abbreviation casing
  `* @param int    $site_id Site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131935**: abbreviations - Invalid abbreviation casing
  `*               An empty array if a valid but non-existing site ID is passed and `$single` is false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131936**: abbreviations - Invalid abbreviation casing
  `*               An empty string if a valid but non-existing site ID is passed and `$single` is true.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131950**: abbreviations - Invalid abbreviation casing
  `* same key and site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131958**: abbreviations - Invalid abbreviation casing
  `* @param int    $site_id    Site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131964**: abbreviations - Invalid abbreviation casing
  `* @return int|bool Meta ID if the key didn't exist, true on successful update,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132019**: abbreviations - Invalid abbreviation casing
  `* Updates the `blog_public` option for a given site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132023**: abbreviations - Invalid abbreviation casing
  `* @param int    $site_id   Site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132063**: abbreviations - Invalid abbreviation casing
  `*     @type int|string|WP_Term $menu                 Desired menu. Accepts a menu ID, slug, name, or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132067**: abbreviations - Invalid abbreviation casing
  `*     @type string             $menu_id              The ID that is applied to the ul element which forms the menu.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132073**: abbreviations - Invalid abbreviation casing
  `*     @type string             $container_id         The ID that is applied to the container. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132149**: abbreviations - Invalid abbreviation casing
  `* Prevents a menu item ID from being used more than once.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132187**: abbreviations - Invalid abbreviation casing
  `* @param int|string|WP_Term $menu Menu ID, slug, name, or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132194**: abbreviations - Invalid abbreviation casing
  `* Determines whether the given ID is a navigation menu.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132200**: abbreviations - Invalid abbreviation casing
  `* @param int|string|WP_Term $menu Menu ID, slug, name, or object of menu to check.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132290**: abbreviations - Invalid abbreviation casing
  `* Determines whether the given ID is a nav menu item.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132294**: abbreviations - Invalid abbreviation casing
  `* @param int $menu_item_id The ID of the potential nav menu item.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132295**: abbreviations - Invalid abbreviation casing
  `* @return bool Whether the given ID is that of a nav menu item.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132308**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error Menu ID on success, WP_Error object on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132318**: abbreviations - Invalid abbreviation casing
  `* @param int|string|WP_Term $menu Menu ID, slug, name, or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132331**: abbreviations - Invalid abbreviation casing
  `* @param int   $menu_id   The ID of the menu or "0" to create a new menu.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132333**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error Menu ID on success, WP_Error object on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132347**: abbreviations - Invalid abbreviation casing
  `* @param int   $menu_id          The ID of the menu. If 0, makes the menu item a draft orphan.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132348**: abbreviations - Invalid abbreviation casing
  `* @param int   $menu_item_db_id  The ID of the menu item. If 0, creates a new menu item.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132351**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error The menu item's database ID or WP_Error object on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132394**: abbreviations - Invalid abbreviation casing
  `* @param int|string|WP_Term $menu Menu ID, slug, name, or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132443**: abbreviations - Invalid abbreviation casing
  `* - ID:               The term_id if the menu item represents a taxonomy term.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132446**: abbreviations - Invalid abbreviation casing
  `* - db_id:            The DB ID of this item as a nav_menu_item object, if it exists (0 if it doesn't exist).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132448**: abbreviations - Invalid abbreviation casing
  `* - menu_item_parent: The DB ID of the nav_menu_item that is this item's menu parent, if any. 0 otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132450**: abbreviations - Invalid abbreviation casing
  `* - object_id:        The DB ID of the original object this menu item represents, e.g. ID for posts and term_id for categories.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132451**: abbreviations - Invalid abbreviation casing
  `* - post_parent:      The DB ID of the original object's parent object, if any (0 otherwise).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132457**: abbreviations - Invalid abbreviation casing
  `* - url:              The URL to which this menu item points.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132474**: abbreviations - Invalid abbreviation casing
  `* @param int    $object_id   Optional. The ID of the original object. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132490**: abbreviations - Invalid abbreviation casing
  `* @param int $object_id The ID of the original object being trashed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132501**: abbreviations - Invalid abbreviation casing
  `* @param int    $object_id The ID of the original object being trashed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132502**: abbreviations - Invalid abbreviation casing
  `* @param int    $tt_id     Term taxonomy ID. Unused.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132528**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Post ID for the customize_changeset.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132595**: abbreviations - Invalid abbreviation casing
  `* 2. When one of the Options API filters is used: {@see 'pre_option_$option'},`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132631**: abbreviations - Invalid abbreviation casing
  `* @param string $option        Name of the option to retrieve. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132696**: abbreviations - Invalid abbreviation casing
  `*                       expected to not be SQL-escaped. The autoload values should be boolean values. For backward`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132715**: abbreviations - Invalid abbreviation casing
  `* @param string[] $options  List of option names. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132736**: abbreviations - Invalid abbreviation casing
  `* @param string $option   Name of the option. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132810**: abbreviations - Invalid abbreviation casing
  `* @param int|null $network_id ID of the network. Can be null to default to the current network ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132824**: abbreviations - Invalid abbreviation casing
  `* @param int $network_id Optional. Network ID of network for which to prime network options cache. Defaults to current network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132847**: abbreviations - Invalid abbreviation casing
  `* @param string    $option   Name of the option to update. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132848**: abbreviations - Invalid abbreviation casing
  `* @param mixed     $value    Option value. Must be serializable if non-scalar. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132886**: abbreviations - Invalid abbreviation casing
  `* @param string    $option     Name of the option to add. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132888**: abbreviations - Invalid abbreviation casing
  `*                              Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132912**: abbreviations - Invalid abbreviation casing
  `* @param string $option Name of the option to delete. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132966**: abbreviations - Invalid abbreviation casing
  `* @param string $transient Transient name. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132980**: abbreviations - Invalid abbreviation casing
  `* @param string $transient Transient name. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132994**: abbreviations - Invalid abbreviation casing
  `* @param string $transient  Transient name. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 132997**: abbreviations - Invalid abbreviation casing
  `*                           Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133125**: abbreviations - Invalid abbreviation casing
  `* @param string $option        Name of the option to retrieve. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133143**: abbreviations - Invalid abbreviation casing
  `* @param string $option Name of the option to add. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133144**: abbreviations - Invalid abbreviation casing
  `* @param mixed  $value  Option value, can be anything. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133158**: abbreviations - Invalid abbreviation casing
  `* @param string $option Name of the option to delete. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133172**: abbreviations - Invalid abbreviation casing
  `* @param string $option Name of the option. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133173**: abbreviations - Invalid abbreviation casing
  `* @param mixed  $value  Option value. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133188**: abbreviations - Invalid abbreviation casing
  `* @param int|null $network_id    ID of the network. Can be null to default to the current network ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133189**: abbreviations - Invalid abbreviation casing
  `* @param string   $option        Name of the option to retrieve. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133207**: abbreviations - Invalid abbreviation casing
  `* @param int|null $network_id ID of the network. Can be null to default to the current network ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133208**: abbreviations - Invalid abbreviation casing
  `* @param string   $option     Name of the option to add. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133209**: abbreviations - Invalid abbreviation casing
  `* @param mixed    $value      Option value, can be anything. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133224**: abbreviations - Invalid abbreviation casing
  `* @param int|null $network_id ID of the network. Can be null to default to the current network ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133225**: abbreviations - Invalid abbreviation casing
  `* @param string   $option     Name of the option to delete. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133240**: abbreviations - Invalid abbreviation casing
  `* @param int|null $network_id ID of the network. Can be null to default to the current network ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133241**: abbreviations - Invalid abbreviation casing
  `* @param string   $option     Name of the option. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133242**: abbreviations - Invalid abbreviation casing
  `* @param mixed    $value      Option value. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133253**: abbreviations - Invalid abbreviation casing
  `* @param string $transient Transient name. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133269**: abbreviations - Invalid abbreviation casing
  `* @param string $transient Transient name. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133285**: abbreviations - Invalid abbreviation casing
  `* @param string $transient  Transient name. Expected to not be SQL-escaped. Must be`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133287**: abbreviations - Invalid abbreviation casing
  `* @param mixed  $value      Transient value. Expected to not be SQL-escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133297**: abbreviations - Invalid abbreviation casing
  `* The settings registered here are primarily useful for the REST API, so this`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133332**: abbreviations - Invalid abbreviation casing
  `*     @type bool|array $show_in_rest      Whether data associated with this setting should be included in the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133385**: abbreviations - Invalid abbreviation casing
  `*         @type bool|array $show_in_rest      Whether data associated with this setting should be included in the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133430**: abbreviations - Invalid abbreviation casing
  `* Changes the current user by ID or name.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133432**: abbreviations - Invalid abbreviation casing
  `* Set $id to null and specify a name if you do not know a user's ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133438**: abbreviations - Invalid abbreviation casing
  `* @param int|null $id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133562**: abbreviations - Invalid abbreviation casing
  `* Changes the current user by ID or name.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133564**: abbreviations - Invalid abbreviation casing
  `* Set $id to null and specify a name if you do not know a user's ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133574**: abbreviations - Invalid abbreviation casing
  `* @param int|null $id   User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133599**: abbreviations - Invalid abbreviation casing
  `* Retrieves user info by user ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133603**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id User ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133613**: abbreviations - Invalid abbreviation casing
  `* @since 4.4.0 Added 'ID' as an alias of 'id' for the `$field` parameter.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133617**: abbreviations - Invalid abbreviation casing
  `* @param string     $field The field to retrieve the user with. id | ID | slug | email | login.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133618**: abbreviations - Invalid abbreviation casing
  `* @param int|string $value A value for $field. A user ID, slug, email address, or login name.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133620**: abbreviations - Invalid abbreviation casing
  `* @phpstan-return ($field is 'id'|'ID' ? ($value is int<min, 0> ? false : \WP_User|false) : \WP_User|false)`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133632**: abbreviations - Invalid abbreviation casing
  `* @param int[] $user_ids User ID numbers list`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133653**: abbreviations - Invalid abbreviation casing
  `* reference the embedded file in your HTML with a `cid:` URL whose value`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133654**: abbreviations - Invalid abbreviation casing
  `* matches the file's Content-ID. By default, the Content-ID (`cid`) used for`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133661**: abbreviations - Invalid abbreviation casing
  `* You may also customize the Content-ID for each file by using the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133725**: abbreviations - Invalid abbreviation casing
  `* @return int|false User ID if valid cookie, false if invalid.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133737**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id    User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133780**: abbreviations - Invalid abbreviation casing
  `* Sets the authentication cookies for a given user ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133791**: abbreviations - Invalid abbreviation casing
  `* @param int         $user_id  User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133903**: abbreviations - Invalid abbreviation casing
  `* @param string       $location      The path or URL to redirect to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133912**: abbreviations - Invalid abbreviation casing
  `* Sanitizes a URL for use in a redirect.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133917**: abbreviations - Invalid abbreviation casing
  `* @return string Redirect-sanitized URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133923**: abbreviations - Invalid abbreviation casing
  `* URL encodes UTF-8 characters in a URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133932**: abbreviations - Invalid abbreviation casing
  `* @return string URL-encoded version of the first RegEx match.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133964**: abbreviations - Invalid abbreviation casing
  `* @param string       $location      The path or URL to redirect to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133973**: abbreviations - Invalid abbreviation casing
  `* Validates a URL for use in a redirect.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133985**: abbreviations - Invalid abbreviation casing
  `* @return string Redirect-sanitized URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 133995**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Comment $comment_id Comment ID or WP_Comment object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 134012**: abbreviations - Invalid abbreviation casing
  `* @param int $comment_id Comment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 134038**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id    User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 134180**: abbreviations - Invalid abbreviation casing
  `* may not always be a user ID associated with the password.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 134194**: abbreviations - Invalid abbreviation casing
  `* @param string|int $user_id  Optional. ID of a user associated with the password.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 134215**: abbreviations - Invalid abbreviation casing
  `* and there may not always be a user ID associated with the password.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 134222**: abbreviations - Invalid abbreviation casing
  `* @param string|int $user_id Optional. ID of a user associated with the password.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 134282**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id  User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 134300**: abbreviations - Invalid abbreviation casing
  `* @param mixed  $id_or_email   The avatar to retrieve. Accepts a user ID, Gravatar MD5 hash,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 134303**: abbreviations - Invalid abbreviation casing
  `* @param string $default_value URL for the default image or a default type. Accepts:`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 134332**: abbreviations - Invalid abbreviation casing
  `*     @type string       $scheme        URL scheme to use. See set_url_scheme() for accepted values.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 134658**: abbreviations - Invalid abbreviation casing
  `* Action API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 134945**: abbreviations - Invalid abbreviation casing
  `* Get the URL directory path (with trailing slash) for the plugin __FILE__ passed in.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 134950**: abbreviations - Invalid abbreviation casing
  `* @return string the URL path of the directory that contains the plugin.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135054**: abbreviations - Invalid abbreviation casing
  `* Builds a unique string ID for a hook callback function.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135068**: abbreviations - Invalid abbreviation casing
  `* @param string                $hook_name Unused. The name of the filter to build ID for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135069**: abbreviations - Invalid abbreviation casing
  `* @param callable|string|array $callback  The callback to generate ID for. The callback may`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135073**: abbreviations - Invalid abbreviation casing
  `* @return string|null Unique function ID for usage as array key.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135084**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post|null $post Optional. Post ID or post object. Defaults to the current post in the loop.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135224**: abbreviations - Invalid abbreviation casing
  `* Displays the ID of the current item in the WordPress Loop.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135232**: abbreviations - Invalid abbreviation casing
  `* Retrieves the ID of the current item in the WordPress Loop.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135236**: abbreviations - Invalid abbreviation casing
  `* @return int|false The ID of the current item in the WordPress Loop. False if $post is not set.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135296**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135309**: abbreviations - Invalid abbreviation casing
  `* URL is escaped to make it XML-safe.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135313**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135327**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135359**: abbreviations - Invalid abbreviation casing
  `* @param WP_Post|object|int $post           Optional. WP_Post instance or Post ID/object. Default null.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135379**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135394**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135407**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post     $post      Optional. Post ID or post object. Defaults to the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135426**: abbreviations - Invalid abbreviation casing
  `* `$css_class` parameter value, with the post ID as the last parameter.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135433**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post     $post      Optional. Post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135577**: abbreviations - Invalid abbreviation casing
  `*     @type int          $child_of              Page ID to retrieve child pages of. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135590**: abbreviations - Invalid abbreviation casing
  `*                                               elements. Accepts any valid post field. Default 'ID'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135638**: abbreviations - Invalid abbreviation casing
  `*     @type int          $child_of     Display only the sub-pages of a single page by ID. Default 0 (all pages).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135656**: abbreviations - Invalid abbreviation casing
  `*                                      'menu_order', 'post_parent', 'ID', 'rand', or 'comment_count'. Default 'post_title'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135718**: abbreviations - Invalid abbreviation casing
  `*     @type string          $menu_id      ID for the div containing the page list. Default is empty string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135820**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post       Optional. Post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135832**: abbreviations - Invalid abbreviation casing
  `* @since 4.4.0 The `$post` parameter can now accept either a post ID or `WP_Post` object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135834**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post  $post      Optional. Post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135863**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135896**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135908**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $revision Revision ID or revision object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135920**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $revision Revision ID or revision object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135935**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135947**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post|null $post Optional. Post ID or WP_Post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135958**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post|null $post Optional. Post ID or WP_Post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135972**: abbreviations - Invalid abbreviation casing
  `* @since 4.4.0 `$post` can be a post ID or WP_Post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135974**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post|null $post Optional. Post ID or WP_Post object. Default is global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135981**: abbreviations - Invalid abbreviation casing
  `* Retrieves the post thumbnail ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135984**: abbreviations - Invalid abbreviation casing
  `* @since 4.4.0 `$post` can be a post ID or WP_Post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135988**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post|null $post Optional. Post ID or WP_Post object. Default is global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 135989**: abbreviations - Invalid abbreviation casing
  `* @return int|false Post thumbnail ID (which can be 0 if the thumbnail is not set),`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136040**: abbreviations - Invalid abbreviation casing
  `* @since 4.4.0 `$post` can be a post ID or WP_Post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136042**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post|null $post Optional. Post ID or WP_Post object.  Default is global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136052**: abbreviations - Invalid abbreviation casing
  `* Returns the post thumbnail URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136056**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post|null $post Optional. Post ID or WP_Post object.  Default is global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136059**: abbreviations - Invalid abbreviation casing
  `* @return string|false Post thumbnail URL or false if no image is available. If `$size` does not match`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136060**: abbreviations - Invalid abbreviation casing
  `*                      any registered image size, the original image URL will be returned.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136066**: abbreviations - Invalid abbreviation casing
  `* Displays the post thumbnail URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136082**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post|null $post Optional. Post ID or WP_Post object. Default is global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136093**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post|null $post Optional. Post ID or WP_Post object. Default is global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136109**: abbreviations - Invalid abbreviation casing
  `* Retrieves attached file path based on attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136120**: abbreviations - Invalid abbreviation casing
  `* @param int  $attachment_id Attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136129**: abbreviations - Invalid abbreviation casing
  `* Updates attachment file path based on attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136136**: abbreviations - Invalid abbreviation casing
  `* @param int    $attachment_id Attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136138**: abbreviations - Invalid abbreviation casing
  `* @return int|bool Meta ID if the `_wp_attached_file` key didn't exist for the attachment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136160**: abbreviations - Invalid abbreviation casing
  `* Retrieves all children of the post parent ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136187**: abbreviations - Invalid abbreviation casing
  `* The 'post_parent' argument is the ID to get the children. The 'numberposts'`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136244**: abbreviations - Invalid abbreviation casing
  `* Retrieves post data given a post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136253**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post|null $post   Optional. Post ID or post object. `null`, `false`, `0` and other PHP falsey values`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136254**: abbreviations - Invalid abbreviation casing
  `*                                 return the current global post inside the loop. A numerically valid post ID that`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136275**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136283**: abbreviations - Invalid abbreviation casing
  `* Retrieves data from a post field based on Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136297**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post    Optional. Post ID or post object. Defaults to global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136307**: abbreviations - Invalid abbreviation casing
  `* Retrieves the mime type of an attachment based on the ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136314**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or post object. Defaults to global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136321**: abbreviations - Invalid abbreviation casing
  `* Retrieves the post status based on the post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136323**: abbreviations - Invalid abbreviation casing
  `* If the post ID is of an attachment, then the parent post status will be given`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136328**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or post object. Defaults to global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136511**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post|null $post Optional. Post ID or post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136576**: abbreviations - Invalid abbreviation casing
  `*              arguments to register the post type in REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136623**: abbreviations - Invalid abbreviation casing
  `*     @type bool         $show_in_rest                    Whether to include the post type in the REST API. Set this to true`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136625**: abbreviations - Invalid abbreviation casing
  `*     @type string       $rest_base                       To change the base URL of REST API route. Default is $post_type.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136626**: abbreviations - Invalid abbreviation casing
  `*     @type string       $rest_namespace                  To change the namespace URL of REST API route. Default is wp/v2.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136627**: abbreviations - Invalid abbreviation casing
  `*     @type string       $rest_controller_class           REST API controller class name. Default is 'WP_REST_Posts_Controller'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136628**: abbreviations - Invalid abbreviation casing
  `*     @type string|bool  $autosave_rest_controller_class  REST API controller class name. Default is 'WP_REST_Autosaves_Controller'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136629**: abbreviations - Invalid abbreviation casing
  `*     @type string|bool  $revisions_rest_controller_class REST API controller class name. Default is 'WP_REST_Revisions_Controller'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136630**: abbreviations - Invalid abbreviation casing
  `*     @type bool         $late_route_registration         A flag to direct the REST API controllers for autosave / revisions`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136634**: abbreviations - Invalid abbreviation casing
  `*     @type string       $menu_icon                       The URL to the icon to be used for this menu. Pass a base64-encoded`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 136712**: abbreviations - Invalid abbreviation casing
  `*     @type string       $_edit_link                   FOR INTERNAL USE ONLY! URL segment to use for edit link of`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137090**: abbreviations - Invalid abbreviation casing
  `* Updates the post type for the post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137092**: abbreviations - Invalid abbreviation casing
  `* The page or post cache will be cleaned for the post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137098**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id   Optional. Post ID to change post type. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137147**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post|null $post Optional. Post ID or post object. Defaults to global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137158**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post|null $post Optional. Post ID or `WP_Post` object. Defaults to global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137186**: abbreviations - Invalid abbreviation casing
  `*     @type int|string $category         Category ID or comma-separated list of IDs (this or any children).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137290**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id    Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137301**: abbreviations - Invalid abbreviation casing
  `* @return int|false Meta ID on success, false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137307**: abbreviations - Invalid abbreviation casing
  `* Deletes a post meta field for the given post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137317**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id    Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137328**: abbreviations - Invalid abbreviation casing
  `* Retrieves a post meta field for the given post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137332**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137341**: abbreviations - Invalid abbreviation casing
  `*               An empty array if a valid but non-existing post ID is passed and `$single` is false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137342**: abbreviations - Invalid abbreviation casing
  `*               An empty string if a valid but non-existing post ID is passed and `$single` is true.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137353**: abbreviations - Invalid abbreviation casing
  `* Updates a post meta field based on the given post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137356**: abbreviations - Invalid abbreviation casing
  `* same key and post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137358**: abbreviations - Invalid abbreviation casing
  `* If the meta field for the post does not exist, it will be added and its ID returned.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137366**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id    Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137372**: abbreviations - Invalid abbreviation casing
  `* @return int|bool Meta ID if the key didn't exist, true on successful update,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137432**: abbreviations - Invalid abbreviation casing
  `* Retrieves post meta fields, based on post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137439**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Optional. Post ID. Default is the ID of the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137442**: abbreviations - Invalid abbreviation casing
  `*               An empty string if a valid but non-existing post ID is passed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137454**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Optional. Post ID. Default is the ID of the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137469**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id Optional. Post ID. Default is the ID of the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137478**: abbreviations - Invalid abbreviation casing
  `* Sticky posts should remain at the top of The Loop. If the post ID is not`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137479**: abbreviations - Invalid abbreviation casing
  `* given, then The Loop ID for the current post will be used.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137487**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Optional. Post ID. Default is the ID of the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137529**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137536**: abbreviations - Invalid abbreviation casing
  `* @phpstan-return ($field is 'ID'|'post_parent'|'menu_order' ? (T is int ? T : int) : ($field is 'ancestors' ? (T is array<int<0, max>>|list<int<0, max>> ? T : (T is list ? list<int<0, max>> : array<int<0, max>>)) : ($context is 'raw' ? T : ($context is 'attribute'|'js' ? string : ($context is 'edit' ? ($field is not 'post_content' ? string : mixed) : mixed)))))`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137548**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137560**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137650**: abbreviations - Invalid abbreviation casing
  `* Converts MIME types into SQL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137658**: abbreviations - Invalid abbreviation casing
  `* @return string The SQL AND clause for mime searching.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137679**: abbreviations - Invalid abbreviation casing
  `* @param int  $post_id      Post ID. (The default of 0 is for historical reasons; providing it is incorrect.)`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137696**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137710**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Optional. Post ID. Default is the ID of the global `$post``
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137724**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Optional. Post ID. Default is the ID of the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137737**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post|null $post Optional. Post ID or post object. Defaults to global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137750**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post|null $post Optional. Post ID or post object. Defaults to global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137766**: abbreviations - Invalid abbreviation casing
  `* @param int   $post_id Optional. The Post ID. Does not default to the ID of the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137823**: abbreviations - Invalid abbreviation casing
  `* @param int   $post_id Optional. The Post ID. Does not default to the ID of the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137874**: abbreviations - Invalid abbreviation casing
  `* @param int             $post_id  Optional. The Post ID. Does not default to the ID of the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137946**: abbreviations - Invalid abbreviation casing
  `* If the $postarr parameter has 'ID' set to a value, then post will be updated.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137964**: abbreviations - Invalid abbreviation casing
  `*     @type int    $ID                    The post ID. If equal to something other than 0,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137965**: abbreviations - Invalid abbreviation casing
  `*                                         the post with that ID will be updated. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137966**: abbreviations - Invalid abbreviation casing
  `*     @type int    $post_author           The ID of the user who added the post. Default is`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137967**: abbreviations - Invalid abbreviation casing
  `*                                         the current user ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137991**: abbreviations - Invalid abbreviation casing
  `*     @type string $guid                  Global Unique ID for referencing the post. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137992**: abbreviations - Invalid abbreviation casing
  `*     @type int    $import_id             The post ID to be used when inserting a new post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 137993**: abbreviations - Invalid abbreviation casing
  `*                                         If specified, must not match any existing post ID. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138004**: abbreviations - Invalid abbreviation casing
  `*                                         so the only way to connect them is using ID. Default empty.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138010**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error The post ID on success. The value 0 or WP_Error on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138012**: abbreviations - Invalid abbreviation casing
  `*   ID?: int,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138059**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error The post ID on success. The value 0 or WP_Error on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138061**: abbreviations - Invalid abbreviation casing
  `*   ID?: int,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138100**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138107**: abbreviations - Invalid abbreviation casing
  `* Publishes future post and make sure post ID has future post status.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138114**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138146**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id     Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138149**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_parent Post parent ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138177**: abbreviations - Invalid abbreviation casing
  `* @param int          $post_id Optional. The Post ID. Does not default to the ID of the global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138192**: abbreviations - Invalid abbreviation casing
  `* @param int          $post_id Optional. The Post ID. Does not default to the ID of the global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138209**: abbreviations - Invalid abbreviation casing
  `* @param int          $post_id  Optional. The Post ID. Does not default to the ID of the global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138229**: abbreviations - Invalid abbreviation casing
  `* @param int       $post_id         Optional. The Post ID. Does not default to the ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138231**: abbreviations - Invalid abbreviation casing
  `* @param int[]|int $post_categories Optional. List of category IDs, or the ID of a single category.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138269**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post  $post        The post ID or object that has been saved.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138279**: abbreviations - Invalid abbreviation casing
  `* Adds a URL to those already pinged.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138287**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post  $post Post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138299**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138312**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Post ID or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138324**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138336**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138354**: abbreviations - Invalid abbreviation casing
  `* Retrieves page data given a page ID or page object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138361**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $page   Page object or page ID. Passed by reference.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138391**: abbreviations - Invalid abbreviation casing
  `* Identifies descendants of a given page ID in a list of page objects.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138397**: abbreviations - Invalid abbreviation casing
  `* @param int       $page_id Page ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138413**: abbreviations - Invalid abbreviation casing
  `* @param int       $page_id Optional. Parent page ID. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138414**: abbreviations - Invalid abbreviation casing
  `* @return string[] Array of post names keyed by ID and arranged by hierarchy. Children immediately follow their parents.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138429**: abbreviations - Invalid abbreviation casing
  `* @param int      $page_id  Page ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138431**: abbreviations - Invalid abbreviation casing
  `* @param string[] $result   Array of page names keyed by ID (passed by reference).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138444**: abbreviations - Invalid abbreviation casing
  `* @param WP_Post|object|int $page Optional. Page ID or WP_Post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138459**: abbreviations - Invalid abbreviation casing
  `*     @type int          $child_of     Page ID to return child and grandchild pages of. Note: The value`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138465**: abbreviations - Invalid abbreviation casing
  `*                                      'post_modified_gmt', 'post_parent', 'ID', 'rand', 'comment_count'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138479**: abbreviations - Invalid abbreviation casing
  `*     @type int          $parent       Page ID to return direct children of. Default -1, or no restriction.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138522**: abbreviations - Invalid abbreviation casing
  `* @param string $url URL to check`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138531**: abbreviations - Invalid abbreviation casing
  `* If you set the 'ID' in the $args parameter, it will mean that you are`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138550**: abbreviations - Invalid abbreviation casing
  `* @param int          $parent_post_id   Optional. Parent post ID or 0 for no parent. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138553**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error The attachment ID on success. The value 0 or WP_Error on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138573**: abbreviations - Invalid abbreviation casing
  `* @param int  $post_id      Attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138588**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id      Attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138598**: abbreviations - Invalid abbreviation casing
  `* Retrieves attachment metadata for attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138603**: abbreviations - Invalid abbreviation casing
  `* @param int  $attachment_id Attachment post ID. Defaults to global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138633**: abbreviations - Invalid abbreviation casing
  `* @param int   $attachment_id Attachment post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138636**: abbreviations - Invalid abbreviation casing
  `*                  True on success, the Meta ID if the key didn't exist.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138643**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL for an attachment.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138649**: abbreviations - Invalid abbreviation casing
  `* @param int $attachment_id Optional. Attachment post ID. Defaults to global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138650**: abbreviations - Invalid abbreviation casing
  `* @return string|false Attachment URL, otherwise false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138660**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Optional. Attachment ID. Default is the ID of the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138667**: abbreviations - Invalid abbreviation casing
  `* Retrieves URL for an attachment thumbnail.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138672**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Optional. Attachment ID. Default is the ID of the global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138673**: abbreviations - Invalid abbreviation casing
  `* @return string|false Thumbnail URL on success, false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138684**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Attachment ID or object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138701**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Attachment ID or object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138713**: abbreviations - Invalid abbreviation casing
  `* @param string|int $mime          MIME type or attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138735**: abbreviations - Invalid abbreviation casing
  `* @param int     $post_id     Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138758**: abbreviations - Invalid abbreviation casing
  `* @param int     $post_id     Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138767**: abbreviations - Invalid abbreviation casing
  `* Retrieves the private post SQL based on capability.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138770**: abbreviations - Invalid abbreviation casing
  `* post_status of a post type. The function will return a piece of SQL code`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138771**: abbreviations - Invalid abbreviation casing
  `* that can be added to a WHERE clause; this SQL is constructed to allow all`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138778**: abbreviations - Invalid abbreviation casing
  `* @return string SQL code that can be added to a where clause.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138784**: abbreviations - Invalid abbreviation casing
  `* Retrieves the post SQL based on capability, author, and type.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138795**: abbreviations - Invalid abbreviation casing
  `* @param int             $post_author Optional. Query posts having a single author ID. Default null.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138798**: abbreviations - Invalid abbreviation casing
  `* @return string SQL WHERE code that can be added to a query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138880**: abbreviations - Invalid abbreviation casing
  `* object cache associated with the post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138889**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Post ID or post object to remove from the cache.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138933**: abbreviations - Invalid abbreviation casing
  `* Performs SQL query to retrieve the metadata for the post IDs and updates the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138935**: abbreviations - Invalid abbreviation casing
  `* function, do not need to perform SQL queries on their own.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138949**: abbreviations - Invalid abbreviation casing
  `* object cache associated with the attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138957**: abbreviations - Invalid abbreviation casing
  `* @param int  $id          The attachment ID in the cache to clean.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 138983**: abbreviations - Invalid abbreviation casing
  `* The $post properties used and must exist are 'ID' and 'post_date_gmt'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139004**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id The ID of the post being published.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139011**: abbreviations - Invalid abbreviation casing
  `* Returns the ID of the post's parent.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139016**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post|null $post Optional. Post ID or post object. Defaults to global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139017**: abbreviations - Invalid abbreviation casing
  `* @return int|false Post parent ID (which can be 0 if there is no parent),`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139033**: abbreviations - Invalid abbreviation casing
  `* @param int $post_parent ID of the parent for the post we're checking.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139034**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id     ID of the post we're checking.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139045**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post         Post ID or post object where thumbnail should be attached.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139047**: abbreviations - Invalid abbreviation casing
  `* @return int|bool Post meta ID if the key didn't exist (ie. this is the first time that`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139060**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Post ID or post object from which the thumbnail should be removed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139115**: abbreviations - Invalid abbreviation casing
  `* @param int[] $ids               ID list.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139123**: abbreviations - Invalid abbreviation casing
  `* Prime the cache containing the parent ID of various post objects.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139129**: abbreviations - Invalid abbreviation casing
  `* @param int[] $ids ID list.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139147**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id   Optional. Post ID that should be ignored. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139203**: abbreviations - Invalid abbreviation casing
  `* @param int  $attachment_id Attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139211**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL to an original attachment image.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139214**: abbreviations - Invalid abbreviation casing
  `* processed after uploading. In this case this function returns the URL`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139219**: abbreviations - Invalid abbreviation casing
  `* @param int $attachment_id Attachment post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139220**: abbreviations - Invalid abbreviation casing
  `* @return string|false Attachment image URL, false on error or if the attachment is not an image.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139233**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id         The ID of the post being restored.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139246**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Post ID or WP_Post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139255**: abbreviations - Invalid abbreviation casing
  `* The block editor depends on the REST API, and if the post type is not shown in the`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139256**: abbreviations - Invalid abbreviation casing
  `* REST API, then it won't work with the block editor.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139308**: abbreviations - Invalid abbreviation casing
  `* Retrieves the ID of the currently queried object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139316**: abbreviations - Invalid abbreviation casing
  `* @return int ID of the queried object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139435**: abbreviations - Invalid abbreviation casing
  `* @param int|string|int[]|string[] $attachment Optional. Attachment ID, title, slug, or array of such`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139456**: abbreviations - Invalid abbreviation casing
  `* @param int|string|int[]|string[] $author Optional. User ID, nickname, nicename, or array of such`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139477**: abbreviations - Invalid abbreviation casing
  `* @param int|string|int[]|string[] $category Optional. Category ID, name, slug, or array of such`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139498**: abbreviations - Invalid abbreviation casing
  `* @param int|string|int[]|string[] $tag Optional. Tag ID, name, slug, or array of such`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139525**: abbreviations - Invalid abbreviation casing
  `* @param int|string|int[]|string[] $term     Optional. Term ID, name, slug, or array of such`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139601**: abbreviations - Invalid abbreviation casing
  `* This is for what is displayed at your site's main URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139702**: abbreviations - Invalid abbreviation casing
  `* @param int|string|int[]|string[] $page Optional. Page ID, title, slug, or array of such`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 139799**: abbreviations - Invalid abbreviation casing
  `* @param int|string|int[]|string[] $post Optional. Post ID, title, slug, or array of such`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140009**: abbreviations - Invalid abbreviation casing
  `* Find the post ID for redirecting an old slug.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140018**: abbreviations - Invalid abbreviation casing
  `* @return int The Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140024**: abbreviations - Invalid abbreviation casing
  `* Find the post ID for redirecting an old date.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140033**: abbreviations - Invalid abbreviation casing
  `* @return int The Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140042**: abbreviations - Invalid abbreviation casing
  `* @since 4.4.0 Added the ability to pass a post ID to `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140046**: abbreviations - Invalid abbreviation casing
  `* @param WP_Post|object|int $post WP_Post instance or Post ID/object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140059**: abbreviations - Invalid abbreviation casing
  `* @param WP_Post|object|int $post WP_Post instance or Post ID/object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140066**: abbreviations - Invalid abbreviation casing
  `* Registers a REST API route.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140074**: abbreviations - Invalid abbreviation casing
  `* @param string $route_namespace The first URL segment after core prefix. Should be unique to your package/plugin.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140075**: abbreviations - Invalid abbreviation casing
  `* @param string $route           The base URL for route you are adding.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140118**: abbreviations - Invalid abbreviation casing
  `* Registers rewrite rules for the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140140**: abbreviations - Invalid abbreviation casing
  `* Registers the default REST API filters.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140151**: abbreviations - Invalid abbreviation casing
  `* Registers default REST API routes.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140159**: abbreviations - Invalid abbreviation casing
  `* Loads the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140170**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL prefix for any API resource.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140180**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL to a REST endpoint on a site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140182**: abbreviations - Invalid abbreviation casing
  `* Note: The returned URL is NOT escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140189**: abbreviations - Invalid abbreviation casing
  `* @param int|null $blog_id Optional. Blog ID. Default of null returns URL for current blog.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140192**: abbreviations - Invalid abbreviation casing
  `* @return string Full URL to the endpoint.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140198**: abbreviations - Invalid abbreviation casing
  `* Retrieves the URL to a REST endpoint.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140200**: abbreviations - Invalid abbreviation casing
  `* Note: The returned URL is NOT escaped.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140206**: abbreviations - Invalid abbreviation casing
  `* @return string Full URL to the endpoint.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140308**: abbreviations - Invalid abbreviation casing
  `* Sends Cross-Origin Resource Sharing headers with API requests.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140361**: abbreviations - Invalid abbreviation casing
  `* Filters the REST API response to include only an allow-listed set of response object fields.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140393**: abbreviations - Invalid abbreviation casing
  `* Adds the REST API URL to the WP RSD endpoint.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140404**: abbreviations - Invalid abbreviation casing
  `* Outputs the REST API link tag into page header.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140415**: abbreviations - Invalid abbreviation casing
  `* Sends a Link header for the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140427**: abbreviations - Invalid abbreviation casing
  `* for logged in users. However, the API has to check nonces`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140497**: abbreviations - Invalid abbreviation casing
  `* Adds Application Passwords info to the REST API index.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140514**: abbreviations - Invalid abbreviation casing
  `* @param mixed $id_or_email The avatar to retrieve a URL for. Accepts a user ID, Gravatar MD5 hash,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140516**: abbreviations - Invalid abbreviation casing
  `* @return (string|false)[] Avatar URLs keyed by size. Each value can be a URL string or boolean false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140636**: abbreviations - Invalid abbreviation casing
  `* Determines if an IP address is valid.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140642**: abbreviations - Invalid abbreviation casing
  `* @param string $ip IP address.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140643**: abbreviations - Invalid abbreviation casing
  `* @return string|false The valid IP address, otherwise false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140769**: abbreviations - Invalid abbreviation casing
  `* Stabilizes a value following JSON Schema semantics.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140782**: abbreviations - Invalid abbreviation casing
  `* Validates if the JSON Schema pattern matches a value.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140858**: abbreviations - Invalid abbreviation casing
  `* Checks the equality of two values, following JSON Schema semantics.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140874**: abbreviations - Invalid abbreviation casing
  `* Validates that the given value is a member of the JSON Schema "enum".`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140887**: abbreviations - Invalid abbreviation casing
  `* Get all valid JSON schema properties.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 140891**: abbreviations - Invalid abbreviation casing
  `* @return string[] All valid JSON schema properties.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141027**: abbreviations - Invalid abbreviation casing
  `* Append result of internal request to REST API for purpose of preloading data to be attached to a page.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141033**: abbreviations - Invalid abbreviation casing
  `* @param string $path REST API path to preload.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141078**: abbreviations - Invalid abbreviation casing
  `* Gets the REST API route for a post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141082**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141090**: abbreviations - Invalid abbreviation casing
  `* Gets the REST API route for a post type.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141102**: abbreviations - Invalid abbreviation casing
  `* Gets the REST API route for a term.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141106**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Term $term Term ID or term object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141114**: abbreviations - Invalid abbreviation casing
  `* Gets the REST API route for a taxonomy.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141139**: abbreviations - Invalid abbreviation casing
  `* @param array  $schema The full JSON schema for the endpoint.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141153**: abbreviations - Invalid abbreviation casing
  `* list in JSON rather than an object/map.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141165**: abbreviations - Invalid abbreviation casing
  `* Checks whether a REST API endpoint request is currently being handled.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141167**: abbreviations - Invalid abbreviation casing
  `* This may be a standalone REST API request, or an internal request dispatched from within a regular page load.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141229**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id The ID of the post to save as a revision.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141230**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error|void Void or 0 if error, new revision ID, if success.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141244**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id The post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141245**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id Optional. The post author ID. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141256**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141257**: abbreviations - Invalid abbreviation casing
  `* @return int|false ID of revision's parent on success, false if not a revision.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141268**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141269**: abbreviations - Invalid abbreviation casing
  `* @return int|false ID of autosave's parent on success, false if not a revision.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141280**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post|array|null $post     Post ID, post object OR post array.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141283**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error WP_Error or 0 if error, new revision ID if success.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141293**: abbreviations - Invalid abbreviation casing
  `* @param int $revision_id The ID of the revision to save the meta to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141294**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id     The ID of the post the revision is associated with.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141305**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post   Post ID or post object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141322**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $revision Revision ID or revision object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141324**: abbreviations - Invalid abbreviation casing
  `* @return int|false|null Null if error, false if no fields to restore, (int) post ID if success.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141334**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id     The ID of the post to restore the meta to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141335**: abbreviations - Invalid abbreviation casing
  `* @param int $revision_id The ID of the revision to restore the meta from.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141346**: abbreviations - Invalid abbreviation casing
  `* @param int    $source_post_id Post ID to copy meta value(s) from.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141347**: abbreviations - Invalid abbreviation casing
  `* @param int    $target_post_id Post ID to copy meta value(s) to.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141384**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $revision Revision ID or revision object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141397**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141405**: abbreviations - Invalid abbreviation casing
  `* Returns the latest revision ID and count of revisions for a post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141409**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141411**: abbreviations - Invalid abbreviation casing
  `*     Returns associative array with latest revision ID and total count,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141414**: abbreviations - Invalid abbreviation casing
  `*     @type int $latest_id The latest revision post ID or 0 if no revisions exist.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141430**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global `$post`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141431**: abbreviations - Invalid abbreviation casing
  `* @return string|null The URL for editing revisions on the given post, otherwise null.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141505**: abbreviations - Invalid abbreviation casing
  `* @param int               $post_id  Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141547**: abbreviations - Invalid abbreviation casing
  `* @param int    $object_id Object ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141551**: abbreviations - Invalid abbreviation casing
  `*               the post type is a revision or the post ID doesn't match the object ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141558**: abbreviations - Invalid abbreviation casing
  `* Adds a rewrite rule that transforms a URL structure to a set of query vars.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141689**: abbreviations - Invalid abbreviation casing
  `* where $match is the part of the URL matched by the endpoint regex (e.g. "foo" in`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141732**: abbreviations - Invalid abbreviation casing
  `* Filters the URL base for taxonomies.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141750**: abbreviations - Invalid abbreviation casing
  `* a post with post_name '05' has the URL `/2015/05/`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141758**: abbreviations - Invalid abbreviation casing
  `* the site's permalink structure in a way that introduces URL conflicts.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141770**: abbreviations - Invalid abbreviation casing
  `* Examines a URL and try to determine the post ID it represents.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 141780**: abbreviations - Invalid abbreviation casing
  `* @return int Post ID, or 0 on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 142131**: abbreviations - Invalid abbreviation casing
  `* a 'url' parameter that has the URL path to the CSS file.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 142133**: abbreviations - Invalid abbreviation casing
  `* The query from $src parameter will be appended to the URL that is given from`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 142134**: abbreviations - Invalid abbreviation casing
  `* the $_wp_admin_css_colors array value URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 142140**: abbreviations - Invalid abbreviation casing
  `* @param string $src    Source URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 142142**: abbreviations - Invalid abbreviation casing
  `* @return string|false URL path to CSS stylesheet for Administration Screens.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 142519**: abbreviations - Invalid abbreviation casing
  `* @param string $stylesheet_url The URL to the stylesheet.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 142553**: abbreviations - Invalid abbreviation casing
  `* Styles are stored via the style engine API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 142591**: abbreviations - Invalid abbreviation casing
  `*     @type string|false     $src    The source URL of the stylesheet.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 142775**: abbreviations - Invalid abbreviation casing
  `* @param string            $src     Optional. Full URL of the script module, or path of the script module relative`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 142794**: abbreviations - Invalid abbreviation casing
  `*                                   It is added to the URL as a query string for cache busting purposes. If $version`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 142826**: abbreviations - Invalid abbreviation casing
  `* @param string            $src     Optional. Full URL of the script module, or path of the script module relative`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 142845**: abbreviations - Invalid abbreviation casing
  `*                                   It is added to the URL as a query string for cache busting purposes. If $version`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 143254**: abbreviations - Invalid abbreviation casing
  `* Retrieves the full URL for a sitemap.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 143261**: abbreviations - Invalid abbreviation casing
  `* @return string|false The sitemap URL or false if the sitemap doesn't exist.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 143282**: abbreviations - Invalid abbreviation casing
  `* {@see 'wp_speculation_rules_href_exclude_paths'} filter to ensure those URL patterns are excluded.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 143592**: abbreviations - Invalid abbreviation casing
  `*              arguments to register the taxonomy in REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 143627**: abbreviations - Invalid abbreviation casing
  `*     @type bool          $show_in_rest          Whether to include the taxonomy in the REST API. Set this to true`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 143629**: abbreviations - Invalid abbreviation casing
  `*     @type string        $rest_base             To change the base url of REST API route. Default is $taxonomy.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 143630**: abbreviations - Invalid abbreviation casing
  `*     @type string        $rest_namespace        To change the namespace URL of REST API route. Default is wp/v2.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 143631**: abbreviations - Invalid abbreviation casing
  `*     @type string        $rest_controller_class REST API Controller class name. Default is 'WP_REST_Terms_Controller'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 143776**: abbreviations - Invalid abbreviation casing
  `*                                              Default 'The &#8220;slug&#8221; is the URL-friendly version`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 143893**: abbreviations - Invalid abbreviation casing
  `* @param int|int[]       $term_ids   Term ID or array of term IDs of terms that will be used.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 143910**: abbreviations - Invalid abbreviation casing
  `* Given a taxonomy query, generates SQL to be appended to a main query.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 143925**: abbreviations - Invalid abbreviation casing
  `* Gets all term data from database by term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 143931**: abbreviations - Invalid abbreviation casing
  `* $term ID must be part of $taxonomy, to get from the database. Failure, might`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 143999**: abbreviations - Invalid abbreviation casing
  `* @since 5.5.0 Added 'ID' as an alias of 'id' for the `$field` parameter.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144003**: abbreviations - Invalid abbreviation casing
  `* @param string     $field    Either 'slug', 'name', 'term_id' (or 'id', 'ID'), or 'term_taxonomy_id'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144027**: abbreviations - Invalid abbreviation casing
  `* @param int    $term_id  ID of term to get children.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144045**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Term $term     Term ID or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144062**: abbreviations - Invalid abbreviation casing
  `* @param int|object $id       Term ID or object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144167**: abbreviations - Invalid abbreviation casing
  `* @param int    $term_id    Term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144178**: abbreviations - Invalid abbreviation casing
  `* @return int|false|WP_Error Meta ID on success, false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144191**: abbreviations - Invalid abbreviation casing
  `* @param int    $term_id    Term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144206**: abbreviations - Invalid abbreviation casing
  `* @param int    $term_id Term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144215**: abbreviations - Invalid abbreviation casing
  `*               An empty array if a valid but non-existing term ID is passed and `$single` is false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144216**: abbreviations - Invalid abbreviation casing
  `*               An empty string if a valid but non-existing term ID is passed and `$single` is true.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144229**: abbreviations - Invalid abbreviation casing
  `* Use the `$prev_value` parameter to differentiate between meta fields with the same key and term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144237**: abbreviations - Invalid abbreviation casing
  `* @param int    $term_id    Term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144243**: abbreviations - Invalid abbreviation casing
  `* @return int|bool|WP_Error Meta ID if the key didn't exist. true on successful update,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144254**: abbreviations - Invalid abbreviation casing
  `* Performs SQL query to retrieve all metadata for the terms matching `$term_ids` and stores them in the cache.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144277**: abbreviations - Invalid abbreviation casing
  `* Gets all meta data, including meta IDs, for the given term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144283**: abbreviations - Invalid abbreviation casing
  `* @param int $term_id Term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144344**: abbreviations - Invalid abbreviation casing
  `* @param int|string $term        The term to check. Accepts term ID, slug, or name.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144346**: abbreviations - Invalid abbreviation casing
  `* @param int        $parent_term Optional. ID of parent term under which to confine the exists search.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144348**: abbreviations - Invalid abbreviation casing
  `*               Returns the term ID if no taxonomy is specified and the term ID exists.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144349**: abbreviations - Invalid abbreviation casing
  `*               Returns an array of the term ID and the term taxonomy ID if the taxonomy is specified and the pairing exists.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144350**: abbreviations - Invalid abbreviation casing
  `*               Returns 0 if term ID 0 is passed to the function.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144359**: abbreviations - Invalid abbreviation casing
  `* You can use either an ID or the term object for both parameters.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144363**: abbreviations - Invalid abbreviation casing
  `* @param int|object $term1    ID or object to check if this is the parent term.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144413**: abbreviations - Invalid abbreviation casing
  `* @param int    $term_id  Term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144494**: abbreviations - Invalid abbreviation casing
  `* @param int          $object_id  The term object ID that refers to the term.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144512**: abbreviations - Invalid abbreviation casing
  `* @param int          $term     Term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144515**: abbreviations - Invalid abbreviation casing
  `*     Optional. Array of arguments to override the default term ID. Default empty array.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144517**: abbreviations - Invalid abbreviation casing
  `*     @type int  $default       The term ID to make the default term. This will only override`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144539**: abbreviations - Invalid abbreviation casing
  `* @param int $cat_id Category term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144559**: abbreviations - Invalid abbreviation casing
  `* @param int|int[]       $object_ids The ID(s) of the object(s) to retrieve.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144648**: abbreviations - Invalid abbreviation casing
  `*     @type int        $term_id          The new term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144649**: abbreviations - Invalid abbreviation casing
  `*     @type int|string $term_taxonomy_id The new term taxonomy ID. Can be a numeric string.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144681**: abbreviations - Invalid abbreviation casing
  `* @param string|int|array $terms     A single term slug, single term ID, or array of either term slugs or IDs.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144696**: abbreviations - Invalid abbreviation casing
  `* @param int              $object_id The ID of the object to which the terms will be added.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144697**: abbreviations - Invalid abbreviation casing
  `* @param string|int|array $terms     The slug(s) or ID(s) of the term(s) to add.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144711**: abbreviations - Invalid abbreviation casing
  `* @param int              $object_id The ID of the object from which the terms will be removed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144712**: abbreviations - Invalid abbreviation casing
  `* @param string|int|array $terms     The slug(s) or ID(s) of the term(s) to remove.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144766**: abbreviations - Invalid abbreviation casing
  `* @param int          $term_id  The ID of the term.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144807**: abbreviations - Invalid abbreviation casing
  `* of term ID. Once that is done, then update the database.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144844**: abbreviations - Invalid abbreviation casing
  `* @param int|array    $object_ids  Single or list of term object ID(s).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144880**: abbreviations - Invalid abbreviation casing
  `* Retrieves the cached term objects for the given object ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144890**: abbreviations - Invalid abbreviation casing
  `* @param int    $id       Term object ID, for example a post, comment, or user ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 144900**: abbreviations - Invalid abbreviation casing
  `* Updates the cache for the given term object ID(s).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145040**: abbreviations - Invalid abbreviation casing
  `* @param int|object $term_id          ID of the shared term, or the shared term object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145041**: abbreviations - Invalid abbreviation casing
  `* @param int|object $term_taxonomy_id ID of the term_taxonomy item to receive a new term, or the term_taxonomy object`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145082**: abbreviations - Invalid abbreviation casing
  `* @param int    $term_id          ID of the formerly shared term.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145083**: abbreviations - Invalid abbreviation casing
  `* @param int    $new_term_id      ID of the new term created for the $term_taxonomy_id.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145084**: abbreviations - Invalid abbreviation casing
  `* @param int    $term_taxonomy_id ID for the term_taxonomy row affected by the split.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145099**: abbreviations - Invalid abbreviation casing
  `* @param int    $term_id          ID of the formerly shared term.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145100**: abbreviations - Invalid abbreviation casing
  `* @param int    $new_term_id      ID of the new term created for the $term_taxonomy_id.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145101**: abbreviations - Invalid abbreviation casing
  `* @param int    $term_taxonomy_id ID for the term_taxonomy row affected by the split.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145113**: abbreviations - Invalid abbreviation casing
  `* @param int    $term_id          ID of the formerly shared term.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145114**: abbreviations - Invalid abbreviation casing
  `* @param int    $new_term_id      ID of the new term created for the $term_taxonomy_id.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145115**: abbreviations - Invalid abbreviation casing
  `* @param int    $term_taxonomy_id ID for the term_taxonomy row affected by the split.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145127**: abbreviations - Invalid abbreviation casing
  `* @param int $old_term_id Term ID. This is the old, pre-split term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145134**: abbreviations - Invalid abbreviation casing
  `* Gets the new term ID corresponding to a previously split term.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145138**: abbreviations - Invalid abbreviation casing
  `* @param int    $old_term_id Term ID. This is the old, pre-split term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145157**: abbreviations - Invalid abbreviation casing
  `* @param int $term_id Term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145171**: abbreviations - Invalid abbreviation casing
  `* @param WP_Term|int|string $term     The term object, ID, or slug whose link will be retrieved.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145173**: abbreviations - Invalid abbreviation casing
  `* @return string|WP_Error URL of the taxonomy term archive on success, WP_Error if term does not exist.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145182**: abbreviations - Invalid abbreviation casing
  `* post without specifying the Post ID. You can also use it outside the Loop to`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145191**: abbreviations - Invalid abbreviation casing
  `*     @type int|WP_Post $post   Post ID or object to get taxonomies of. Default current post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145214**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145237**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Post $post Optional. Post ID or WP_Post object. Default is global $post.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145252**: abbreviations - Invalid abbreviation casing
  `* @param int                       $object_id ID of the object (post ID, link ID, ...).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145254**: abbreviations - Invalid abbreviation casing
  `* @param int|string|int[]|string[] $terms     Optional. Term ID, name, slug, or array of such`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145279**: abbreviations - Invalid abbreviation casing
  `* @param int    $object_id     Optional. The ID of the object. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145291**: abbreviations - Invalid abbreviation casing
  `* Returns the term's parent's term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145295**: abbreviations - Invalid abbreviation casing
  `* @param int    $term_id  Term ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145297**: abbreviations - Invalid abbreviation casing
  `* @return int|false Parent term ID on success, false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145336**: abbreviations - Invalid abbreviation casing
  `* @param int|WP_Term $term Term ID or term object.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145890**: abbreviations - Invalid abbreviation casing
  `* This adds a `wp_theme_preview` URL parameter to API requests from the Site Editor, so they also respond as if the theme is set to the value of the parameter.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145931**: abbreviations - Invalid abbreviation casing
  `* @param int $post_id Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145945**: abbreviations - Invalid abbreviation casing
  `* @param int    $post_id       Post ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 145995**: abbreviations - Invalid abbreviation casing
  `*     @type int   $blog_id (Multisite) The blog ID used to calculate which themes`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 146459**: abbreviations - Invalid abbreviation casing
  `* Gets random header image URL from registered images in theme.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 146485**: abbreviations - Invalid abbreviation casing
  `* Displays header image URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 146521**: abbreviations - Invalid abbreviation casing
  `* @param array $headers Array of headers keyed by a string ID. The IDs point to arrays`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 146558**: abbreviations - Invalid abbreviation casing
  `* Retrieves header video URL for custom header.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 146564**: abbreviations - Invalid abbreviation casing
  `* @return string|false Header video URL or false if there is no video.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 146570**: abbreviations - Invalid abbreviation casing
  `* Displays header video URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147017**: abbreviations - Invalid abbreviation casing
  `*                                    the Themes REST API schema. Intended for developers.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147019**: abbreviations - Invalid abbreviation casing
  `*         Whether this feature should be included in the Themes REST API endpoint.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147023**: abbreviations - Invalid abbreviation casing
  `*         @type array    $schema           Specifies the JSON Schema definition describing`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147027**: abbreviations - Invalid abbreviation casing
  `*                                          in the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147028**: abbreviations - Invalid abbreviation casing
  `*         @type callable $prepare_callback A function used to format the theme support in the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147085**: abbreviations - Invalid abbreviation casing
  `* @param int $id The attachment ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147159**: abbreviations - Invalid abbreviation casing
  `* Returns a URL to load the Customizer.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147296**: abbreviations - Invalid abbreviation casing
  `* @param array $extra_stats Extra statistics to report to the WordPress.org API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147315**: abbreviations - Invalid abbreviation casing
  `* @param array $extra_stats Extra statistics to report to the WordPress.org API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147332**: abbreviations - Invalid abbreviation casing
  `* @param array $extra_stats Extra statistics to report to the WordPress.org API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147558**: abbreviations - Invalid abbreviation casing
  `*                               null is passed in and this isn't an API request.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147573**: abbreviations - Invalid abbreviation casing
  `* @param int|false $input_user User ID if one has been determined, false otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147574**: abbreviations - Invalid abbreviation casing
  `* @return int|false The authenticated user ID if successful, false otherwise.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147597**: abbreviations - Invalid abbreviation casing
  `* This is a callback for the {@see 'determine_current_user'} filter, rather than API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147601**: abbreviations - Invalid abbreviation casing
  `* @param int|false $user_id The user ID (or false) as received from`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147603**: abbreviations - Invalid abbreviation casing
  `* @return int|false User ID if validated, false otherwise. If a user ID from`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147619**: abbreviations - Invalid abbreviation casing
  `* @param int          $userid      User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147638**: abbreviations - Invalid abbreviation casing
  `* @return array<int, string> Amount of posts each user has written, as strings, keyed by user ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147644**: abbreviations - Invalid abbreviation casing
  `* Gets the current user's ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147648**: abbreviations - Invalid abbreviation casing
  `* @return int The current user's ID, or 0 if no user is logged in.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147657**: abbreviations - Invalid abbreviation casing
  `* If the user ID is not given, then the current user will be used instead. If`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147658**: abbreviations - Invalid abbreviation casing
  `* the user ID is given, then the user data will be retrieved. The filter for`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147669**: abbreviations - Invalid abbreviation casing
  `* @param int    $user       Optional. User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147689**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id     User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147694**: abbreviations - Invalid abbreviation casing
  `* @return int|bool User meta ID if the option didn't exist, true on successful update,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147711**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id     User ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147721**: abbreviations - Invalid abbreviation casing
  `* Retrieves user info by user ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147725**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147793**: abbreviations - Invalid abbreviation casing
  `*                                 'display_name', 'post_count', 'ID', 'meta_value', 'user_login'. Default 'name'.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147800**: abbreviations - Invalid abbreviation casing
  `*     @type string $feed_image    If not empty, show a link to the user's feed and use this image URL as`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147838**: abbreviations - Invalid abbreviation casing
  `* @param int  $user_id User ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147854**: abbreviations - Invalid abbreviation casing
  `* @param int $user_id Optional. The unique ID of the user. Defaults to the current user.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147855**: abbreviations - Invalid abbreviation casing
  `* @param int $blog_id Optional. ID of the blog to check. Defaults to the current site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147868**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id    User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147879**: abbreviations - Invalid abbreviation casing
  `* @return int|false Meta ID on success, false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147897**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id    User ID`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147914**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147923**: abbreviations - Invalid abbreviation casing
  `*               An empty array if a valid but non-existing user ID is passed and `$single` is false.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147924**: abbreviations - Invalid abbreviation casing
  `*               An empty string if a valid but non-existing user ID is passed and `$single` is true.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147935**: abbreviations - Invalid abbreviation casing
  `* Updates user meta field based on user ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147938**: abbreviations - Invalid abbreviation casing
  `* same key and user ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147948**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id    User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147954**: abbreviations - Invalid abbreviation casing
  `* @return int|bool Meta ID if the key didn't exist, true on successful update,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 147977**: abbreviations - Invalid abbreviation casing
  `* @param int|null $site_id  Optional. The site ID to count users for. Defaults to the current site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148002**: abbreviations - Invalid abbreviation casing
  `* @param int|null $network_id ID of the network. Defaults to the current network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148013**: abbreviations - Invalid abbreviation casing
  `* @param int|null $network_id ID of the network. Defaults to the current network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148025**: abbreviations - Invalid abbreviation casing
  `* @param int|null $network_id ID of the network. Defaults to the current network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148047**: abbreviations - Invalid abbreviation casing
  `* @param int|null $network_id ID of the network. Defaults to the current network.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148063**: abbreviations - Invalid abbreviation casing
  `* @global int     $user_ID       The ID of the user`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148068**: abbreviations - Invalid abbreviation casing
  `* @param int $for_user_id Optional. User ID to set up global data. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148109**: abbreviations - Invalid abbreviation casing
  `*     @type bool|int        $multi                   Whether to skip the ID attribute on the 'select' element.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148118**: abbreviations - Invalid abbreviation casing
  `*     @type int             $selected                Which user ID should be selected. Default 0.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148119**: abbreviations - Invalid abbreviation casing
  `*     @type bool            $include_selected        Whether to always include the selected user ID in the drop-`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148122**: abbreviations - Invalid abbreviation casing
  `*     @type string          $id                      ID attribute of the select element. Default is the value of `$name`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148124**: abbreviations - Invalid abbreviation casing
  `*     @type int             $blog_id                 ID of blog (Multisite only). Default is ID of the current blog.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148225**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id User ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148251**: abbreviations - Invalid abbreviation casing
  `* @param WP_User|int $user User object or ID to be cleaned from the cache`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148267**: abbreviations - Invalid abbreviation casing
  `* @return int|false The user ID on success, false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148282**: abbreviations - Invalid abbreviation casing
  `* @return int|false The user ID on success, false on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148303**: abbreviations - Invalid abbreviation casing
  `* 'ID', 'rich_editing', 'syntax_highlighting', 'comment_shortcuts', 'admin_color', 'use_ssl',`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148321**: abbreviations - Invalid abbreviation casing
  `*     @type int    $ID                   User ID. If supplied, the user will be updated.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148325**: abbreviations - Invalid abbreviation casing
  `*     @type string $user_nicename        The URL-friendly user name.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148326**: abbreviations - Invalid abbreviation casing
  `*     @type string $user_url             The user URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148363**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error The newly created user's ID or a WP_Error object if the user could not`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148366**: abbreviations - Invalid abbreviation casing
  `*   ID?: int,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148408**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error The updated user's ID or a WP_Error object if the user could not be updated.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148426**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error The newly created user's ID or a WP_Error object if the user could not`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148563**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error Either user's ID or error on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148577**: abbreviations - Invalid abbreviation casing
  `* @param int    $user_id ID of the newly created user.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148638**: abbreviations - Invalid abbreviation casing
  `* @param int|null $site_id Optional. The site ID to get users with no role for. Defaults to the current site.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148739**: abbreviations - Invalid abbreviation casing
  `* @param int $request_id ID of the request.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148753**: abbreviations - Invalid abbreviation casing
  `* @param int $request_id The ID of the request.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148766**: abbreviations - Invalid abbreviation casing
  `* @param int $request_id The privacy request post ID associated with this request.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148778**: abbreviations - Invalid abbreviation casing
  `* @param int $request_id The request ID being confirmed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148799**: abbreviations - Invalid abbreviation casing
  `* @return int|WP_Error                   Returns the request ID if successful, or a WP_Error object on failure.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148822**: abbreviations - Invalid abbreviation casing
  `* @param int $request_id ID of the request created via wp_create_user_request().`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148833**: abbreviations - Invalid abbreviation casing
  `* @param int $request_id Request ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148844**: abbreviations - Invalid abbreviation casing
  `* @param int    $request_id ID of the request being confirmed.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148856**: abbreviations - Invalid abbreviation casing
  `* Returns the user request object for the specified request ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 148860**: abbreviations - Invalid abbreviation casing
  `* @param int $request_id The ID of the user request.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149114**: abbreviations - Invalid abbreviation casing
  `* @global array $wp_registered_sidebars The new sidebars are stored in this array by sidebar ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149121**: abbreviations - Invalid abbreviation casing
  `*                        sidebars are being defined, the ID will have "-2" appended, and so on.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149137**: abbreviations - Invalid abbreviation casing
  `* Builds the definition for a single sidebar and returns the ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149141**: abbreviations - Invalid abbreviation casing
  `* generate a sidebar ID and name based on the current number of registered`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149144**: abbreviations - Invalid abbreviation casing
  `* When allowing for automatic generation of the name and ID parameters, keep`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149169**: abbreviations - Invalid abbreviation casing
  `*                                  to this sidebar. Receives the widget's ID attribute as `%1$s``
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149184**: abbreviations - Invalid abbreviation casing
  `*     @type bool $show_in_rest     Whether to show this sidebar publicly in the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149187**: abbreviations - Invalid abbreviation casing
  `* @return string Sidebar ID added to $wp_registered_sidebars global.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149212**: abbreviations - Invalid abbreviation casing
  `* @param string|int $sidebar_id The ID of the sidebar when it was registered.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149224**: abbreviations - Invalid abbreviation casing
  `* @param string|int $sidebar_id The ID of the sidebar when it was registered.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149248**: abbreviations - Invalid abbreviation casing
  `* @param int|string $id              Widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149258**: abbreviations - Invalid abbreviation casing
  `*     @type bool   $show_instance_in_rest Whether to show the widget's instance settings in the REST API.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149283**: abbreviations - Invalid abbreviation casing
  `* @param int|string $id Widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149299**: abbreviations - Invalid abbreviation casing
  `* @param string $id sidebar ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149310**: abbreviations - Invalid abbreviation casing
  `* @param int|string $id Widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149327**: abbreviations - Invalid abbreviation casing
  `* @param int|string $id               Sidebar ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149337**: abbreviations - Invalid abbreviation casing
  `*                               text widget. The widget ID will end up looking like `{$id_base}-{$unique_number}`.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149359**: abbreviations - Invalid abbreviation casing
  `* @param string   $id_base         The base ID of a widget created by extending WP_Widget.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149383**: abbreviations - Invalid abbreviation casing
  `* @param int|string $id            Widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149404**: abbreviations - Invalid abbreviation casing
  `* @param int|string $id Widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149413**: abbreviations - Invalid abbreviation casing
  `* 'name' parameter for its registered sidebars you can pass an ID or name as the $index parameter.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149421**: abbreviations - Invalid abbreviation casing
  `* @param int|string $index Optional. Index, name or ID of dynamic sidebar. Default 1.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149432**: abbreviations - Invalid abbreviation casing
  `* Without the optional $widget_id parameter, returns the ID of the first sidebar`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149434**: abbreviations - Invalid abbreviation casing
  `* With the $widget_id parameter, returns the ID of the sidebar where`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149435**: abbreviations - Invalid abbreviation casing
  `* the widget with that callback/$id_base AND that ID is found.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149449**: abbreviations - Invalid abbreviation casing
  `* @param string|false   $widget_id     Optional. Widget ID. Optional, but needed for checking.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149451**: abbreviations - Invalid abbreviation casing
  `* @param string|false   $id_base       Optional. The base ID of a widget created by extending WP_Widget.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149455**: abbreviations - Invalid abbreviation casing
  `* @return string|false ID of the sidebar in which the widget is active,`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149512**: abbreviations - Invalid abbreviation casing
  `* Retrieves the registered sidebar with the given ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149518**: abbreviations - Invalid abbreviation casing
  `* @param string $id The sidebar ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149556**: abbreviations - Invalid abbreviation casing
  `* @param string $base_name   Root ID for all widgets of this type.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149597**: abbreviations - Invalid abbreviation casing
  `* Retrieves the widget ID base value.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149601**: abbreviations - Invalid abbreviation casing
  `* @param string $id Widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149602**: abbreviations - Invalid abbreviation casing
  `* @return string Widget ID base.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149757**: abbreviations - Invalid abbreviation casing
  `* Converts a widget ID into its id_base and number components.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149761**: abbreviations - Invalid abbreviation casing
  `* @param string $id Widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149772**: abbreviations - Invalid abbreviation casing
  `* @param string $widget_id The widget ID to look for.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149773**: abbreviations - Invalid abbreviation casing
  `* @return string|null The found sidebar's ID, or null if it was not found.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149783**: abbreviations - Invalid abbreviation casing
  `* @param string $widget_id  The widget ID to assign.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149784**: abbreviations - Invalid abbreviation casing
  `* @param string $sidebar_id The sidebar ID to assign to. If empty, the widget won't be added to any sidebar.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149797**: abbreviations - Invalid abbreviation casing
  `* @param string $widget_id Widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149798**: abbreviations - Invalid abbreviation casing
  `* @param string $sidebar_id Sidebar ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149811**: abbreviations - Invalid abbreviation casing
  `* @param string $id Widget ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 149995**: abbreviations - Invalid abbreviation casing
  `* @param string $domain     The domain URL.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 150001**: abbreviations - Invalid abbreviation casing
  `* @param int    $blog_id    The site ID.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

