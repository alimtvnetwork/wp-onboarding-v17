# Subtask 474: Fix violations in src/pages/Plugins.tsx

Target File: `src/pages/Plugins.tsx`

## Violations

- **Line 350**: abbreviations - Invalid abbreviation casing
  `// Trigger sync via API - the dialog will track progress via WebSocket`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 713**: abbreviations - Invalid abbreviation casing
  `// Fetch fresh mappings from API instead of using potentially stale plugin.mappings`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 720**: abbreviations - Invalid abbreviation casing
  `// Fallback to local data if API fails`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 1231**: abbreviations - Invalid abbreviation casing
  `Requires backend folder picker API (coming soon)`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 1253**: abbreviations - Invalid abbreviation casing
  `<Label htmlFor="git-url">Git Remote URL (optional)</Label>`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

