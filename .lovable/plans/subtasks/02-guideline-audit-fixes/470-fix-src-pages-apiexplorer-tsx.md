# Subtask 470: Fix violations in src/pages/ApiExplorer.tsx

Target File: `src/pages/ApiExplorer.tsx`

## Violations

- **Line 49**: abbreviations - Invalid abbreviation casing
  `// Auto-select site from URL query param`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 127**: abbreviations - Invalid abbreviation casing
  `throw new Error(`Failed to fetch API spec: ${response.status} ${response.statusText}`);`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 131**: abbreviations - Invalid abbreviation casing
  `addHistoryItem({ ...historyBase, responseBody: JSON.stringify(data, null, 2).slice(0, 500) + "..." });`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 173**: abbreviations - Invalid abbreviation casing
  `addHistoryItem({ ...historyBase, responseBody: JSON.stringify(data, null, 2).slice(0, 500) + "..." });`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 175**: abbreviations - Invalid abbreviation casing
  `// Inject the correct server base URL so "Try it out" hits the right host`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 266**: abbreviations - Invalid abbreviation casing
  `API Explorer`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 269**: abbreviations - Invalid abbreviation casing
  `Browse and test API endpoints with Swagger UI`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 303**: abbreviations - Invalid abbreviation casing
  `<span className="hidden xs:inline">Backend</span> API`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 307**: abbreviations - Invalid abbreviation casing
  `<span className="hidden xs:inline">WordPress</span> API`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 317**: abbreviations - Invalid abbreviation casing
  `{/* Backend API mode */}`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 332**: abbreviations - Invalid abbreviation casing
  `<p className="text-muted-foreground">Loading backend API specification...</p>`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 345**: abbreviations - Invalid abbreviation casing
  `Backend API`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 348**: abbreviations - Invalid abbreviation casing
  `Go backend REST API — auto-generated from handler registrations`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 362**: abbreviations - Invalid abbreviation casing
  `Raw JSON`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 386**: abbreviations - Invalid abbreviation casing
  `{/* WordPress API mode */}`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 470**: abbreviations - Invalid abbreviation casing
  `<p className="text-muted-foreground">Loading API specification...</p>`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 497**: abbreviations - Invalid abbreviation casing
  `<CardTitle className="text-lg">Riseup Asia Uploader API</CardTitle>`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 499**: abbreviations - Invalid abbreviation casing
  `Interactive API documentation - expand endpoints to test them`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 511**: abbreviations - Invalid abbreviation casing
  `Raw JSON`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

