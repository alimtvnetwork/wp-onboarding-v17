# Subtask 428: Fix violations in src/components/sites/AddSiteDialog.tsx

Target File: `src/components/sites/AddSiteDialog.tsx`

## Violations

- **Line 110**: abbreviations - Invalid abbreviation casing
  `toast.error("URL, username, and password are required to test");`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 281**: abbreviations - Invalid abbreviation casing
  `Connect a WordPress site using its REST API credentials.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 303**: abbreviations - Invalid abbreviation casing
  `<Label htmlFor="url">Site URL</Label>`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

