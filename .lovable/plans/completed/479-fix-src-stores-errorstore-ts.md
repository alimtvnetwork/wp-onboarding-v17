Status: completed

# Subtask 479: Fix violations in src/stores/errorStore.ts

Target File: `src/stores/errorStore.ts`

## Violations

- **Line 76**: abbreviations - Invalid abbreviation casing
  `// Additional fields for API errors`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 355**: abbreviations - Invalid abbreviation casing
  `// API / request meta`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

