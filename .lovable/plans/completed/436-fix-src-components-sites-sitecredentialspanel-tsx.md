# Subtask 436: Fix violations in src/components/sites/SiteCredentialsPanel.tsx

Target File: `src/components/sites/SiteCredentialsPanel.tsx`

## Violations

- **Line 145**: abbreviations - Invalid abbreviation casing
  `Manage application passwords for this site. The default credential is used for all API operations.`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

