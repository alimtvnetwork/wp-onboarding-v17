# Subtask 394: Fix violations in src/components/debug/DebugRoutesPanel.tsx

Target File: `src/components/debug/DebugRoutesPanel.tsx`

## Violations

- **Line 154**: abbreviations - Invalid abbreviation casing
  `Select a site to view registered REST API routes`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

