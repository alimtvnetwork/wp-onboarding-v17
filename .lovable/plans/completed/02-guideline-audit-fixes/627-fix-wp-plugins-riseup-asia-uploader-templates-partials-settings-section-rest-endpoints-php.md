Status: completed
# Subtask 627: Fix violations in wp-plugins/riseup-asia-uploader/templates/partials/settings/section-rest-endpoints.php  Target File: `wp-plugins/riseup-asia-uploader/templates/partials/settings/section-rest-endpoints.php`  ## Violations  - **Line 3**: abbreviations - Invalid abbreviation casing   `* Settings Partial — REST API Endpoints reference table.`   **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).  - **Line 18**: abbreviations - Invalid abbreviation casing   `<!-- REST API Endpoints -->`   **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).  - **Line 22**: abbreviations - Invalid abbreviation casing   `<?php esc_html_e('REST API Endpoints', $pluginSlug); ?>`   **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).  - **Line 25**: abbreviations - Invalid abbreviation casing   `<?php esc_html_e('Base URL:', $pluginSlug); ?> <code><?php echo esc_url(rest_url(PluginConfigType::apiFullNamespace())); ?></code>`   **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL). 
