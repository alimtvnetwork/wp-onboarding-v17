# Subtask 420: Fix violations in src/components/publish/SiteVersionBadge.tsx

Target File: `src/components/publish/SiteVersionBadge.tsx`

## Violations

- **Line 3**: abbreviations - Invalid abbreviation casing
  `* Local version renders instantly from props (no API wait).`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

- **Line 14**: abbreviations - Invalid abbreviation casing
  `/** Local version — rendered instantly, no API call needed */`
  **Instruction**: Change the abbreviation to standard PascalCase/camelCase (e.g. Id instead of ID, Url instead of URL).

